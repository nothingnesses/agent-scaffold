# agent-scaffold plan

Status: in progress; see the Roadmap for per-step status and order. In short: the core is complete (the assets and principle data, the file-dropper with two-tier ownership, the idempotency and write-safety pass, the selection UI and its polish, and bring-your-own-template support), `init-vcs` (a git repository by default, `--vcs none` to disable) is complete. A workflow-hardening review round (round 1; see `docs/plans/agent-scaffold.ledger.md`) produced 14 triager-valid findings, and the human queued four workflow-interface features; both are folded into concrete Roadmap steps, correctness before features: `convergence-accounting` (complete, committed `19d69e5`) and `workflow-doc-fixes` for the findings, then `human-onboarding`, `deliberation-mode`, `human-review-queue`, and `ledger-template` for the features. Two more steps joined during implementation: `pack-rebuild-tracking` (to be done before the golden sync-test in `ledger-template`; a `build.rs` so the embedded pack re-embeds on change, since `include_dir!` does not register rebuild dependencies and `scaffold-self` could regenerate from a stale pack) and `triager-independence` (the triager is always a separate agent, independent of both the producer and the orchestrator). A structured state-file request is held as the deferred `state-schema` step (`Q-11`). Open decisions live in the living human-decision queue in the Open Questions section; the earlier optional and deferred steps remain optional. Published to crates.io as `agent-scaffold` v0.0.1 (tag `v0.0.1`); `cargo install agent-scaffold` works. `workflow-doc-fixes`, `pack-rebuild-tracking`, `triager-independence`, `file-safety-rules`, `agent-isolation`, `user-prompts-dir`, `human-onboarding`, `gate-prompt-clarity`, `compaction-prep`, `deliberation-mode`, `human-review-queue`, `no-wrap-convention`, `findings-files`, and `ledger-template` are also complete: the ENTIRE workflow-hardening cluster is now done (the human-interface cluster is done; `gate-prompt-clarity`, `Q-20`, was recovered via a transcript dig after a compaction flattened the purpose of the planner's gate prompts). The hardening cluster is done, and the human then directed an optional cluster (2026-07-15) in the order `instrument-flag` -> `state-schema` -> `optional-modules` -> `workflow-calibration`; `instrument-flag` (the `--instrument` metrics flag) and `state-schema` (the `validate` / `status --json` subcommands over the metrics log and the plan's structured regions, `Q-11`/`Q-24`) are now complete, so the immediate next step is `optional-modules` (which needs a design pass). Still optional/deferred: `optional-modules`, `greenfield-flake`, `later-enhancements`, `git-url-fetch`, `tui-authoring`, `workflow-calibration`, and the new deferred `workflow-viz` (a Gantt / nix-output-monitor-style live visualiser). The remaining planned work is `optional-modules` then `workflow-calibration`, plus the LATER whole-codebase acceptance review (see below); `deliberation-mode` is complete (it added the cross-cutting human-input contract and discharged the two owed follow-ups: the restored "impasse" trigger and the orchestrator gate-relay duty); `human-review-queue` is complete (it made the Open Questions section a push-at-checkpoint living queue, defined the step-boundary checkpoint with a report-and-continue default cadence configurable at kickoff, re-strengthened the onboarding decision duty to the push model, and added the compaction-prep/resume onboarding pointer). `Q-21` (between-step checkpoint cadence; decided: report-and-continue, configurable at kickoff; folded into `human-review-queue`), `Q-22` (prose line-wrapping is not policed; decided; folded into the new `no-wrap-convention` step), and `Q-23` (the onboarding compaction-lifecycle pointer; decided: a thin pointer, folded into `human-review-queue`) are all resolved, so no open questions remain. Standing convention now in force: prose is not hard-wrapped and line length is never a review finding. A consolidating planner pass has folded a backlog of already-decided workflow rules into new Roadmap steps: `file-safety-rules` (git-durability-and-recovery discipline), `agent-isolation` (capability-tiered writer isolation), `findings-files` (reviewers and triagers write findings to files, `Q-14`), `compaction-prep` (checkpoint / resume, `Q-15`), and `user-prompts-dir` (a `.agents/user-prompts/` directory, `Q-16`, revising `human-onboarding`); the queue carries `Q-14` through `Q-18` as decided. A later job, once those land, is a full review of the whole codebase as it currently is (not a diff-based review): drive it with the role-separated reviewer workflow (or prompt agents to review the entire codebase for correctness and quality), not the diff-oriented `/code-review` command. The implementation lives in the repo (`src/`, `pack/`); this plan is the durable context for resuming after a compaction, and the "Repository Layout and Current Architecture" section maps the shipped code so a fresh implementor can continue without prior context. Verification convention: `cargo clippy --all-targets -- -D warnings`, `nix fmt`, and ASCII-clean before each commit.

This document plans a tool that scaffolds the agent workflow (front-load context -> structured plan -> iterative and adversarial review -> isolated implementation -> adversarial review) into a project, so the structure does not have to be hand-rolled each time. It follows the same planning format the tool is meant to scaffold.

## Motivations

- Setting up the workflow by hand for each project is repetitive: the same planning-document skeleton, the same guidance/principles, the same reusable prompts, and (when wanted) the same worktree and container isolation.
- The setup should work in two modes: starting a new project, and adding to or editing an existing one. A greenfield-only template does not cover the second mode.
- The default should be minimal. Extra machinery (diagram prompt packs, container isolation, language starters) should be opt-in rather than imposed.
- Guidance should be harness-agnostic: `AGENTS.md` is the canonical file, with any harness-specific file (for example `CLAUDE.md`) reduced to a thin pointer to it.

## Project Principles

These govern how this tool is built.

1. Prefer the cleaner long-term architecture over the smallest diff: prioritise correctness, internal coherence, and maintainability, and when a local fix conflicts with a cleaner design, choose the cleaner design unless a concrete limitation prevents it.
2. Minimal by default. The core does one thing well; everything else is an optional module the user opts into. Adding a module must not complicate the core.
3. Safe on existing projects. Scaffolding into a populated repo must never clobber or silently overwrite existing files. Prefer create-if-absent, namespaced locations, and clearly marked idempotent edits.
4. Idempotent. Running the tool twice produces the same result as running it once, with no duplicated blocks and no drift.
5. Make illegal states unrepresentable. Work out the valid inputs and outcomes first and encode them, rather than admitting bad states and guarding against them at runtime.
6. Ground decisions in evidence. Before an approach is built out, validate it with a small proof-of-concept that builds and produces the expected output. If the candidates are exhausted, raise the impasse for a decision rather than forcing through an unvalidated approach.
7. Reproducible. Prefer the project's existing toolchain conventions (Nix, just, direnv) so a scaffolded project and its checks behave the same on any machine.

Note: the principles the tool _ships_ to other projects (the default `AGENTS.md` content) are a separate artifact from these; the shipped set and its data model are covered in the `core-assets` step and live in `pack/principles.toml`.

## Documentation Protocol

This plan is kept current during the work.

Progress lives in the Roadmap: an ordered table of step slugs and their status (`not started`, `in progress`, `complete`, `skipped`, `next`, `optional`, `deferred`, or `blocked on <slug>`). The Roadmap is the single source of truth for status and for implementation order. The Step Details below carry each step's design, decisions, and (once done) outcome and evidence, and do not repeat the status label.

Steps are identified by a stable kebab-case slug, not a number, so a reference stays valid when the order changes: every cross-reference to a step uses its slug. Each Roadmap row maps to a detail block headed by the same slug; some detail blocks are grouped under an umbrella heading that carries context shared by several steps, and the umbrella itself is not a Roadmap entry. To add a step, add its detail block and insert its slug into the Roadmap at the right position; to reorder or reprioritise, edit the Roadmap rows. Keep the two in sync: every Roadmap slug has a detail block and vice versa.

The "Open Questions, Decisions, Issues and Blockers" section is the single living human-decision queue (the model adopted in `human-review-queue`). Each item has a stable id, a one-line ask, a status (`open`, `decided -> folded into <slug>`, or `superseded`), and a context pointer into the step or ledger that carries the detail. Resolved items are marked resolved, not deleted, so the same question is not addressed twice; the adopted decision and its reasoning are folded into the relevant step, and the queue item points to that step. The orchestrator updates the queue at every checkpoint as a required step and pushes the open items to the human rather than waiting for the human to pull them.

## Repository Layout and Current Architecture

This section maps the code as built, so the plan can be resumed without prior conversation context. The code in the repository is the source of truth; this is an index into it. The completed steps carry the reasoning; this carries the shape.

Build, run, and verify (see `README.md` for full user-facing usage):

- Use `just build` / `just test` / `just clippy` / `just fmt`, or plain `cargo` inside `nix develop` (with direnv, run `direnv allow` once). The justfile wraps the Nix environment.
- Verification before each commit: `cargo clippy --all-targets -- -D warnings`, `nix fmt`, and all text ASCII-clean (no em-dashes, en-dashes, emoji, or other non-ASCII; use `->`, `>=`, `!=`).
- The binary is a plain Rust program (edition 2021, MSRV 1.88) that runs without Nix. Nix, just, and direnv are development conveniences only. Dependencies: `clap` (derive), `serde`, `toml`, `ratatui = "0.30"`, `include_dir`.

Directory layout:

- `src/` holds the binary crate: four modules described below.
- `pack/` is the built-in default pack: `pack.toml` (the manifest), `AGENTS.md`, `plan-template.md`, `principles.toml`, and `prompts/`. It is embedded into the binary at compile time with `include_dir`, and it also loads through the same manifest path as an external `--template` pack (the pack is dogfooded).
- `flake.nix`, `.envrc`, `justfile`, `rustfmt.toml` are the dev environment (fenix toolchain, treefmt formatters, recipes). `LICENSE` is Blue Oak Model License 1.0.0.

Modules and key types (all shipped and tested):

- `src/main.rs` (CLI and orchestration). `Cli` (clap derive) exposes `--output-dir`, `--force`, `--vcs <git|none>` (git by default), `--write`, `--dry-run` (conflicts with `--write`), `--principles`, `--principle-detail`, `--list-principles`, `--template <dir>`, and `--var key=value` (repeatable); there is no `-i` flag, the selector is the default on a terminal. `main` builds the active `manifest::PackSource` (the built-in pack, or a `--template` directory), parses `--var` into an overrides map, loads the active pack's principles via `pack_principles`, and resolves the selection. `use_selector` then decides the mode: the two-pane TUI opens when both stdin and stdout are terminals, there are principles to choose, and neither `--write` nor `--dry-run` was given; its Save modal is the write confirmation. Otherwise the run is non-interactive and writes only with `--write`. Either way `main` builds the asset set with `build_assets`, computes each asset's `Outcome` with `outcome_of` (read-only, for the plan preview), decides the `--vcs` action with `init_plan` (read-only; `inside_git_repo` skips when already inside a repository), prints the plan (the repository line first), and, when confirmed, runs `run_git_init` (shell out to `git init`) before writing the assets with `apply_asset`. `Outcome` is `Created`/`Refreshed`/`SkippedExisting`/`Overwritten`; `outcome_of` decides it without touching disk and `apply_asset` performs the write, so the preview never writes. `#[cfg(test)] scaffold` is a build-plus-write convenience for the drop tests. User errors print to stderr and exit 2.
- `src/pack.rs` (principle data and selection). `Principle { id, name, summary, rationale, tags, default_selected, default_order, references, related }`. `parse_principles(&str)` parses a principles TOML file. `resolve_selection(principles, spec)` implements the `--principles` token grammar (comma-separated `default` / `all` / `none` / `tag:<t>` / bare-id, de-duplicated by first occurrence; keyword and tag tokens expand in `default_order`, id lists keep their given order) and returns `SelectionError { UnknownId, UnknownTag }`. `Detail { Name, Summary, Full }` with `render_principles` renders the selected set as a numbered list. `default_principles` / `DEFAULT_PRINCIPLES_TOML` are `#[cfg(test)]` only, because production reads principles through the active pack source (see `pack_principles`).
- `src/manifest.rs` (the pack format and loader; the abstraction `optional-modules` builds on). `PackSource { Embedded(&Dir), Directory(PathBuf) }` with a public `read(rel)` and a private `manifest()`; `builtin()` returns the embedded pack. `Manifest { asset: Vec<AssetSpec>, var: Vec<VarSpec>, module: Vec<ModuleSpec> }` deserializes `pack.toml` and ignores unknown keys. `AssetSpec { source, dest, ownership, render, module: Option<String> }`, `VarSpec { name, default: Option<String>, module: Option<String> }`, `ModuleSpec { name, description }`, and `Ownership { Reference, Working }`. A `module = "<name>"` tag on an asset or var declares optional-module membership: untagged entries are core (always applied), tagged ones only when `--module <name>` selects that module; the `[[module]]` section is the authoritative set of known module names. `render(template, vars)` does minimal `{{key}}` substitution (unknown placeholders left as-is). `load(source, builtin_vars, overrides, selected_modules)` reads the manifest, filters out unselected modules' assets and vars, resolves the variable map with `resolve_vars` (built-in vars first, then each selected/core declared default, then `--var` overrides; the name `principles` is reserved), and returns `Vec<Asset { dest, contents, ownership }>` or `LoadError { Io, UndeclaredVar, MissingRequiredVar, ReservedVar, UnknownModule, UndeclaredModuleTag, DuplicateModule }` (all exit 2).
- `src/tui.rs` (the interactive selector; TEA-lite, functions not traits, where TEA is The Elm Architecture: a single state value, a pure `update` reducer, and a render function). The only public item is `run_selection(principles, initial_included, save_summary) -> io::Result<Option<Vec<usize>>>`, which returns the chosen principle indices in order, or `None` on abort. Internally: one `App` value; `enum Mode { Editing, Filtering, Confirming { button } }`; `enum Event { Key, Resize }`; a pure `update(&mut App, Event) -> Step` reducer (`Step` is `Continue`/`Confirm`/`Abort`); a `ui` render function; and a blocking `run` loop over `next_event()` (the single seam where a concurrent event source could later be merged). No async, no custom traits. Key bindings are documented in the `selection-ui` step.

Pack format (the contract shared by the built-in pack and `--template` packs):

- A pack is a directory containing `pack.toml` plus the source files it references. External packs are passed with `--template <dir>`.
- `pack.toml` has `[[asset]]` entries `{ source, dest, ownership = "reference"|"working", render = true|false, module? }` (one source may map to several assets; `render` defaults to false) and optional `[[var]]` entries `{ name, default?, module? }` (an absent `default` makes the variable required), plus optional `[[module]]` entries `{ name, description }` declaring the opt-in modules.
- Optional modules: an asset or var with a `module = "<name>"` tag is dropped or required only when that module is selected with the repeatable `--module <name>` flag; untagged entries are core (always applied), so with no module selected the output is byte-identical to the core. Every module a tag references (and every `--module`) must be declared in a `[[module]]` section, and module names must be unique; a dangling tag, an unknown `--module`, or a duplicate declaration is an error and nothing is written.
- `ownership = "reference"` assets (conventionally under `.agents/`) are always (re)written; `ownership = "working"` assets are created only if absent, unless `--force`.
- `render = true` applies `{{name}}` substitution. `{{principles}}` and `{{instrument}}` are reserved, tool-computed variables (the rendered principle selection, and the instrumentation block when `--instrument` is set); a pack may neither declare nor `--var`-set either. Other variables come from `[[var]]` defaults, overridden by `--var key=value`; setting an undeclared variable, or leaving a required one unset, is an error and nothing is written. Every rendered asset is normalised to end with exactly one trailing newline, so an unused reserved placeholder leaves no trailing blank lines and the off output is byte-identical without relying on a formatter.
- A pack that ships `principles.toml` has its principles drive selection, `--list-principles`, the interactive selector, and `{{principles}}`; a pack without one simply has no principles to select. A pack may likewise ship an `instrument.md` render fragment, which is inlined into `{{instrument}}` when `--instrument` is set; this fragment is a special source read directly (like `principles.toml`), NOT a manifest `[[asset]]`, so it is not dropped as its own file. A pack without one renders `{{instrument}}` empty.

One run's data flow: parse CLI, build the `PackSource`, read and parse the pack's `principles.toml`, `resolve_selection` (or the interactive TUI) chooses and orders principles, `render_principles` produces the `{{principles}}` value, `manifest::load` resolves the variables and reads/renders each asset, `outcome_of` computes each asset's plan outcome, and, once writing is confirmed, `apply_asset` drops each honouring ownership and `--force`.

## Open Questions, Decisions, Issues and Blockers

This section is the single living human-decision queue for the task (the model adopted in `human-review-queue`). Each item has a stable id, a one-line ask, a status, and a context pointer into the step or ledger that carries the detail. Statuses are `open` (awaiting a human decision), `decided -> folded into <slug>` (resolved; the decision and reasoning live in that step, and the item stays here marked resolved so it is not re-addressed), or `superseded` (replaced by a later item, kept for provenance). The orchestrator maintains this queue under the push-at-checkpoint rule stated in the Documentation Protocol; a folded decision reopens only on evidence that beats its recorded reasoning.

Historical note: the `OQ-<letter>` labels (OQ-A .. OQ-K) elsewhere in this plan are provenance markers for questions resolved and folded before this queue existed, not live entries; a reference such as "from OQ-E" points to a decision now recorded inline in the relevant step. The pre-queue questions were: OQ-B/C/D (selection-polish), resolved in the TUI-polish steps; OQ-E (manifest and dogfooding), OQ-F (schema and variables), OQ-G (embedding), OQ-I (variable declaration schema), OQ-J (principle source under `--template`), resolved in the bring-your-own-template steps; OQ-H (git-URL fetch), deferred to `git-url-fetch`; OQ-K (version-control init), folded into `init-vcs`; plus the earliest questions (tool form, ownership and update model, principle set and data model, non-interactive selection UI, interactive TUI design), resolved in earlier steps and the code. The live queue below uses `Q-<n>` ids.

Live queue:

- `Q-1` (decided -> folded into `convergence-accounting`) convergence-accounting round accounting: how to make the escalation cap computable from the ledger. Decision and reasoning in `convergence-accounting`.
- `Q-2` (decided -> folded into `ledger-template`) ledger-template placement: where the ledger template lives. Decision and reasoning in `ledger-template`.
- `Q-3` (decided -> folded into `ledger-template`) ledger-template reference-vs-working: whether the per-task ledger copy is refreshed or create-if-absent. Decision and reasoning in `ledger-template`.
- `Q-4` (decided -> folded into `instrument-flag`) instrument-flag CLI surface: a flag or an optional module. Decision and reasoning in `instrument-flag`.
- `Q-5` (decided -> folded into `instrument-flag`) instrument-flag log location: which committed cross-task path holds the JSONL metrics log. Decision and reasoning in `instrument-flag`.
- `Q-6` (decided -> folded into `instrument-flag`) instrument-flag writer: whether the orchestrator appends JSONL directly or via a helper. Decision and reasoning in `instrument-flag`.
- `Q-7` (decided -> folded into `workflow-doc-fixes`) severity scale (finding F4): the reviewer/triager scale and which level the dismissed-finding guard triggers on. Decision and reasoning in `workflow-doc-fixes`.
- `Q-8` (decided -> folded into `deliberation-mode`) deliberation-mode approach: how to acknowledge question-driven (Socratic) human input. Decision and reasoning in `deliberation-mode`.
- `Q-9` (decided -> folded into `human-onboarding`) human-onboarding approach: how the human is onboarded and told their ongoing decision duty. Decision and reasoning in `human-onboarding`.
- `Q-10` (decided -> folded into `human-review-queue`) human-review-queue approach: where the living human-decision queue lives and how it is disciplined. Decision and reasoning in `human-review-queue`.
- `Q-11` (decided -> folded into `state-schema`) structured state file: a machine-parseable projection of progress/tasks/agents the tool can validate. Decision and reasoning in `state-schema`.
- `Q-12` (decided -> folded into `deliberation-mode`) human-input contract: what every human-input point must present. Decision and reasoning in `deliberation-mode`.
- `Q-13` (decided -> folded into `triager-independence`) triager independence: whether the triager may be collapsed into another role. Decision and reasoning in `triager-independence`.
- `Q-14` (decided -> folded into `findings-files`) findings-to-files: whether reviewers and triagers write findings to per-agent files that other agents read directly, instead of the orchestrator transcribing them. Decision and reasoning in `findings-files`; context in `docs/plans/agent-scaffold.ledger.md` (post-189b7bd notes).
- `Q-15` (decided -> folded into `compaction-prep`) compaction-prep: whether to add a reusable compaction-prep user prompt and a checkpoint/resume procedure. Decision and reasoning in `compaction-prep`; context in the ledger's post-189b7bd notes.
- `Q-16` (decided -> folded into `user-prompts-dir`) user-prompts directory: whether to separate human-invoked prompts from role prompts into their own directory. Decision and reasoning in `user-prompts-dir` (which revises `human-onboarding`); context in the ledger's post-189b7bd notes.
- `Q-17` (decided -> folded into `file-safety-rules`) file-safety discipline: the git-durability-and-recovery baseline for writer agents. Decision and reasoning in `file-safety-rules`; the two motivating incidents are in the ledger's post-189b7bd notes.
- `Q-18` (decided -> folded into `agent-isolation`) isolation tiering: whether to run writer agents under capability-tiered isolation. Decision and reasoning in `agent-isolation`; context in the ledger's post-189b7bd notes.
- `Q-19` (decided -> folded into `compaction-prep` and `user-prompts-dir`) resume user prompt: a reusable prompt to continue the in-progress task after a compaction, the pickup counterpart to `compaction-prep`'s flush. Decision and reasoning in `compaction-prep`; the prompt lives in `.agents/user-prompts/` per `user-prompts-dir`.
- `Q-23` (decided -> folded into `human-review-queue`) compaction lifecycle discoverability in onboarding: the "Getting started, for the human" section did not mention the compaction-prep / resume prompts. Decided (human, thin pointer): add a short pointer in Getting started naming `.agents/user-prompts/compaction-prep.md` (paste before a compaction) and `resume.md` (paste to continue after one), no duplication of the procedure; land it as part of `human-review-queue`. Decision and reasoning in `human-review-queue`.
- `Q-22` (decided -> folded into `no-wrap-convention`) prose line-wrapping and the formatter: whether agents police line length, and whether the tooling owns wrapping. Decided (human): no manual hard-wrapping and line length is never a review finding (a workflow convention plus a reviewer/triager rule); and adopt `prettier` `proseWrap: never` via a repo-root `.prettierrc.json` so the existing `nix fmt` markdown formatter actively unwraps prose and owns wrapping (accepting a one-time repo-wide reflow). Decision and reasoning in `no-wrap-convention`; user preference also in memory (`no-hard-wrap-prose`).
- `Q-21` (decided -> folded into `human-review-queue`) between-step checkpoint cadence: how the orchestrator behaves at a step boundary. Decided (human, `(d)`+`(b)`): configurable at kickoff, defaulting to report-and-continue (the orchestrator reports what completed and what is next, then continues; the human can opt into gating each boundary, and can interrupt via intake at any time). Decision and reasoning in `human-review-queue`.
- `Q-20` (decided -> folded into `gate-prompt-clarity`) gate-prompt clarity: `clarifying-questions.md` and `open-questions-gate.md` are agent-run human-decision gates (the "me" in them is the human), not agent-only role prompts; their "ask me" wording is imprecise about the sub-agent -> orchestrator -> human routing, and the README labels them "one prompt per workflow role" though they are planner-phase gates. Decided: keep them as the planner's gate prompts (do not fold into `planner.md`), fix the routing wording, and correct the README label. Recovered from the transcript ([07-11T10:17] "answer at the gates"); a compaction had flattened "human-decision gate" into "agent-facing role prompt". Decision and reasoning in `gate-prompt-clarity`.
- `Q-24` (decided -> folded into `state-schema`) instrumentation data integrity: the `--instrument` metrics log is written by the orchestrator (an LLM) as hand-authored JSONL (`Q-6`), so records are not guaranteed well-formed. Decided (human): guarantee structure by DETECTION, not prevention, the `state-schema` `validate` subcommand checks `docs/metrics/workflow.jsonl` against a schema defined once (the record types and fields fixed in `pack/instrument.md`), reporting or rejecting malformed records, so the data is deterministically verifiable and bad lines are discardable. `instrument-flag` keeps its tool-free direct-write (no runtime dependency on the binary during a workflow run, honouring the harness-agnostic constraint), so `Q-6` is NOT reopened. A validated-append writer (prevention) was considered and declined because it would require the `agent-scaffold` binary on PATH at workflow-run time. Decision and reasoning in `state-schema`.
- `Q-25` (decided -> folded into `optional-modules`) module design: the `[[module]]` schema, the module set, and the deterministic-checks module. Decided (human, design pass): a hybrid schema (asset `module = "<name>"` tags + a `[[module]]` metadata section) with a `--module` flag; the priority modules are a deterministic-checks module (a user-configurable, pluggable lint/format gate, `ast-grep` first-party, implementer applies formatters while a read-only `checks-reviewer` runs linters and check-mode formatters into a findings file the triager adjudicates) and the isolation module (agent-box/agent-images pointer setup), built machinery -> checks -> isolation. Decision and reasoning in `optional-modules`.
- `Q-26` (decided -> folded into `optional-modules`) deterministic-checks module config format: how a project declares its checks so `ast-grep` is first-party but other linters/formatters plug in. Decided (human, 2026-07-16, resurfaced post-compaction): option (a), a declarative `.agents/checks.toml` with `[[check]]` entries naming a command, a kind (lint/format), and a format check-mode command, pre-populated with ast-grep + the formatter, users add entries (one source of truth, pluggable, harness-agnostic, no tool dependency). Declined: (b) reuse justfile recipes (depends on `just`, couples to that module) and (c) ast-grep native `sgconfig.yml` + guidance (less pluggable for non-ast-grep tools). Unblocks building increment 2 of `optional-modules`; config layout and guidance wording settled at the start of that increment. Decision and reasoning fold into `optional-modules`.
- `Q-27` (decided -> folded into `workflow-invariants`) deterministic workflow-invariant enforcement: a deterministic check so a skipped or unreviewed step fails rather than relying on the LLM orchestrator's discretion. Motivating incident (2026-07-16): the orchestrator committed the `pause.md` step outside the review loop on its own "match ceremony to stakes" judgement, which no check would have caught. Design space sketched, human wants the invariants designed before building: (1) prompt-level checklists (what exists, LLM-judged, insufficient alone); (2) RECOMMENDED extend `validate` with a `--workflow` mode cross-referencing plan `COMPLETE` status against ledger rounds and the metrics log (a step marked complete must carry the ledger rounds its risk class requires; a `round` metric per ledger round), runnable in CI or the checks pre-commit hook, detection-not-prevention like the existing validators; (3) a state-machine gate where `agent-scaffold step complete <slug>` refuses illegal transitions. Decided (human, 2026-07-16, after a two-independent-explorer design pass synthesised by the orchestrator): option (2), `validate --workflow` (detection, not prevention), with the LEDGER as the primary authoritative source (always present during a task) cross-referenced against plan status, and the metrics log as a secondary corroboration when instrumented; the state-machine gate (3) was rejected (prevention, stateful, makes the binary a runtime dependency and a second writer of step status). The strictness tension resolves as: trivial changes are not forced through review, they are forced to be DECLARED trivial (the check confirms the declaration), which turns a silent skip into a visible one. This needs the `ledger-parse` keystone (a `src/ledger.rs`) first. Folded into `workflow-invariants`; the enabling parser is `ledger-parse`.
- `Q-29` (decided -> folded into `exploration-mode`) first-class support for design-space exploration / deliberation: the workflow handles deciding (the `deliberation-mode` human-input contract) and deferring (an item stays `open`) a decision, but has no first-class mode for exploring a design space BEFORE options are decidable, which is what the human asked for on `Q-27`/`Q-28`. Gaps the incident exposed: (1) the Open-Questions status vocabulary (`open` / `decided -> folded` / `superseded`) cannot express "open, a design pass is owed first" (the `validate --plan` structural check hard-rejected that phrasing when tried in the status field), so the nuance can only live in body prose; (2) there is no defined deliverable for an exploration (implementation has an artifact, a plan has plan-review, an exploration has neither, so it lives as ad-hoc orchestrator prose); (3) the exploration itself is unreviewed, though it is higher-leverage than implementation; and (4) intake names factual-answer and decision-gate modes but not "the human is asking me to deliberate", so handling falls to orchestrator disposition. Decided (human, 2026-07-16, after the two-explorer design pass): add all four, low-ceremony, mostly docs plus one code line: an `exploring` Open-Questions status (in `plan.rs`), a design-notes artifact convention at `docs/plans/<task>.explorations/<question>.md` (parallel to `.reviews/`) whose shape is the human-input contract written to a file, design-exploration as a named intake mode in `AGENTS.md`, and an optional light review of an exploration (the multi-independent-explorer-plus-orchestrator-synthesis setup that produced this very decision is the review analog). Same "structure over disposition" root cause as `Q-27`/`Q-28`. Folded into `exploration-mode` (built first, being the cheapest and unblocking proper deliberation).
- `Q-28` (decided -> folded into `state-queries`) workflow-state navigation/query commands: whether to add targeted read commands (e.g. `agent-scaffold state`, `agent-scaffold next`) that emit a slice of the ledger RESUME STATE, the current step, the open-questions items, and the next required action, for token-efficient resume instead of reading the whole plan and ledger. Trade-off: a token win only where the source grows unbounded or is re-read (the ledger especially), against per-call and per-turn overhead that can exceed a single file read for small files, plus a projection-drift risk (mitigated by the existing structural validator). Decided (human, 2026-07-16, after the two-explorer design pass): build a thin `next` plus a `status --resume` slice (NOT a separate `state` verb, NOT a wholesale navigator), scoped to the resume and CI/check paths only (the honest token win is at cold resume and in a hook, and is negative per-turn during active work, so do not poll it each turn); build it AFTER the `ledger-parse` keystone, since its value depends on parsing the unbounded ledger, and reuse `workflow-invariants`' next-action transition logic. `next` doubles as the enforcement aid. Folded into `state-queries`.
- `Q-30` (exploring) test-driven process module: a module that expands the planner's role to specify test cases, inputs/expected outputs, properties, and end-to-end flows per step, then inserts a test-authoring phase before implementation (tests written to fail first, reviewed and triaged, then code implemented until they pass). Human-raised (2026-07-16) as a way to deterministically check that an implementation actually meets the plan's requirements, in the same "verify, don't trust the LLM" spirit as the metrics validator and the checks module. Early shape from the intake discussion (to be settled in the design pass): likely an OPTIONAL module (not all languages have test tooling); separate the discipline (planner test-spec, test-first ordering) from the tooling (test execution plugs in as a `kind = "test"` entry in the checks module's `.agents/checks.toml`, so it composes with the deterministic-checks module rather than duplicating it); the red-phase-vs-green-checks tension resolves by treating the test-authoring artifact and the code artifact as two artifacts with two gate profiles (test artifact: compiles and fails by assertion, covers the plan's cases, not vacuous; code artifact: compiles, lints clean, all pre-authored tests pass), with an interface-stub sub-step so tests compile before the code exists; opt-in and matched to risk class given it roughly doubles the reviewed-artifact count. A DESIGN PASS is owed (two-independent-explorer, like `Q-27`/`Q-28`/`Q-29`), SCHEDULED after the workflow self-improvement cluster and the deterministic-checks module (`optional-modules` increment 2) land, since it composes with the checks module. Related: `Q-31`.
- `Q-31` (exploring) mutation-testing module: an optional module layered ON TOP of the `Q-30` test-driven module that mutates the code and confirms the tests kill the mutants, verifying the tests are robust rather than vacuous (adversarial verification applied to the tests themselves). Human-raised (2026-07-16). Trade-offs to settle in the design pass: expensive (runs the suite many times) and slow, tooling is language-specific and less universal than test runners (`cargo-mutants`, `mutmut`, Stryker), so optional-on-optional and gated to when suite quality matters. Conceptual hierarchy each layer verifying the one below: checks module (runs lint/format/test) -> test-driven module (`Q-30`) -> mutation module (`Q-31`). DESIGN PASS owed, bundled with `Q-30`'s (explored together, after the process cluster and the checks module).
- `Q-32` (decided -> folded into `session-preflight`) session preflight and adherence confirmation: the workflow has no defined moment where the orchestrator confirms, with the human, the process, the disciplines it requires, and the isolation modes/tools available, so adherence is assumed rather than established (the `pause.md` skip and the untested isolation both trace to this). Decided (human, 2026-07-16): add a preflight step the orchestrator runs before the workflow starts (at kickoff) and whenever it is resumed (post-compaction or post-pause), in which it reviews the workflow process and its disciplines, detects and states the available writer-isolation modes and tools (container via `agent-box`/`agent-images` if wired, else git worktree, else the file-safety fallback), and confirms with the human what it will do to adhere before proceeding. Also folds in the human's standing directive to ALWAYS use separate, isolated (worktree-first) writer agents where possible, so the orchestrator drives and reviews but does not implement code directly. Folded into `session-preflight`.
- `Q-33` (exploring) real agent-box/agent-images integration for our own spawned agents (vs pointer scaffolding): `optional-modules` increment 3 as scoped only scaffolds POINTER SETUP (docs/config pointing a user at the isolation tools); it does not make the workflow's own spawned agents run inside `agent-box`/`agent-images` containers. Human asked to discuss first (2026-07-16): what would genuinely running our spawned writer agents under `agent-box`/`agent-images` container isolation take, given the harness spawns subagents through its Agent tool (which today offers native git-worktree and remote isolation, not local-container isolation via these tools)? Is container integration a scaffolder concern at all, or a harness concern outside this tool's remit? DESIGN PASS owed before deciding whether increment 3 stays pointer-scaffolding or grows a real integration path; relates to `agent-isolation` (the rule) and `session-preflight` (which states the available tiers).

## Roadmap

Steps in implementation order, with status. The Roadmap is the single source of truth for status; the slug in each row keys the matching detail block under "Step Details". `next` marks the prioritised next work; `optional` and `deferred` mark not-started work that is not on the critical path.

| Step                     | Status      |
| ------------------------ | ----------- |
| `core-assets`            | complete    |
| `file-dropper`           | complete    |
| `idempotency-safety`     | complete    |
| `selection-ui`           | complete    |
| `mode-enum`              | complete    |
| `tag-selection`          | complete    |
| `available-filter`       | complete    |
| `include-all-visible`    | skipped     |
| `pack-manifest`          | complete    |
| `external-packs`         | complete    |
| `pack-owned-principles`  | complete    |
| `init-vcs`               | complete    |
| `convergence-accounting` | complete    |
| `workflow-doc-fixes`     | complete    |
| `pack-rebuild-tracking`  | complete    |
| `triager-independence`   | complete    |
| `file-safety-rules`      | complete    |
| `agent-isolation`        | complete    |
| `user-prompts-dir`       | complete    |
| `human-onboarding`       | complete    |
| `gate-prompt-clarity`    | complete    |
| `compaction-prep`        | complete    |
| `deliberation-mode`      | complete    |
| `human-review-queue`     | complete    |
| `no-wrap-convention`     | complete    |
| `findings-files`         | complete    |
| `ledger-template`        | complete    |
| `state-schema`           | complete    |
| `workflow-viz`           | deferred    |
| `optional-modules`       | in progress |
| `greenfield-flake`       | optional    |
| `later-enhancements`     | optional    |
| `git-url-fetch`          | deferred    |
| `tui-authoring`          | optional    |
| `workflow-calibration`   | deferred    |
| `instrument-flag`        | complete    |
| `exploration-mode`       | complete    |
| `session-preflight`      | next        |
| `ledger-parse`           | not started |
| `workflow-invariants`    | not started |
| `state-queries`          | not started |

## Step Details

Decisions carried from the resolved open questions:

- **Form.** A standalone Rust binary (clap CLI plus a ratatui TUI) that runs without Nix; Nix is a development-only dependency (the dev shell). Distributed via `cargo install` or prebuilt binaries, with an optional flake app for Nix users. Runs from any working directory and writes to any target via `--output-dir`, defaulting to the current directory with a namespaced layout.
- **Ownership (two-tier).** Tool-owned reference assets under `.agents/` are always refreshed; user working files (the root `AGENTS.md`, plans under `docs/plans/`) are create-if-absent, with `--force` to overwrite. An existing `AGENTS.md` is left untouched and the namespaced reference copy is refreshed for the user to merge from. Marked-block co-tenancy and a 3-way-merge `update` were considered and deferred (`later-enhancements`).
- **Template source.** A built-in default pack, with optional `--template <local-path>` for bring-your-own packs (`byo-template`); git-URL fetch is deferred to `git-url-fetch` (must not depend on Nix) and a flake-ref stays out of scope.
- **Principle data.** Structured TOML (`pack/principles.toml`). Each principle has `id`, `name`, `summary`, `rationale`, `tags` (category tags plus applicability tags `universal`/`static-types`/`fp`/`oop`), `default_selected`, `default_order`, and optional `references`/`related`. The selected set renders as a numbered list ordered by `default_order`, at `name`, `summary`, or `full` detail. The default set is about two dozen principles, applicability-tagged so it adapts to the project type (see `pack/principles.toml` for the live set and counts); the shipped prompts and guidance stay principle-agnostic and defer to the selected set.
- **Optional modules (not yet built).** A diagram prompt pack, a justfile of recipes, language starters, and container/worktree isolation. Each is defined concretely in `optional-modules`; the isolation module would integrate the separate agent-box (`https://github.com/0xferrous/agent-box`) and agent-images (`https://github.com/nothingnesses/agent-images`) projects, which are not part of this repository.

### `core-assets`: Author the minimal core assets and principle data

Wrote the canonical `AGENTS.md` guidance (with a `{{principles}}` placeholder), the plan-document template, the three core prompts, and the principle data as structured TOML (initially 48 principles, 21 default; see `pack/principles.toml` for the current set, which has grown since), parsed and tested. Content policy: the shipped prompts and guidance stay principle-agnostic and defer to the user-selected principle set in `AGENTS.md`, rather than baking in specific opinions a user may not have chosen (the open-questions gate was corrected to follow this, and the prompts instruct the agent to read `AGENTS.md` to align with the current principles). The workflow (in `AGENTS.md`) is role-separated so no agent grades its own work, and each role has its own prompt in `.agents/prompts/`: `orchestrator.md` (drives the phases, spawns the roles, routes context, runs the review loop, escalates), `planner.md` (drafts the plan; with `clarifying-questions.md` and `open-questions-gate.md`), `reviewer.md` (independent reviewers, assume-issues, given the change context via before and after commit hashes or a diff range, diverse lenses and models where available), `triager.md` (adjudicates each finding, never the agent that produced the artifact, may run a short producer-versus-reviewer debate on a contested finding), and `implementer.md`. Where the harness lacks sub-agents, one agent plays the roles in sequence but records each role's output. Implementation iterates over the plan's Roadmap steps (implement, review to convergence, mark the step complete), and the workflow stops only when every step is complete and an acceptance review (reviewers, then a triager, as in the other review phases) confirms the plan's Success Criteria. The orchestrator's convergence rule ends a single review loop after the required consecutive clean rounds (scaled to stakes, since fresh reviewers make one clean round weak evidence, and a dismissed high-severity finding is re-checked by a second triager before it counts towards a clean round); it escalates to a human, then resumes, if the artifact reaches a total-round cap (default five). Escalation is a decision request, not a stop. The two round constants (the required consecutive clean rounds and the total-round cap of five) are provisional defaults chosen to be safe rather than measured, to be calibrated against data from real use (see `workflow-calibration`). A human may also interrupt at any point with new or changed requests; the orchestrator first runs a bounded intake assessment that advises on impact and routing and gives feedback on the request (the human decides), folding a trivial request in directly and routing anything that touches scope or Success Criteria to the planner, so non-trivial human input always enters durably through the plan rather than ad hoc. Relitigation is prevented by a review ledger (each finding, its verdict, reasoning, and action, plus each round's outcome) handed to each round under a "no re-raise without new evidence" rule; the ledger is kept in a file committed to version control beside its plan so it survives the orchestrator losing context and travels across machines and sessions, deleted (deletion committed) at task close, kept out of the Open Questions section, and only durable decisions fold into the plan's steps (reopened only by evidence that beats the recorded reasoning). The plan template's Project Principles section (and the `AGENTS.md` Plan step that points at it) instructs the agent to seed a plan's principles from the `AGENTS.md` principles in order, add the project-specific ones after, consolidate any overlap into a single amended principle, and keep the list numbered so it can be referenced by number.

### `file-dropper`: Proof-of-concept the file-dropper

The Rust binary drops the asset set under `--output-dir`, refreshing tool-owned reference assets under `.agents/`, creating absent working files (root `AGENTS.md`, `docs/plans/TEMPLATE.md`), and leaving existing working files untouched unless `--force` is given. `AGENTS.md` is generated by rendering the selected principles into the pack guidance template. Covered by unit tests.

### `idempotency-safety`: Idempotency and safety pass

Re-run refreshes reference assets and skips existing working files (tests cover create, refresh-versus-skip, and `--force` overwrite); `--output-dir` targets any directory.

Write safety and default mode (added later): writes are off unless confirmed, and the interactive selector is the default on a terminal. A run always builds the asset set and prints a plan preview (a present-tense line per asset: `create` / `refresh` / `skip (exists)` / `overwrite`). The mode is chosen by `use_selector`: when both stdin and stdout are terminals, there are principles to choose, and neither `--write` nor `--dry-run` was given, the two-pane selector opens and its Save modal is the confirmation (Save writes, Cancel or abort writes nothing). `--write` takes the non-interactive path and applies the changes (using `--principles`), skipping the selector; `--dry-run` prints the plan and exits without writing (it conflicts with `--write`); and with no terminal and no `--write` (a pipe or CI) the run previews and writes nothing. There is no `-i` flag and no y/N prompt; the selector's modal replaced the prompt. `--force` stays orthogonal (it decides overwrite-versus-skip for existing working files; `--write` decides whether to write at all). Implementation: `outcome_of` computes each asset's outcome without touching the filesystem (so the preview never writes), and `apply_asset` performs the write; terminal detection uses `std::io::IsTerminal` (no new dependency). Validated by a unit test that planning creates nothing, plus functional runs of the no-flags preview, `--write` (byte-identical to the pre-change golden), `--dry-run`, and the `--dry-run`-plus-`--write` conflict (exit 2). The auto-launch of the selector on a terminal needs a manual TTY run (not automatable here).

### `selection-ui`: Selection UI

The non-interactive path: `--principles default|all|none|<ids>` selects the set and `--principle-detail name|summary|full` sets the rendering, both feeding generation and `--list-principles`. A non-interactive route and a recorded selection always remain available so the tool stays scriptable and idempotent.

The interactive TUI (ratatui, `src/tui.rs`) is built and, since the later default-mode change, opens by default on an interactive terminal (there is no `-i` flag; `--write` and `--dry-run` take the non-interactive path), seeded from the resolved `--principles` set. First-pass design as built:

- **Scope: principles only.** Toggle inclusion and reorder principles. Modules are out of this pass, since they are specified but not yet built (`optional-modules`); a module pane or mode is added when modules exist, reusing this pass's interaction code.
- **Layout: two-pane.** Left pane lists available (not included) principles; right pane lists included principles (numbered) in their chosen order. The focused pane is marked by border colour and the cursor highlight; the cursor wraps at both ends of a pane.
- **Key bindings.** `i`/`a` move the highlighted principle to the other pane, inserting it before (`i`) or after (`a`) that pane's cursor, with the cursor following the moved item. (This replaced an earlier Space/Shift+Space scheme: Shift+Space is not distinguishable from Space without the enhanced keyboard protocol, which is unreliable, for example on Alacritty; plain letter keys work everywhere and match vim's insert/append.) `Tab`/`BackTab`/left/right/`h`/`l` switch focus; up/down or `j`/`k` move the cursor; with the Included pane focused, Shift+up/down or `K`/`J` reorder the highlighted principle, overriding `default_order`. `u` undoes and `U` redoes. `/` opens the Available filter (type to narrow by name/id/tag, Enter applies and keeps it, Esc clears it; added in `available-filter`). `Enter` opens the save modal; `q` aborts, and Esc aborts from editing but cancels the modal.
- **Undo/redo.** `u`/`U` step through whole-state snapshots. Each editing action (`i`/`a` move, `K`/`J` reorder) checkpoints the pre-edit state before mutating; a fresh edit clears the redo stack. Navigation (cursor, focus) does not checkpoint, so undo steps through edits rather than movement. Snapshots are valid states, so undo/redo cannot break the disjoint-partition invariant.
- **Detail footer.** A footer shows the highlighted principle's `name`, `summary`, and `tags`. No search/filter in this pass (the few dozen principles scroll fine); filter is a clean later follow-up.
- **Save modal.** `Enter` is labelled save and opens a modal stating what saving will do (target directory, which files are created or refreshed, whether `--force` overwrites, and the included count) with Save and Cancel buttons. Focus defaults to Cancel so an accidental confirm never writes; left/right (or Tab/`h`/`l`) moves between buttons, Enter activates, Esc cancels. The modal is modal: editing keys are ignored while it is open. Choosing Save runs the scaffold and prints the ready-to-paste `--principles <id1,id2,...>` line (in the chosen order) for non-interactive replay; Cancel returns to editing; aborting writes nothing. `main` supplies the modal's summary lines (the file set is static; the target and `--force` behaviour come from the flags).
- **Dependencies.** `ratatui = "0.30"` (0.30.2 latest) with default features (`all-widgets`, `crossterm`, `macros`); the umbrella crate re-exports the workspace split (`ratatui-core`, `ratatui-crossterm`, `ratatui-widgets`), so no sub-crates are added directly. Events use the bundled crossterm (0.29) via `ratatui::crossterm`, not a separate dependency. Terminal lifecycle uses `ratatui::init()`/`restore()`; `init()` installs a panic hook that restores the terminal, so no hand-rolled hook is needed. A blocking `event::read()` loop is used, not async/tokio. MSRV is 1.88 (satisfied by the flake's `fenix-monthly latest`); the crate stays on edition 2021. void-installer (an earlier ratatui 0.28 TUI prototype in a separate repository, not part of this one) was assessed and not reused: it has no toggle/reorder/multi-pane code and its binary is broken against its own WIP API; only the 0.28 terminal-lifecycle and `List`/`ListState` idioms carried over, and those are current in 0.30.
- **Internals (TEA-lite, functions not traits).** The selector is one concrete `App` value with the Elm split expressed as plain functions: `update(&mut App, Event) -> Step` (a pure-ish reducer, no IO, where `Step` is `Continue`/`Confirm`/`Abort`; unit-tested for move/reorder/undo/redo/save-modal/abort), `ui(&mut Frame, &mut App)` (a render function of state, no `Widget` impl on `App`), and a thin `run()` that wires read to update to draw. No custom traits and no generic harness; the reducer is tested by calling `update` with synthesised events, and ratatui's `Backend`/`TestBackend` is available for render tests, so no bespoke `OutputSink` is needed. This keeps the separation of concerns and testability that void-installer's generic trait framework was reaching for, without the generics.
- **Concurrency (sync now, one seam for later).** A blocking loop over a small `enum Event { Key(KeyEvent), Resize }` fed by one `next_event()` that currently wraps `event::read()` (filtering to key-press and resize events). No async, no tokio, no channel. `next_event()` is the single upgrade point: if a real concurrent producer ever lands (for example an isolation step streaming progress, or a live preview pane), it becomes a `std::sync::mpsc` receiver merging sources, still synchronous, without touching `update`/`ui`. This is void-installer's event-source decoupling minus its async runtime; full async stays off the table for a scaffolder.
- **Order round-trips (gap closed).** Reordering overrides `default_order`. `resolve_selection` was re-sorting every result through `ordered_by_default`, so an explicit id list lost the user's order; it now keeps the given order (only `default`/`all` sort by `default_order`), so the printed `--principles` line reproduces the TUI result. The `selection_modes_resolve` test asserts list order accordingly.

### TUI polish and tag-based selection

Shared context for the four steps below (`mode-enum`, `tag-selection`, `available-filter`, `include-all-visible`); this umbrella is not itself a Roadmap entry. These are near-term selector improvements that build on the shipped TUI; each reuses the existing `App`/`update`/`ui` structure and the `next_event` seam. The sub-steps are ordered so each is validated before the next depends on it.

Decisions adopted from the resolved open questions:

- **Modes (from OQ-B).** A `Mode` enum (`Editing`, `Filtering`, `Confirming { button }`) replaces the `confirming` bool and `confirm_button`, so the modes are mutually exclusive by construction (make illegal states unrepresentable) and later modes do not multiply bools. The applied filter query lives on `App` (not inside `Filtering`) because its narrowing of the Available pane persists back in `Editing`; `Filtering` just means keystrokes edit that query, which also keeps `Mode` `Copy`.
- **Filter (from OQ-C).** The filter narrows the Available pane only, live and incremental, a case-insensitive substring over name, id, and tags, with a hand-rolled query string (no new dependency) and a visible-to-underlying index projection for the cursor and toggle. Included is never filtered, so reordering is never over a partial view.
- **Tags (from OQ-D).** `--principles` accepts `tag:<t>` tokens (bare tokens stay ids; `default`/`all`/`none` unchanged); each tag expands in `default_order`, the whole list is de-duplicated by first occurrence, and an unknown tag returns `SelectionError::UnknownTag`. Interactive tag selection reuses the filter (which matches tags), with an optional include-all-visible action on top.

Each sub-step below carries the evidence-grounding discipline: validate the adopted approach with a proof-of-concept (build plus tests plus, where relevant, a functional run); on failure fall back to the recorded next-best approach; if all are exhausted, raise the impasse rather than force an unvalidated approach.

#### `mode-enum`: Mode enum refactor

Replaced `confirming: bool` and `confirm_button` with `enum Mode { Editing, Confirming { button: Button } }`; `update` dispatches on `app.mode` and the focused button exists only inside `Confirming`. The `Filtering` variant is introduced in `available-filter`, where it is first used, so every commit stays free of unused-variant warnings (`available-filter` added it as a unit variant with the query on `App`). Validated by proof-of-concept: the 24 existing selector tests pass unchanged (retargeted from the old bool to `Mode`) and behaviour is identical (open/save/cancel, editing keys ignored while confirming); clippy `-D warnings` clean. The fallback (keep the bool with a `debug_assert`) was not needed.

#### `tag-selection`: Non-interactive tag selection

`resolve_selection` now treats `--principles` as a token list where each token is `default`/`all`/`none`, `tag:<t>`, or a bare id; tokens expand (keywords and tags in `default_order`, ids to themselves) and concatenate, de-duplicated by first occurrence, so a bare id list still preserves its round-trip order. `tag:<t>` naming a tag no principle carries returns `SelectionError::UnknownTag`. Validated by proof-of-concept: unit tests for tag expansion, keyword/id/tag composition with dedup and default-set-first ordering, unknown-tag error, a guard that no id collides with a reserved token, and the unchanged all/none/default/id-list behaviour; plus functional runs (`--principles tag:fp --list-principles` lists the two fp principles in order; `--principles tag:bogus` errors with exit 2). The fallback (a separate `--tags` flag) was not needed. (Since the binary now reads `tags`, the `Principle` struct's blanket `dead_code` allowance was narrowed to the still-unused `related` field.)

#### `available-filter`: Interactive Available filter

`/` enters `Mode::Filtering`; the Available pane narrows live to a case-insensitive substring over name, id, and tags; typing appends to the query, Backspace deletes, Enter applies and returns to `Editing` (filter kept), Esc clears and returns. The Available cursor and toggle map through a visible-to-underlying projection (`available_visible`/`available_display`); Included is never filtered. Refinement: an item returned to Available while a filter is active is appended (its pool position is not user-meaningful, and "before/after cursor" is ambiguous under a projection); with no filter, behaviour is byte-for-byte unchanged, so all prior toggle tests pass. Validated by proof-of-concept: unit tests on the projection narrowing and persistence, tag matching, modality (editing keys type into the query), backspace/esc editing, and toggle-under-filter moving the correct principle and preserving the partition; clippy `-D warnings` clean, 33 tests pass. The interactive feel still wants a manual TTY run (not automatable here). The fallback (jump-to-first-match) was not needed.

#### `include-all-visible`: Optional include-all-visible

Skipped by decision. A key to move every currently-visible Available match into Included at once (tag-based bulk selection on top of the filter). Skipped because the `available-filter` `/` filter already makes finding and adding matches quick (filter, then `i`/`a` on each), so bulk-add is not needed now; it stays minimal-by-default and can be revisited if adding many tagged principles at once becomes common. No `A` binding was added; the `selection-ui` key-bindings reference already includes `/` from `available-filter`.

### Bring-your-own template

Shared context for the three steps below (`pack-manifest`, `external-packs`, `pack-owned-principles`); this umbrella is not itself a Roadmap entry. Support `--template <local-path>` so a project can scaffold from a user-supplied pack, not only the built-in one (git-URL fetch is deferred to `git-url-fetch`; a Nix flake-ref stays out of scope). Sequenced ahead of optional modules (`optional-modules`) because it formalises the pack/manifest abstraction modules slot into.

Background: the original `assets()` in `src/main.rs` hardcoded the mapping from each built-in pack file to its destination path, ownership (reference vs working), and whether it is rendered (substituted) or copied verbatim; one source can fan out to several assets (`AGENTS.md` becomes both the working root file and the `.agents/` reference, both rendered). These steps turn that mapping into data.

Decisions adopted from the resolved open questions:

- **Manifest, dogfooded (OQ-E).** A pack is a directory with a `pack.toml` manifest declaring its assets and variables; the built-in pack ships the same manifest and loads through one path, replacing the hardcoded mapping in `assets()`. Encodes the pack structure as data (Principle 5), one load path with no built-in-versus-external special-casing (Principle 1).
- **Schema (OQ-F).** Each `[[asset]]` is `{ source, dest, ownership = reference|working, render }`; a source may map to several assets. `render = true` applies minimal `{{name}}` substitution with no template engine (Principle 2): `{{principles}}` is a built-in variable computed from the selection, and packs declare other variables (with defaults) that `--var key=value` overrides. A `[[module]]` section is reserved by keeping the parser tolerant of unknown keys (serde ignores them), so these steps carry no dead field and `optional-modules` adds the field when it consumes modules. Behavioural modules (isolation) stay tool flags, not manifest content.
- **Embedding (OQ-G).** The built-in `pack/` is embedded with the `include_dir` crate so built-in and external packs both present as a directory of files under one loader, removing the hand-maintained filename-to-content map (Principle 1); accepted over dependency-free `include_str!` per the user's decision.

Each sub-step is evidence-grounded: validate the approach with a proof-of-concept (build plus tests plus, where relevant, a functional run); on failure fall back to the recorded next-best; if exhausted, raise the impasse.

#### `pack-manifest`: Pack manifest and built-in refactor

Added `pack/pack.toml` declaring the seven built-in assets (`source`, `dest`, `ownership`, `render`), embedded `pack/` with `include_dir`, and added the `manifest` module: `Ownership`, `AssetSpec`, `Manifest`, a resolved `Asset { dest, contents, ownership }`, a `PackSource` (`Embedded`/`Directory`) with a read-by-relative-path interface, `render(template, vars)` (`{{key}}` substitution, unknown placeholders left as-is), and `load(source, vars)` producing the asset set in manifest order. `main::assets()` and its `include_str!` constants are gone; `scaffold` now builds a `{{principles}}` variable from the selection and calls `manifest::load(&manifest::builtin(), &vars)`. `pack::render_agents` was removed (substitution now lives in `manifest::render`); `pack::render_principles` supplies the `{{principles}}` value. Validated by proof-of-concept: scaffold output is byte-identical to the pre-refactor golden (`diff -r` clean across all seven files), all four existing drop tests pass unchanged, plus four new `manifest` tests (render substitution, the built-in asset list, render-only-the-rendered-assets, and a filesystem `Directory` fixture proving the loader is source-agnostic); clippy `-D warnings` clean, 37 tests pass. The `Directory` variant carries `#[allow(dead_code)]` (constructed only in the test build until `external-packs` wires `--template`; `expect` would be unfulfilled in the test build). The fallback (keep the hardcoded `assets()`) was not needed.

#### `external-packs`: External local-path packs and variables

Added `--template <path>` (an external pack directory loaded through the same manifest loader as the built-in) and `--var key=value` (repeatable). `main` builds the `PackSource` once (built-in or `Directory`) and threads it into `scaffold`. The manifest gained a `[[var]]` array (`VarSpec { name, default: Option<String> }`); `manifest::load` now takes the built-in variables and the `--var` overrides, resolves the substitution map (`resolve_vars`), and returns a `LoadError` (`Io`, `UndeclaredVar`, `MissingRequiredVar`, `ReservedVar`, with `Display` and exit 2). Validated by proof-of-concept: 41 tests pass (four new `manifest` variable tests: default-applies/override-wins, missing-required error, undeclared-override error, reserved-name rejection for both declaration and override); the built-in pack still scaffolds byte-identically with no `--template` (`diff -r` clean); and a functional external-pack run confirmed rendered-versus-verbatim assets, ownership, optional defaults, overrides, and that each error path (missing required, undeclared, reserved, malformed `--var`, missing template directory) exits 2 and writes nothing. clippy `-D warnings` clean. The `Directory` variant's earlier `#[allow(dead_code)]` was removed (now constructed by `main`). The fallback (split loaders) was not needed. Interim limitation per OQ-J: `{{principles}}` and selection still come from the built-in set even under `--template`; `pack-owned-principles` removes this.

Decisions adopted from the resolved open questions:

- **Variable schema (OQ-I).** Variables are declared as a `[[var]]` array of tables, each `{ name, default? }` (a `VarSpec { name: String, default: Option<String> }`); a present `default` marks the variable optional, an absent `default` marks it required. Chosen over a `[vars]` name-to-default table (cannot express a required variable) and a string-or-table `[vars]` (untagged parsing complexity): the array of tables encodes required-versus-optional as `Option` rather than a sentinel (Principle 5), matches the `[[asset]]` convention already in the manifest (Principle 1), and needs no template engine (Principle 2).
- **Variable resolution and validation.** Resolve the substitution map from three sources: the built-in `principles` variable, then each declared variable's default, then `--var` overrides. Errors (a `TemplateError` enum with `Display`, exiting 2 like `SelectionError`): a `--var` naming a variable the pack does not declare; a required variable (no default) that no `--var` supplies; and any use of the reserved name `principles` (a pack declaring a `[[var]]` named `principles`, or a `--var principles=...`), mirroring the existing reserved-token guard in `resolve_selection`. Unknown `{{...}}` placeholders in asset bodies stay left as-is, as in `pack-manifest`.
- **Principle source, interim (OQ-J).** Here, `{{principles}}` and principle selection still come from the built-in embedded set even under `--template`; this is a documented interim limitation that `pack-owned-principles` removes. To keep that change localized (no back-tracking), `main` constructs the `PackSource` (built-in or `--template` `Directory`) once and threads it into `scaffold` in this step, so the asset load already flows through the chosen source.

Validate: a fixture pack (a temp directory with a `pack.toml` declaring an optional and a required variable and a templated asset that uses them) scaffolds correctly, honouring ownership, render, and `--var`; the required-variable, undeclared-variable, and reserved-name errors each fire; and the built-in pack still scaffolds byte-identically with no `--template`. Fallback: if a filesystem source cannot share the embedded loader cleanly, split the loaders but keep one manifest schema, and raise it.

#### `pack-owned-principles`: Pack-owned principles

Principles are now a property of the active pack. `PackSource::read` was made public and `main` gained a `pack_principles(source)` helper that parses the pack's `principles.toml` (empty set when the pack ships none; a malformed file is a parse error exiting 2); `main` uses it in place of `pack::default_principles()`, so selection, `--list-principles`, the interactive selector, and the `{{principles}}` value all reflect the active pack. For the built-in pack this reads the same embedded `principles.toml` the drop uses, so the built-in path is unchanged. `pack::default_principles`/`DEFAULT_PRINCIPLES_TOML` became `#[cfg(test)]` (production reads through the source; they remain for the pack-data tests), keeping the production build free of dead code. Validated by proof-of-concept: 44 tests pass (three new: built-in principles read through the source match the helper, an external pack's `principles.toml` drives selection with no built-in leak, and a pack with no `principles.toml` yields an empty set); functional runs confirmed `--list-principles --template` and `--principles all --template` list the pack's principles, a `--template` scaffold renders the pack's principles into `AGENTS.md` (no built-in principle present) and drops the pack's `principles.toml`, and a pack without principles renders `{{principles}}` empty without error; the built-in scaffold is byte-identical to the golden; clippy `-D warnings` clean. The fallback (keep the built-in set for external packs) was not needed. Known minor limitation (out of scope): the interactive save modal's summary text still names the built-in file set even under `--template`.

### `init-vcs`: Initialise a VCS repository (git by default)

`--vcs <git|none>` (default `git`) initialises an empty version-control repository in the output directory. Git is initialised by default, matching cargo; `--vcs none` skips it. This reverses OQ-K's original opt-in `--init` default (the user chose cargo's on-by-default convention); it is reconciled with safe-on-existing (Principle 3) by never nesting or reinitialising (see below), and `--vcs none` preserves the opt-out. Behaviour:

- Default git, `none` to skip: `--vcs` is a clap value-enum (`Vcs { Git, None }`) defaulting to `Git`; the reserved future extension (jj, hg, and so on) slots in as more variants without changing the default.
- Safe on existing (cargo-style): `inside_git_repo` walks up from the output directory (resolving a relative path against the current directory) looking for a `.git` entry; if the directory is already inside a repository the action is skipped, so scaffolding into a subdirectory of an existing repo never nests a new one, and an existing repo is never reinitialised (Principle 3).
- Mechanism: `run_git_init` shells out to `git init <output-dir>` (no libgit2 or Nix dependency, consistent with `git-url-fetch`). If a repository would be initialised but the `git` binary is not found, the run errors (exit 2) rather than silently skipping; `--vcs none` needs no git.
- Write-safety and idempotency: `init_plan` decides the action from a read only (`InitPlan::Init` / `SkipExists` / `None`), so the preview never writes. The repository line appears first in the plan (`init  git repository`, or `skip (exists)  git repository` when already inside a repo) and `git init` runs only when writing is confirmed (`--write` or the selector's Save), before the assets are dropped. The repository is left empty (no initial commit and no `.gitignore`); committing the scaffolded files stays the user's choice.

Validated by proof-of-concept: a unit test asserts `init_plan`'s decision (default git in a fresh directory -> init, `--vcs none` -> none, a directory that is a repository -> skip, a subdirectory of a repository -> skip so nothing nests) without needing a git binary; and functional runs confirmed that a default run into a fresh directory with `--write` creates `.git`, `--vcs none` writes no repository, scaffolding into a subdirectory of an existing repo skips (no nested repo), and a default run with `git` absent from `PATH` errors (exit 2) before any asset is written. 46 tests pass; clippy `-D warnings` clean. The fallback (raise rather than add a git-library dependency) was not needed.

### Workflow hardening (from the round-1 review)

Shared context for the eight steps below (`convergence-accounting`, `workflow-doc-fixes`, `pack-rebuild-tracking`, `triager-independence`, `human-onboarding`, `deliberation-mode`, `human-review-queue`, `ledger-template`); this umbrella is not itself a Roadmap entry. They fold in the outcome of the workflow-hardening review round (round 1, 14 triager-valid findings; see `docs/plans/agent-scaffold.ledger.md`), four human-queued workflow-interface features, a build-system fix found while implementing `convergence-accounting` (`pack-rebuild-tracking`), and a human workflow-rule decision (`triager-independence`). The correctness fixes (`convergence-accounting`, `workflow-doc-fixes`, `pack-rebuild-tracking`) come before the feature work. All change the canonical pack assets (`pack/AGENTS.md`, the prompts under `pack/prompts/`, the README diagram, and, where noted, `pack/pack.toml` and the manifest test) except `pack-rebuild-tracking`, which is a build-system fix (a `build.rs`); each is a non-trivial change kept small and reviewable, and each carries the evidence-grounding discipline: validate the change (build plus the relevant tests plus, where a document renders, a read-through that the prose, the prompts, the diagram, and this plan agree); on failure fall back to the recorded next-best; if exhausted, raise the impasse. The finding ids `F<n>` reference the combined findings recorded in the review ledger. The `state-schema` block that follows this cluster (immediately after `ledger-template`) is a separate, deferred item (`Q-11`, sequenced after the human-interface work), not one of these eight hardening steps; the hardening cluster ends at `ledger-template`.

### `convergence-accounting`: Fix the convergence accounting (F1, F2, F7, F10)

The core design fix, addressing F1, F2, F7, and F10. The convergence rule's round taxonomy was not a partition, and the "contested" round state the separate contested-rounds cap depended on was never defined or recorded, so that cap could not be computed from the ledger; the cap-boundary wording was inconsistent (F7) and the convergence bullets omitted the total-round cap (F10).

Decision (`Q-1`, decided as approach (b)): drop the separate contested-rounds cap entirely and bound each review loop with a single total-round cap (default five). The per-round outcome is a two-way partition, `clean` or `new-valid`, with no separate round state for a persisting dispute and no cross-round finding ids; escalation fires when the total rounds on an artifact reach the cap, whatever the clean-versus-new-valid mix. Reasoning: judged against Principle 1 (prefer the cleaner long-term architecture) and Principle 5 (make illegal states unrepresentable), the single cap removes the non-partition taxonomy and the under-defined round state instead of adding per-round state and per-finding identity to track them, so there is no unrecordable state and no ambiguous 3-versus-5 boundary to get wrong. The accepted trade-off: the loop loses the "stuck on the same dispute" signal, so a loop that keeps finding new genuine issues and a loop relitigating one finding escalate on the same schedule; the total-round cap still bounds both.

The fix makes the escalation trigger deterministic and computable from the ledger, and states the same rule the same way in every document: `pack/AGENTS.md` (the Convergence and ledger sections), `pack/prompts/orchestrator.md` (the round-counting steps), the ledger round-summary schema (the `clean`/`new-valid` per-round outcome and the running total-round count), and the README diagram's cap-boundary labels (aligning "cap hit" with the prose so F7's off-by-one is gone). This step also owns the matching correction to the plan's own `core-assets` narrative; that narrative's convergence sentence and round-constant count are updated in this plan now (to a single total-round cap and two round constants) so a resuming agent is not misled, matching how F3 was corrected ahead of `workflow-doc-fixes`. Validate by reading the sources against each other and confirming a re-spawned orchestrator could recompute the escalation decision from the ledger alone.

### `workflow-doc-fixes`: Resolve the remaining valid review findings

The remaining triager-valid findings, each a small, self-contained documentation fix. Grouped here because they are mechanical or near-mechanical; split into separate commits if that keeps each reviewable. Ordering note: the F5 and F16 edits touch the convergence and clean-round text that `convergence-accounting` rewrites first, so they apply on top of that rewrite, not before it.

- F3 (done): the plan's `core-assets` narrative previously called the review ledger "a durable scratch file (not committed)", contradicting `pack/AGENTS.md`, `pack/prompts/orchestrator.md`, and `CHANGELOG.md` ("committed beside its plan"). Already corrected in the narrative (it now reads "committed to version control beside its plan"), applied ahead of this step because the plan is the resume anchor and would otherwise mislead a resuming agent; no further edit is needed here. (Interacts with `ledger-template`, which pins the ledger's home.)
- F4 (severity scale): enumerate the severity scale used by `pack/prompts/reviewer.md` and `pack/prompts/triager.md` once, and reconcile the dismissed-high-severity guard's "high" wording with its stated aim of catching a "critical" finding. The decided scale (`Q-7`) is stated in the "Severity scale" paragraph below; apply it here rather than restating it.
- F5 (re-check overturn): specify what happens when the second-triager re-check overturns a dismissed high-severity finding (flip the round outcome, reset the consecutive-clean streak, send it back to be fixed), and state that convergence blocks until the re-check returns, so the re-check is a step in the loop, not an aside.
- F6 (acceptance loop): state explicitly whether the acceptance review runs the convergence loop, and align the README diagram with the answer (the diagram currently draws acceptance as a single pass with no loop, cap, or escalation, while the prose says "as in the other review phases").
- F8 (per-artifact reset): counts are per-artifact but the ledger is per-task; state the rule that resets the consecutive-clean count and the round total when the loop moves to a new artifact or step, so segmentation is specified rather than inferred.
- F9 (resume reset): state that the escalation-resume path resets the round counters, so a total-cap escalation does not immediately re-fire on resume.
- F12 (diagram routing): fix the README diagram's trivial-interrupt edge (`intake -> impl`), which dead-ends when no step is pending; route it to match the prose (the orchestrator folds a trivial request in directly, so it should re-enter wherever the loop paused rather than always into implementation).
- F15 (risk classification): define and record the risk classification that selects one vs two required clean rounds ("risky / high-blast-radius"), so the choice is recorded rather than subjective.
- F16 (zero-findings clean round): make the zero-findings case explicit in the "clean round" definition, which currently reads as "every finding dismissed" and does not obviously cover a round that found nothing.
- F17 (nit): the plan's `core-assets` narrative called them "three round constants"; under the single-total-cap design (`Q-1`, approach (b)) there are only two (the required consecutive clean rounds and the total-round cap of five), the contested-rounds cap having been dropped. This narrative correction is owned by `convergence-accounting`, which rewrites the same convergence sentence, and is already applied in this plan; nothing separate is needed here.

Fixes span `pack/AGENTS.md`, `pack/prompts/reviewer.md`, `pack/prompts/triager.md`, `pack/prompts/orchestrator.md`, `README.md` (the diagram), and this plan's own `core-assets` narrative (F3, F17). Validate by reading each fixed source against the others; the three invalid findings (F11, F13, F14; cosmetic soft-wrap and a faithful caption) are not addressed, per the triager verdicts.

Severity scale (F4, `Q-7`, decided). The reviewer and triager severity scale is a four-level rating: `low`, `medium`, `high`, `critical`. It is an absolute rating, not a relative ranking (validated against CVSS practice; prioritisation like EPSS/KEV is a separate overlay out of scope here). Enumerate it once in `pack/prompts/reviewer.md` and `pack/prompts/triager.md`, and have the dismissed-finding guard trigger on a dismissed `high`-or-above finding, so it still catches a `critical`; correct the guard's wording (which today says only "high-severity") to match (Principles 1 and 5).

### `pack-rebuild-tracking`: Register the embedded pack as a rebuild dependency

To be done before the golden sync-test in `ledger-template` (found while implementing `convergence-accounting`; see `docs/plans/agent-scaffold.ledger.md`). `include_dir!` in `src/manifest.rs` embeds `pack/` into the binary at compile time, but the macro does NOT register those files as cargo rebuild dependencies, so after a pack file changes a plain `cargo build` can leave the binary embedding a STALE pack. `just scaffold-self` (and the future golden sync-test in `ledger-template`) then regenerate the root `AGENTS.md` and `.agents/` from that stale embedded pack. This actually happened: the bootstrap's committed `.agents/` copies were stale (missing the hardening content) until a forced `cargo clean -p agent-scaffold` + rebuild caught them up.

Fix: add a `build.rs` that emits `cargo:rerun-if-changed=pack` (or the equivalent per-file directives) so any change under `pack/` forces a rebuild and re-embed. This must land before the golden sync-test in `ledger-template` is worth building, since that test would otherwise be fooled by the same staleness, and before anything relies on `scaffold-self` being current.

Evidence-grounding: edit a `pack/` file, run `cargo build` with no manual `cargo clean`, and confirm the change is re-embedded and appears in a fresh scaffold; on failure fall back to the recorded next-best, and if the candidates are exhausted raise the impasse.

### `triager-independence`: The triager is always a separate agent

Decided by the human (`Q-13`): ALWAYS a separate triager, NEVER collapsed. Amend `pack/AGENTS.md`'s role-separation rules so the triager is always a separate agent, independent of BOTH the agent that produced the artifact under review AND the orchestrator, for every review round including trivial ones. The orchestrator drives the loop and owns convergence and cost, so it is biased toward dismissing findings to converge; letting it triage would let that bias decide which findings count.

Changes to `pack/AGENTS.md`:

- Narrow the "Match the ceremony to the stakes: collapse roles for a trivial change" rule so it explicitly does NOT permit collapsing the triager: the triager stays separate even for a trivial or low-risk review round.
- Update the Triager role description (today it states only "The triager must not be the agent that produced the artifact under review") to add the orchestrator-not-triager independence alongside the existing producer-not-triager rule.

Align `pack/prompts/triager.md` and `pack/prompts/orchestrator.md` with the strengthened rule where they name who triages. Evidence-grounding: read the role-separation rule, the collapse-for-trivial rule, and the Triager role description against each other and confirm they agree that the triager is separate from both the producer and the orchestrator for every review round.

Outcome (complete; implemented in `bd483e1`, converged at `bdebe9d`). Narrowed the collapse-for-trivial rule so the triager is the one exception (never merged into the producer or the orchestrator, even for a trivial or low-risk round), tightened the no-sub-agents fallback in both `pack/AGENTS.md` and `pack/prompts/orchestrator.md` to "the other roles" so it no longer collapses the triager, and added the orchestrator-independence reasoning (the orchestrator owns convergence and cost, so it is biased toward dismissing findings) to the Triager role description and both prompts. Reviewed with two independent reviewers (different models) and a separate triager (round 1: three valid coherence findings fixed, two invalid, including a dismissed high with auditable reasoning), then a fresh verification reviewer and a separate triager (round 2: clean, one cosmetic finding dismissed on the F11/F13/F14 precedent). The invariant is now stated consistently across `pack/AGENTS.md` (opening pointer plus the full Triager-role rule), `pack/prompts/orchestrator.md`, and `pack/prompts/triager.md`, each permitting "(or a human)" as the triager, and the generated root `AGENTS.md` and `.agents/` mirrors are in sync. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `human-onboarding`: Getting started, for the human

Decided (`Q-9`): approach B plus a thin C slice. Add a "Getting started, for the human" section to the canonical `pack/AGENTS.md` that onboards the human and points them at the editable kickoff prompt they can copy, fill in, and paste to start a task, plus a brief mirror in `README.md` that points to it rather than duplicating it (one source of truth, Principle 1). The section also states the human's ongoing responsibilities, not just the first step: that decisions accumulate in the Open Questions / human-decision queue, and that the human decides on them when the orchestrator pushes them at a checkpoint. Pairs with `deliberation-mode` and `human-review-queue` as the human-interface cluster.

Revised by `user-prompts-dir` (`Q-16`): the kickoff prompt does NOT live inline in `pack/AGENTS.md`. It moves into `.agents/user-prompts/` (the human-invoked prompts directory), and the "Getting started" section becomes a thin trigger that points to that directory rather than carrying the prompt text, so no workflow content is duplicated. Build this step against that revision (a pointer, not an inline block); `user-prompts-dir` owns creating the directory, the manifest entries, and the moved prompt.

Validate by reading the section and confirming a human with no prior context could start a task and knows their standing decision duty; confirm the "Getting started" section points to `.agents/user-prompts/` for the kickoff prompt rather than embedding it, and that the README mirror points to the canonical section and does not restate it.

Outcome (complete; implemented in `27e6171`, converged at `e722a14`). Added a "Getting started, for the human" section near the top of `pack/AGENTS.md`: a newcomer copies the kickoff prompt from `.agents/user-prompts/kickoff.md` to start a task, the section distinguishes the human-invoked prompts from the role prompts, and it states the ongoing decision duty (the orchestrator brings its recommendation when a decision is needed; the decisions collect in the plan's Open Questions queue, which the human reviews as the work proceeds). Added a brief README mirror that points to the canonical section rather than restating it (Principle 1). Reviewed with two independent reviewers and a separate triager (round 1: three valid findings, an "orchestrator"/"checkpoint" jargon gloss, and two forward-references softened; round 2 clean). Because the push-at-checkpoint discipline (`human-review-queue`) and the full human-input contract (`deliberation-mode`) are sequenced later, the decision-duty wording was softened to the interim pull model the pack enforces today, so the section is accurate at this commit; two follow-ups are recorded in `human-review-queue` and `deliberation-mode` to re-strengthen it (the push wording) and restore the "impasse" trigger when that machinery lands. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `deliberation-mode`: Question-driven (Socratic) human input

LOCKED (`Q-8`): approach B plus the thin C naming slice; not a new phase or role. Acknowledge question-driven (Socratic) human input as a first-class entry mode in `pack/AGENTS.md`, alongside the existing request-driven interrupt. Generalise the existing intake / clarifying-questions / Open-Questions machinery (`pack/AGENTS.md`, `pack/prompts/planner.md`, `pack/prompts/clarifying-questions.md`, `pack/prompts/open-questions-gate.md`, `pack/prompts/orchestrator.md`) so that when the human asks a question, the orchestrator answers with options, trade-offs, a recommendation, and reasoning, and the human decides (mirroring how the planner already treats open questions). Bind each resolved question to a durable Open-Questions decision (a queue item that gets folded into a step), and define convergence for this mode as the human committing a decision to action, guarded by the existing "no re-raise without new evidence" rule. Reuses the existing machinery rather than adding a phase or role (Principles 2 and 1). Validate by reading the workflow and confirming a human question routes through intake to a recorded, folded decision with no new role introduced.

Human-input contract (`Q-12`, decided; folded in from a human request). Generalise this step from question-driven input to ALL human-input points. At every point where the workflow needs human input, escalation on an impasse, the intake of a new or changed request, an open question, or a clarifying question, the agent presents the viable options or approaches, the trade-offs of each, a recommendation, and the reasoning, with the reasoning judged against the current numbered Project Principles of the project the workflow is used on. Scale the contract to the stakes: the full structure for a real decision, a lighter form (a one-line recommendation and reason) for a trivial confirmation. Implement it as ONE cross-cutting rule stated once in `pack/AGENTS.md` (a standard decision-block format), referenced from each human-input point rather than duplicated (Principles 1 and 2). In particular, strengthen the escalation step, which today says only to "escalate with the ledger for a decision" and does not require structured options: escalation must present the options, trade-offs, recommendation, and Principle-judged reasoning like the other human-input points. This is approach A (the single cross-cutting rule) plus a light standard decision-block format. Validate by reading each human-input point (escalation, intake, open questions, clarifying questions) and confirming each references the one contract and that escalation now requires structured options.

Cluster follow-up (from the `human-onboarding` review): when this step's human-input contract lands (structured options at escalation/impasse), restore "an impasse" to the `human-onboarding` "Getting started" section's decision-trigger enumeration ("when the agents reach a question or a trade-off" -> "a question, an impasse, or a trade-off"). It was softened out because the impasse case was not yet grounded in the pack (escalation then said only "escalate with the ledger for a decision").

Cluster follow-up (from the `gate-prompt-clarity` review, S1): make the orchestrator's gate-relay duty explicit in `pack/prompts/orchestrator.md`. The gate prompts (`clarifying-questions.md`, `open-questions-gate.md`) now tell a sub-agent to "return your questions to the orchestrator, which relays them to the human and returns the answers", but `orchestrator.md` does not yet name that duty (it is grounded in `AGENTS.md` but not restated in the orchestrator prompt the orchestrator reads). This step's cross-cutting human-input contract already touches `orchestrator.md` and both gate prompts, so add a sentence there: when a spawned role returns clarifying or open questions for the human, present them to the human per the contract and relay the decisions back.

Outcome (complete; implemented in `39ab3a3`, converged at `360f7b9`). Stated the human-input contract once in `pack/AGENTS.md` (wherever the workflow needs a human decision, present the options, their trade-offs, a recommendation, and reasoning judged against the plan's numbered Project Principles; scale to stakes; the human decides; record durably in Open Questions), and referenced it from every human-input point (intake, escalation, the two planner gates, and `planner.md`) instead of duplicating it. Added question-driven (Socratic) input as a first-class entry mode reusing the intake/Open-Questions machinery (no new phase or role), with a factual-vs-decision carve-out so a purely factual question is answered directly. Strengthened escalation in both `pack/AGENTS.md` and `pack/prompts/orchestrator.md` to present the decision per the contract. Both cluster follow-ups discharged: the "impasse" trigger is restored in the Getting started section, and the orchestrator gate-relay duty (the deferred `gate-prompt-clarity` S1) is now explicit in `orchestrator.md`. Classified risky/high-blast-radius (the core cross-cutting contract), so it required two consecutive clean rounds; reviewed with two thorough independent reviewers and a separate triager (round 1: five valid groups fixed, notably a reference-plus-restatement hybrid trimmed to pure pointers; round 2 clean, its only finding a line-wrap the human ruled a non-defect; round 3 clean). During this step the human ruled that prose line-wrapping is not a defect (`Q-22`); reviewers/triagers no longer police line length. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `human-review-queue`: Single living human-decision queue

Decided (`Q-10`): approach A (reuse the Open Questions section) upgraded with approach B's discipline, and made push-plus-pull. Make the plan template's "Open Questions, Decisions, Issues and Blockers" section the single living human-decision queue: each item has a stable id, a one-line ask, a status (`open` / `decided -> folded into <slug>` / `superseded`), and a context pointer. Resolved items are marked resolved, not deleted, so a decision is not addressed twice. The orchestrator updates the queue at every checkpoint as a required step and pushes the open items to the human at each checkpoint rather than relying on the human to pull them (a new human would not know to check, and a pull-only model is fragile). This changes `pack/AGENTS.md` (the workflow: the ledger/Open-Questions handling and a required push-at-checkpoint step), `pack/prompts/orchestrator.md` (the checkpoint procedure), `pack/plan-template.md` (the template's Open Questions section adopts the queue format), and this plan's own Open Questions section, which is already converted to the queue format in this pass. Validate by reading the template and workflow and confirming the queue's format and the required push step are stated once and consistently, and that this plan's section matches.

Cluster follow-up (from the `human-onboarding` review): when this step's push-at-checkpoint discipline lands, re-strengthen the `human-onboarding` "Getting started" section's decision-duty wording from the interim pull model ("check that section as the work proceeds") back to the push model Q-9 intends (the orchestrator raises the open items with the human at each checkpoint). The onboarding text was softened to the pull model so it stayed accurate before this machinery existed.

Between-step checkpoint cadence (`Q-21`, decided by the human: configurable, default report-and-continue). Define the orchestrator's behaviour at a step boundary (a step converged, the next not yet started) as a first-class checkpoint alongside the Open-Questions push. Default: the orchestrator emits a brief progress report (what step completed and what is next) and continues to the next step without blocking, so the human has visibility and can interrupt via intake at any time. The human may set a different cadence at kickoff: gate for a go-ahead at each boundary, or run fully autonomous to acceptance. State this in `pack/AGENTS.md` (the checkpoint definition, and that both the Open-Questions push and the progress report happen at a step boundary) and `pack/prompts/orchestrator.md` (report-and-continue at each step boundary, honouring the kickoff cadence), and name the cadence option in the kickoff prompt (`.agents/user-prompts/kickoff.md`). This closes the gap surfaced when the orchestrator had no defined between-step behaviour and improvised an ad-hoc pause.

Compaction-lifecycle pointer in onboarding (`Q-23`, decided by the human: thin pointer). The "Getting started, for the human" section of `pack/AGENTS.md` did not tell a newcomer that the workflow has a before-a-compaction (flush) and after-a-compaction (resume) lifecycle; that was documented only in the two prompts' own preambles and the agent-facing "Checkpoint and resuming after context loss" section. Add a short pointer to the Getting started section naming `.agents/user-prompts/compaction-prep.md` (paste before a compaction to flush state) and `.agents/user-prompts/resume.md` (paste to continue after one), without restating the procedure (Principle 1, one source of truth; Principle 2, minimal). Folded here because this step already edits the onboarding and checkpoint wording.

Outcome (complete; implemented in `fbd3ffa`, converged at `4823785`). Added a new "Checkpoints (the human-decision queue and progress)" subsection to `pack/AGENTS.md` that names the push-at-checkpoint discipline (the orchestrator updates the queue and pushes its open items to the human at every checkpoint, rather than the human pulling) and defines the step boundary as a checkpoint with a report-and-continue default cadence the human can override at kickoff (gate each boundary, or run autonomously through to acceptance); the queue item format lives once in `pack/plan-template.md` (stable id, one-line ask, status `open`/`decided -> folded into <slug>`/`superseded`, context pointer, resolved-marked-not-deleted), which the AGENTS.md subsection defers to and which points back to AGENTS.md for the push rule. `pack/prompts/orchestrator.md` gained the checkpoint procedure (update-and-push the queue, report-and-continue honouring the kickoff cadence). The `Q-9` follow-up re-strengthened the "Getting started" decision duty from the interim pull model to the push model; the `Q-23` pointer to `compaction-prep.md`/`resume.md` was added to the same section. `pack/user-prompts/kickoff.md` names the cadence option. Reviewed with two independent reviewers (opus correctness, sonnet consistency; findings to Q-14 files) and a separate triager: round 1 found five valid findings (one medium, the AGENTS.md/template format split not cleanly realised; four low), all fixed; round 2 (fresh verification reviewer given the ledger) was clean, so the low-risk artifact converged at one clean round. The Documentation Protocol note that resolved questions are "removed" from the queue was corrected to marked-resolved-not-deleted, matching the adopted model. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `no-wrap-convention`: Prose line-wrapping is not policed

Decided (`Q-22`, by the human). The human prefers no manual hard-wrapping of prose (soft-wrap); line length is never a review finding and agents must not reflow or police it. Two parts:

- Convention (workflow-doc change, always applicable): add to `pack/AGENTS.md` a short convention that prose is not hard-wrapped and line length is never a review finding, and fold "line length is never a finding" into `pack/prompts/reviewer.md` and `pack/prompts/triager.md` so reviewers and triagers do not raise or act on it.
- Formatter (tooling follow-on). Decided (human, investigation surfaced the sub-choice): the project already runs `prettier` on `*.md` through `nix fmt` (treefmt-nix, see `flake.nix`), by default at `proseWrap: preserve` (it leaves prose line breaks as authored and does not reflow). Adopt `proseWrap: never` via a repo-root `.prettierrc.json`, so the formatter actively unwraps every paragraph to a single soft-wrapped line and wrapping becomes tool-owned rather than a manual concern (the human's original wish). This is preferred over `preserve` (which needs no reflow but leaves the tool passive) because the human wants the tool, not a convention, to own wrapping; the cost, a one-time reflow diff across all markdown and slightly noisier line-based diffs afterwards (word-diff mitigates), is accepted. The evidence PoC confirmed a repo-level `.prettierrc.json { "proseWrap": "never" }` makes treefmt's prettier unwrap prose (tool-native and portable, prettier auto-discovers it even outside Nix). This supersedes the earlier "do not retroactively reflow" caution, which was aimed at wasteful manual agent reflowing; a formatter doing it once and owning it thereafter is the intended automation. Because the repo-wide reflow is a mechanical formatter run touching orchestrator-owned files (the plan and the ledger), the implementer adds the doc convention and the `.prettierrc.json`, and the orchestrator runs the repo-wide `nix fmt` reflow (incidental reformatting is the orchestrator's, per the file-safety rules) as a separate mechanical commit. The `scaffold-self` recipe also gains a trailing `nix fmt` step, because the tool's raw render is not formatter-clean under `proseWrap: never`; this `nix fmt` deliberately formats the whole tree rather than only the generated files (scoping it would duplicate the manifest's dest-path list against Principle 1), which is harmless because treefmt is idempotent and leaves the repo at a stable committed fixed point.

Validate by reading the convention and the reviewer/triager rule and confirming line length is stated once as a non-finding; and, for the formatter, that after the reflow `nix fmt` is idempotent (a second run is a no-op), `just test` / `just clippy` pass, and no structured content (tables, code blocks, the Roadmap table, rendered principles) is corrupted by the reflow.

Outcome (complete; implemented across `4fa5e74` (convention + `.prettierrc.json`), `81aa4bd` (pre-reflow safety), `dc5f69b` (repo-wide reflow + `scaffold-self` fmt step), `027aeba` (F1 fix); converged after round 3). Convention: `pack/AGENTS.md` gained a "Prose formatting" paragraph (prose is not hard-wrapped; line length is never a review finding), and `pack/prompts/reviewer.md` and `pack/prompts/triager.md` gained the matching "line length is never a finding" rule (a reviewer must not raise it; a triager auto-dismisses it). Formatter: adopted `prettier` `proseWrap: never` via a repo-root `.prettierrc.json` (a dev-tooling file, not a shipped pack asset), which the existing `nix fmt` (treefmt-nix) auto-discovers; a one-time repo-wide `nix fmt` unwrapped all hard-wrapped Markdown prose to single soft-wrapped lines (the plan was already unwrapped), and the `just scaffold-self` recipe now runs `nix fmt` after the raw render so it reaches a stable committed fixed point. Reviewed as a RISKY artifact (repo-wide reflow of shipped pack prompts plus a formatter-contract change) requiring two consecutive clean rounds: round 1 had two independent reviewers plus a separate triager (the reflow was verified meaning-preserving via `git diff --word-diff`; one valid low finding, F1, that `scaffold-self` formats the whole repo, was fixed by correcting the recipe comment and recording the whole-repo format as intentional; F2, a `style:` commit prefix, was accepted as residual risk); rounds 2 and 3 (fresh reviewers, different models) were both clean, so streak = 2 and the artifact converged. A treefmt cache artifact (files unchanged since before `.prettierrc.json` were stale-cached as clean) was diagnosed and cleared once with `nix fmt -- --no-cache`; a fresh checkout has an empty cache, so it is not a committed defect. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `ledger-template`: Scaffold the review ledger as a template asset

Make the review ledger a scaffolded template asset so its schema is pinned rather than invented ad hoc per task. Placement (`Q-2`, decided): the ledger template is a `.agents/` reference asset (for example `.agents/LEDGER.template.md`), copied per task to `docs/plans/<task>.ledger.md`; the template is the single source of the ledger format, referenced (not restated) from `pack/AGENTS.md` and `pack/prompts/orchestrator.md`. Per-task copy (`Q-3`, decided): the template is a reference asset, but the per-task copy is a working file (create-if-absent), so a live ledger is never clobbered. Adding a pack asset also updates the manifest (`pack/pack.toml` gains a `[[asset]]` entry) and its expected-asset test in `src/` (the built-in asset list test). Interacts with F3 (the ledger's committed-beside-its-plan home) and with `convergence-accounting` (the template must carry the round-outcome schema that decision fixes: the `clean`/`new-valid` per-round outcome and the running total-round count, without cross-round finding ids). Validate by proof-of-concept: the template drops through the existing loader, the manifest asset-list test is updated and passes, and the schema referenced from the workflow matches the template.

Outcome (complete; implemented in `062d8cf`, converged at `7e2b11b`). Added `pack/LEDGER.template.md`, a task-agnostic ledger template pinning the round-outcome schema (per round exactly `clean` or `new valid findings`, a running total-round count and a consecutive-clean streak, both per-artifact, no cross-round finding identity, per `convergence-accounting`), a findings table (four-level severity, triager verdict, action vocabulary; within-round ids only), a round-records narrative, and a RESUME STATE block. Shipped as the `.agents/LEDGER.template.md` reference asset (`pack/pack.toml` `[[asset]]` entry after `principles.toml`, and the `builtin_manifest_lists_the_expected_assets` test in `src/manifest.rs` extended), and referenced (not restated) from `pack/AGENTS.md` and `pack/prompts/orchestrator.md`: the orchestrator copies the template to `docs/plans/<task>.ledger.md` at task start if that file is absent, never clobbering a live ledger (Q-3 create-if-absent; the per-task ledger is not itself a manifest asset, so `scaffold-self` cannot clobber it). Reviewed with two independent reviewers (opus correctness, sonnet consistency) and a separate triager: round 1 found five findings (four valid low after the medium was re-severitised, one invalid); the valid ones were one-source-of-truth trims (the finding-file path now lives in the round-records narrative not a table column; `pack/AGENTS.md` and `pack/prompts/orchestrator.md` were trimmed to defer to the template rather than restate a partial finding-row schema; the derived `Consecutive clean` column was labelled; the example row aligned to "new valid findings"). Round 2 (fresh reviewer) was clean, so the low-risk artifact converged at one clean round. This is the last workflow-hardening step; the cluster is complete. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `findings-files`: Reviewers and triagers write findings to files

Decided (`Q-14`, adopted by the human). Reviewers and triagers write their findings to per-agent files rather than returning them for the orchestrator to transcribe. Part of the structured-record cluster with `ledger-template`, `human-review-queue`, and `state-schema`: one schema per record type (the finding schema here, the round-outcome schema in the ledger template, and the decision-queue item schema in `human-review-queue`), each defined once and referenced rather than re-invented per artifact, with `state-schema` validating and projecting over them.

Design:

- Each reviewer and triager in a round writes its findings to its own file under `docs/plans/<task>.reviews/`, so parallel writers never contend for one file (parallel-safe). The filenames follow one authoritative convention stated in `pack/AGENTS.md` (reviewer files `<step>-<role>-<disambiguator>.md`, with the orchestrator assigning each spawned reviewer a distinct disambiguator so same-role reviewers cannot collide; the triager `<step>-triage.md`; the backstop re-check triager `<step>-triage-recheck.md`), and the role prompts write to the path the orchestrator assigns rather than each inventing a divergent form (Principle 1, one source of truth; Principle 5, no representable colliding-path state).
- Other agents read those files directly. The separate triager reads the reviewers' files; the orchestrator references a finding by its file path in the ledger instead of copying its text. This removes the lossy transcription hop that dropped finding A-M5 in this very task when findings were hand-merged (Principle 1: the findings file is the single source for a finding, referenced not copied).
- The orchestrator owns a cleanup step that deletes the findings files once the round is fully resolved (or at task close). Per the commit-before-delete rule in `file-safety-rules`, the files are committed at least once and then removed as a committed deletion, so the review record survives in git history and the removal is recoverable. This resolves the earlier committed-versus-transient question toward committed.

Change `pack/AGENTS.md` (the review-loop and ledger handling: reviewers and triagers write to files, the orchestrator references them by path, the cleanup-with-commit-first step) and `pack/prompts/reviewer.md`, `pack/prompts/triager.md`, and `pack/prompts/orchestrator.md` (each writes or reads the findings files per its role). Validate by reading the roles against each other and confirming a finding flows reviewer-file -> triager-read -> ledger-reference with no orchestrator transcription, and that the cleanup commits before deleting.

Outcome (complete; implemented in `a8a75f5`, converged at `6a35ce1`). Added a "Findings files" subsection to `pack/AGENTS.md` and per-role instructions to `pack/prompts/reviewer.md`, `pack/prompts/triager.md`, and `pack/prompts/orchestrator.md`: each reviewer and the triager write to their own file under `docs/plans/<task>.reviews/`, other agents read them directly, the orchestrator references a finding by its file path in the ledger (no transcription), and the orchestrator cleans up with commit-before-delete when a round resolves or at task close. Reviewed with two independent reviewers (opus correctness, sonnet consistency) and a separate triager: round 1 found four valid findings (one medium, the filename convention did not guarantee uniqueness across parallel same-role reviewers and was stated three divergent ways; three low), fixed by establishing one authoritative naming convention stated once in `pack/AGENTS.md` (reviewer files `<step>-<role>-<disambiguator>.md` with the orchestrator assigning each spawned reviewer a distinct path so collisions are unrepresentable, triager `<step>-triage.md`, backstop re-check triager `<step>-triage-recheck.md`), with the role prompts writing to the assigned path rather than restating a divergent form; round 2 (fresh reviewer) was clean, so the low-risk artifact converged at one clean round. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `state-schema`: Derived JSON projection of the plan state

Deferred and non-blocking (`Q-11`); sequenced after the human-interface work (`human-onboarding`, `deliberation-mode`, `human-review-queue`). Decided approach: Approach 1, a derived projection. The prose artifacts stay the single source of truth (the Roadmap for step status, the living decision queue for open decisions, the review ledger for round state); the tool gains a `validate` / `status --json` subcommand that validates those state documents and emits a JSON projection for downstream visualisation, so there is no second authoritative store to drift (Principle 1). Exclude authoritative live-agent state, which the harness owns; at most an append-only dispatch/return event log. Reuse ONE schema shared with `ledger-template` (the round-outcome schema), `findings-files` (the finding schema), and `human-review-queue` (the decision-queue item schema) rather than defining a second store, and build on `ledger-template` and the Roadmap. It lands after the human-interface steps so the queue and ledger schemas it projects are settled first. Record the design sub-questions (the exact subcommand name and JSON shape, how strict validation is, what the event log covers) in Open Questions before implementing, with approaches, trade-offs, a recommendation, and Principle-judged reasoning; evidence-ground with a proof-of-concept that validates the state docs and emits a projection a small viewer can read.

Metrics validation (`Q-24`, decided by the human). The `validate` subcommand also covers the `--instrument` metrics log at `docs/metrics/workflow.jsonl`: it parses each JSONL record against the fixed schema (the record types `round` / `escalation` / `dismissal_recheck` / `intake` and their fields, defined once in `pack/instrument.md`) and reports or rejects malformed records, so the LLM-written data is deterministically verifiable and bad lines are discardable rather than silently corrupting calibration (Principle 5, illegal states caught; Principle 1, one schema). This is DETECTION: `instrument-flag` keeps its tool-free orchestrator-direct-write (`Q-6`), so running a scaffolded project's workflow needs no `agent-scaffold` binary on PATH (harness-agnostic); a validated-append writer that would prevent malformed records at write time was declined for that runtime-dependency cost. The metrics-record schema is part of the ONE shared schema this step reuses, so it is defined once and referenced, not re-specified.

Carried forward from the `instrument-flag` review (V-4, a deferred low finding): the metrics records defined in `pack/instrument.md` currently carry no task identifier or timestamp, but `docs/metrics/workflow.jsonl` is a cross-task accumulating log, so records cannot be segmented by task for aggregation. When this step lands, ADD a task-identifying field (for example a `task` key naming the plan/ledger task, and optionally a timestamp) to every record type in the schema and in `pack/instrument.md`, so per-task calibration stats are computable; the `validate` subcommand then checks it. Resolve the exact field(s) in this step's design pass.

Design-pass decisions (2026-07-15, human-decided). Four questions were put to the human and resolved:

- CLI surface: SUBCOMMAND-ONLY (the human released the backwards-compatibility constraint, so this is the cleanest structure, not a hybrid). Every action is a verb: `agent-scaffold scaffold ...`, `agent-scaffold validate`, and `agent-scaffold status --json`; bare `agent-scaffold` prints the subcommand list (clap `arg_required_else_help = true`), so a newcomer discovers the surface. Chosen over verb-flags (`--validate`) and over a scaffold-as-magic-default hybrid: making scaffold an explicit verb is more uniform (Principle 1, cleaner long-term structure) and scopes each verb's arguments to its own subcommand rather than top-level flags that only apply under a default (Principle 5), matching the git/cargo convention. Consequence: the interactive principle selector now opens on `agent-scaffold scaffold` run on a terminal (bare no longer scaffolds); this is the one deliberate behaviour change, and backwards compatibility with v0.0.1 was explicitly not required. `tui-authoring` later reuses the subcommand surface (`pack new` / `pack edit`).
- Storage architecture: SINGLE FILE, parse the structured regions. The markdown plan/ledger stay the single source; the machine-relevant state already lives in their structured regions (the Roadmap pipe-table `slug -> status`, the Open Questions queue list `id/ask/status/pointer`, the ledger round-summary pipe-table `round/outcome/streak`), which parse deterministically, while the free narrative (Step Details, round-record prose) holds no machine state and is not parsed. `state-schema` formalises and enforces the schema of those regions (the ONE shared schema) and projects them; the metrics JSONL stays its own append-only file. Splitting the facts into a separate machine data file was declined: it would either duplicate the facts (drift) or move the Roadmap/queue out of the human-readable, tool-free, harness-portable plan, against the markdown-first premise (Principle 1 single source per region, Principle 3, Principle 2). "Full derived" is thus achieved over all machine-relevant state without free-prose parsing.
- Validation strictness: HARD-FAIL. `validate` exits non-zero and reports on any schema or cross-reference violation (a malformed metrics record, a broken Roadmap table, a queue id with no target, a Roadmap slug with no Step Detail), so CI or an agent can gate on it (Principle 5). `status --json` stays best-effort and never hard-fails.
- Agent dispatch/return event log: DEFERRED (not built now). The metrics JSONL already records the round-level events calibration needs, and no visualiser consumes an activity timeline yet, so building a third stream now is speculative (Principle 2). The human wants it REVISITED later for a live workflow visualiser (see `workflow-viz`).

Scope, as decided: `validate` (a) checks `docs/metrics/workflow.jsonl` against the metrics-record schema in `pack/instrument.md` (`Q-24`, plus the `task`/timestamp field from V-4), and (b) checks the plan/ledger structured regions parse and their cross-references hold; both hard-fail. `status --json` emits a derived projection of the structured regions (steps and statuses, open-questions items, round-summary state, and a metrics summary), not committed as a source (regenerable). Reuse the ONE schema shared with `ledger-template` / `findings-files` / `human-review-queue`. Because this is a large code step (a CLI restructure, markdown-region parsing, JSONL validation, and a JSON projection), implement it in reviewed increments (the subcommand restructure with a byte-identical scaffold; then the metrics schema + `validate`; then the structured-region parse + `validate` + `status --json`), each reviewed as its own artifact.

Outcome (complete; three converged increments, `e32dc5a` / `bdc0955` / `fdd3774`, with their review fixes). Increment 1 (`e32dc5a`): restructured the flags-only CLI into subcommand-only (`scaffold`, and later `validate`/`status`; bare prints the subcommand list via clap `arg_required_else_help`; the selector opens on `agent-scaffold scaffold`), scaffold output byte-identical. Increment 2 (`bdc0955`): added `src/metrics.rs`, a typed-enum record-schema validator, and the `validate` verb that hard-fails (exit 1) on any malformed `docs/metrics/workflow.jsonl` record (`Q-24`), with the V-4 `task`/`ts` fields, `serde_json`, and a prose-vs-code drift-guard test that took three review rounds to harden until it provably catches divergence for every record type, field, and enum value. Increment 3 (`fdd3774`): added `src/plan.rs` parsing the plan's Roadmap table and Open Questions queue (structured regions only, not free narrative), extended `validate --plan` to hard-fail on a malformed table/row, an unknown or over-permissive status, a duplicate slug, a Roadmap slug with no Step Detail, the reverse (a detail with no Roadmap row), or an unresolvable `decided -> folded into <slug>` target, plus a plan-vocabulary drift guard against `pack/plan-template.md`; and added the `status` subcommand emitting a best-effort `{plan, metrics}` JSON projection. Dogfood: `agent-scaffold validate --plan docs/plans/agent-scaffold.md` validates this repo's own 36-step plan clean, and `status --json` projects it. Each increment reviewed by two independent reviewers plus a separate triager; 79 tests, clippy clean, scaffold byte-identical throughout. Deliberately deferred: the ledger round-summary parsing (the metrics log already carries round data) and the agent dispatch/return event log (into `workflow-viz`); the duplicate-JSON-key metrics behaviour is accepted as-is. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `workflow-viz`: Live workflow visualiser (deferred)

Deferred; a design-questions pass is needed before implementing. The human wants a way to visualise a workflow run over time, in two flavours discussed: a Gantt-style chart of agent dispatch/return (which role ran at which step, and for how long), and a live, streaming process view in the spirit of nix-output-monitor (`nom`), which renders an in-flight Nix build's dependency/progress tree live. This consumes the append-only agent dispatch/return event log deferred in `state-schema` (build that log as this step's input when taken up), and reuses the derived-projection schema. Sequenced after `state-schema` (which settles the shared schema and the projection command) and non-blocking. Record its design sub-questions (the event-log schema and where it is written; live-streaming versus post-hoc rendering; a TUI view reusing the `tui.rs` TEA-lite patterns versus emitting data for an external viewer; how it stays harness-agnostic since only the orchestrator sees agent lifecycles) in Open Questions before implementing.

### Durability, recovery, isolation, and user prompts (consolidated workflow rules)

Shared context for the four steps below (`file-safety-rules`, `agent-isolation`, `user-prompts-dir`, `compaction-prep`); this umbrella is not itself a Roadmap entry. They fold in a backlog of already-decided workflow rules gathered while running this task (see the ledger's post-189b7bd notes and everything after). The unifying idea across the durability and recovery rules is that git is the workflow's durability and recovery substrate. `findings-files` (above, with the structured-record cluster) came from the same backlog and depends on the commit-before-delete rule defined here. All of these change the canonical pack assets (`pack/AGENTS.md`, the prompts under `pack/prompts/`, and, where noted, `pack/pack.toml`, the manifest asset-list test, and `README.md`); each is a non-trivial change kept small and reviewable and carries the evidence-grounding discipline (validate the change, on failure fall back to the recorded next-best, and if exhausted raise the impasse).

### `file-safety-rules`: Git-durability-and-recovery discipline

Decided (`Q-17`, adopted by the human). Add to `pack/AGENTS.md` an always-on file-safety discipline that works on any harness, whether or not it offers isolation; this is the baseline that `agent-isolation` layers a structural upgrade on top of, not a substitute for it. Git is the durability and recovery substrate: the rules keep every writer's damage to a visible, committed-or-recoverable diff. The discipline:

- Clean-tree-before-writer: commit pending work (especially the ledger) before spawning a writer agent, so a writer's kill or misstep leaves only a visible uncommitted diff and never risks already-decided state.
- Commit-before-delete: commit any workflow-managed file (a findings file, the ledger at task close, any transient artifact) before deleting it, so the deletion is a committed deletion recoverable from git history.
- Format only your own files: implementers must NOT run repo-wide formatters (`just fmt` / `nix fmt`) or `git checkout` / `git restore` on files they do not own; they format only the files they changed and leave incidental reformatting to the orchestrator.
- Orchestrator recovery protocol: on any agent kill or interrupt, the orchestrator inspects `git status` and the diff, reverts stray temp artifacts, discards or completes partial work, and confirms a known-good tree before continuing.
- Validation-in-scratch: destructive validations run in a temp directory or worktree, not the live tree.

Motivating incidents (both in the ledger's post-189b7bd notes): an implementer ran repo-wide `just fmt` then `git checkout` on the ledger it did not own, clobbering uncommitted ledger notes; and an implementer was accidentally killed mid-task, which a clean tree and a recovery protocol make safe. Change `pack/AGENTS.md` (a file-safety / durability section) and the role prompts that need it (`pack/prompts/implementer.md` for the format-only-your-own-files and validation-in-scratch rules; `pack/prompts/orchestrator.md` for clean-tree-before-writer, commit-before-delete, and the recovery protocol). Validate by reading the rules across `pack/AGENTS.md` and the prompts and confirming each writer role and the orchestrator carry the rule that applies to it, stated once and consistently.

Outcome (complete; implemented in `d86ec0f`, converged at `aeea55b`). Added a "File safety and durability (git is the recovery substrate)" section to `pack/AGENTS.md` carrying the five rules (clean-tree-before-writer, commit-before-delete, format-only-your-own-files, validate-in-scratch, orchestrator recovery-on-interrupt), each attributed to the role that performs it, and folded the role-specific rules into `pack/prompts/implementer.md` (format-only, validate-in-scratch) and `pack/prompts/orchestrator.md` (clean-tree, commit-before-delete, recovery, plus a tree-hygiene reformatting clause). The Roles section now classifies the spawned roles (planner and implementer are writers; reviewers and the triager are read-only with respect to the plan and code, writing only their own findings files) and defines "writer agent", which `agent-isolation` reuses for its read-only carve-out. Reviewed over three rounds (two independent reviewers plus a separate triager in round 1: three valid low coherence findings fixed, notably a loose "writer agent" term, an unowned incidental-reformatting duty, and a duplicated commit-before-delete; round 2 fixed one more valid low finding, "read-only" made precise; round 3 clean). The commit-before-delete rule now owns the ledger-at-close deletion (the ledger paragraph's duplicate clause was dropped in both `pack/AGENTS.md` and `pack/prompts/orchestrator.md`). See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `agent-isolation`: Capability-tiered writer isolation

Decided (`Q-18`, adopted by the human). Two parts: the isolation RULE (a workflow-doc change, always applicable) and the isolation MECHANISM (optional-module integration, heavier and deferred).

- Rule (in `pack/AGENTS.md`): run each WRITER agent in the strongest isolation the harness supports, in preference order: (1) container isolation via agent-box / agent-images if available, preferred because it isolates filesystem, environment, and a security sandbox; (2) worktree isolation if that is what the harness offers (for example claude-code native worktrees); (3) the `file-safety-rules` discipline as the fallback when the harness has no isolation. Read-only agents (reviewers reading, the triager) need no isolation (Principle 2, minimal by default: do not add machinery a read-only agent does not need). Isolation is the structural upgrade over the file-safety baseline: a killed isolated writer cannot touch the main tree, which directly addresses the two incidents in the ledger (the fmt-plus-checkout ledger clobber and the accidental kill) that are the evidence for prioritising it.
- Mechanism (optional-module work under `optional-modules`, which already lists container and worktree isolation as a behavioural module): our own build and adoption order is worktree-first, since worktree isolation is native to claude-code and cheap now, then agent-box / agent-images container integration as the heavier follow-on that adds environment and sandbox isolation. The runtime selection rule in the workflow docs is container-preferred, but for us today it resolves to worktree anyway.

Change `pack/AGENTS.md` (the workflow's isolation rule, referencing `file-safety-rules` as the fallback tier) and `pack/prompts/orchestrator.md` (the orchestrator selects the tier when spawning a writer). Validate by reading the rule and confirming the tier order and the read-only-needs-none carve-out are stated once, and that the fallback tier is exactly the `file-safety-rules` discipline.

Outcome (complete; rule implemented in `032964a`, converged at `ef875bd`). Added a "Writer isolation (capability-tiered)" section to `pack/AGENTS.md`: run each writer in the strongest isolation the harness supports, container (agent-box / agent-images) > worktree > the `file-safety-rules` discipline as the fallback tier, with the read-only agents (reviewers, triager) needing none, and the isolation mechanism framed as an optional module deferred to `optional-modules` (the rule is the always-applicable selection policy and resolves to the file-safety fallback until the module is built). Added the tier selection to `pack/prompts/orchestrator.md`. The rule reuses the writer/read-only classification and the "writer agent" definition from `file-safety-rules` rather than restating them, and the file-safety section defers the "structural upgrade" framing to this section (one source of truth). Reviewed with two independent reviewers and a separate triager (round 1: two valid low de-duplication findings fixed, two invalid; round 2 clean). The isolation MECHANISM (container and worktree integration) remains to be built under `optional-modules`. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `user-prompts-dir`: Separate user prompts from role prompts

Decided (`Q-16`, confirmed by the human). Create a `.agents/user-prompts/` directory for human-invoked prompts (the kickoff prompt and the `compaction-prep` prompt); `.agents/prompts/` stays the role prompts (orchestrator, planner, reviewer, triager, implementer). Human prompts and role prompts are different in kind: a human copies and pastes a user prompt to drive the workflow, whereas a role prompt is handed to a spawned agent.

This REVISES `human-onboarding`: the kickoff prompt moves from an inline block in `pack/AGENTS.md` into `.agents/user-prompts/`, and the `pack/AGENTS.md` "Getting started" section becomes a thin trigger that points to the directory. The user prompts stay thin trigger-prompts so no workflow content is duplicated between them and the canonical `pack/AGENTS.md` (Principle 1). Adding pack assets also touches `pack/pack.toml` (new `[[asset]]` entries for the user-prompts directory) and its expected-asset test in `src/` (the built-in asset-list test), and the README layout description of the scaffolded tree. Validate by proof-of-concept: the new assets drop through the existing loader, the manifest asset-list test is updated and passes, and the README layout names the new directory. (The "Getting started" section that points to `.agents/user-prompts/` is validated by `human-onboarding`, which owns that section; this step only creates the directory the section points to.)

Outcome (complete; implemented in `9c1a88d`, converged after review). Created a `.agents/user-prompts/` directory with a thin kickoff prompt (`pack/user-prompts/kickoff.md`): a human copies it, fills in the bracketed task and optional context, and pastes it to start a task; it points the agent to act as the orchestrator, read `AGENTS.md`, and use or start a plan, and deliberately restates no workflow content (Principle 1). Added the `pack/pack.toml` `[[asset]]` entry (source `user-prompts/kickoff.md` -> dest `.agents/user-prompts/kickoff.md`, ownership `reference` so the template is refreshed and cannot drift, since the human copies it out rather than editing in place), extended the built-in asset-list test in `src/manifest.rs`, and named the new directory in the README layout. Reviewed with two independent reviewers (mechanics; content and scope) and a separate triager: the implementation was found mechanically correct and the prompt thin and usable; two valid low plan-bookkeeping findings (reflect the cluster reorder in the Roadmap; move the "Getting started" validation criterion to its owner `human-onboarding`) were folded into this completion update. The compaction-prep and resume user prompts are added by `compaction-prep`, not here. See `docs/plans/agent-scaffold.ledger.md` for the round record.

### `gate-prompt-clarity`: Clarify the planner's gate prompts and their routing

Decided (`Q-20`, from a human question during dogfooding). `clarifying-questions.md` and `open-questions-gate.md` are the planner's two human-decision gates: an agent runs them, but the human is the decider (the "me"/"I" in them refers to the human, who answers the clarifying questions and chooses among the open-question options). They are distinct from the role prompts an agent both runs and completes (`reviewer.md`, `triager.md`), and distinct from the human-invoked prompts in `.agents/user-prompts/` (`kickoff.md`), which a human pastes. The design intent is recorded at transcript [07-11T10:17] ("answer at the gates: clarifying questions before work starts ... you're the decider there"); a later compaction flattened "human-decision gate" into "agent-facing role prompt", which is the context this step restores.

Keep them as the planner's gate prompts (do not fold into `planner.md`: they are reusable, separately-invocable gate steps, and the plan template's Open Questions section plus `planner.md` carry the same discipline by design, not by accident). Fix two concrete defects:

- Routing wording. Both prompts address the human as "me"/"I" ("ask me", "for me to review and choose from"), which reads as a direct planner-to-human dialogue. In the role-separated flow the planner is a sub-agent whose questions reach the human via the orchestrator. Reword so the audience (the human) and the routing (a sub-agent returns the questions/options to the orchestrator, which surfaces them and relays the human's decisions) are explicit, while staying correct in the single-agent case.
- README label. The `prompts/` layout block calls them "one prompt per workflow role", but they are planner-phase gates, not roles. Correct the label.

Change `pack/prompts/clarifying-questions.md` and `pack/prompts/open-questions-gate.md` (the routing wording) and `README.md` (the layout label). Validate by reading the two prompts and confirming the human is named as the decider and the sub-agent routing is stated, and that the README no longer mislabels the gates as roles.

Outcome (complete; implemented in `41cf5ca`, converged at `593b71d`). Both gate prompts now name the human as the decider and state the sub-agent -> orchestrator -> human routing (a conditional that stays correct in the single-agent case), replacing the ambiguous "ask me"/"for me to review". The README `prompts/` label is now "role prompts and the planner's decision gates" and the two gates are described as agent-asks / human-decides. Kept as separate planner gate prompts (not folded into `planner.md`). Reviewed with two independent reviewers and a separate triager (round 1: three valid low findings, an "Otherwise" reflow, a "surfaces" verb against the style preference, and an orchestrator gate-relay duty the prompts now assert; round 2 clean). The gate-relay duty (S1) was split out to `deliberation-mode`, which already adds the cross-cutting human-input contract across `orchestrator.md` and the gate prompts, so `orchestrator.md` was not edited here (a scope note is recorded in `deliberation-mode`). See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `compaction-prep`: Compaction-prep prompt and checkpoint section

Decided (`Q-15`, confirmed by the human). Add a reusable compaction-prep USER prompt (in `.agents/user-prompts/`, per `user-prompts-dir`) plus a short "Checkpoint / resuming after context loss" section in `pack/AGENTS.md`. This reuses the durability rules from `file-safety-rules` rather than inventing new ones. The procedure:

- Before a compaction: the orchestrator flushes the plan, the ledger, and the Open-Questions queue to current, verifies the plan's resume-anchor (its Status line) is accurate, and commits everything, so state survives the context loss.
- On resume: reconstruct state from `AGENTS.md` plus the plan plus the ledger.

Keep the wording harness-agnostic: name the plan, the ledger, and the durable notes, not a specific harness memory feature, so the procedure works on any harness. Change `pack/AGENTS.md` (the checkpoint / resume section, referencing the existing durability rules) and add the prompt asset under `.agents/user-prompts/` (with the matching `pack/pack.toml` entry and asset-list test update, shared with `user-prompts-dir`). Validate by reading the section and the prompt and confirming a resuming agent with no prior context can reconstruct state from `AGENTS.md`, the plan, and the ledger, and that the pre-compaction checklist flushes and commits all three durable artifacts.

Outcome (complete; implemented in `226ca33`, converged at `16b1260`). Added a "Checkpoint and resuming after context loss" section to `pack/AGENTS.md`: the plan and ledger are the durable state; before a context loss the orchestrator flushes the plan, ledger, and Open Questions queue, verifies the Status-line resume anchor, and commits; on resume it reconstructs from `AGENTS.md` + plan + ledger and continues rather than restarting; harness-agnostic. Added two human-invoked prompts under `.agents/user-prompts/`: `compaction-prep.md` (flush before a compaction) and `resume.md` (the Q-19 pickup counterpart to the kickoff prompt), both thin triggers that name the AGENTS.md section without restating it (matching `kickoff.md`). Added the `pack/pack.toml` entries, extended the asset-list test, and named them in the README layout. Reviewed with two independent reviewers and a separate triager (round 1: three valid findings, the prompts restated the AGENTS.md procedure they claimed not to, a commit-discipline over-claim, and a "durable notes" referent mismatch; round 2 clean). See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `optional-modules`: Optional modules

Not started; a design-questions pass is needed before implementing (as with `git-url-fetch`). The isolation MECHANISM (container and worktree isolation) is the behavioural module here; `agent-isolation` fixes its adoption order (worktree-first, then agent-box / agent-images containers) and its runtime selection rule.

Goal: package opt-in additions as self-contained modules, each off by default, none complicating the core (Principle 2: adding a module must not change core behaviour when unused). Two kinds are expected:

- Content modules: extra assets. These are expected to live in the pack manifest via a `[[module]]` section, which `pack.toml` parsing already tolerates (unknown keys are ignored, so current packs stay valid). A module groups a set of `[[asset]]` entries (and possibly `[[var]]` entries) under a name the user opts into. When no module is selected, none of its assets are dropped, so the core output is unchanged.
- Behavioural modules: tool features rather than dropped files. These change how the tool runs, not what it writes, so they are expected to be tool flags rather than pack content.

Candidate modules (illustrative; the design pass confirms which to build and their exact contents and ownership). Each of these was named loosely in earlier planning; the concrete intent is:

- Diagram prompt pack (content): additional reusable prompts, in the same form as the built-in `.agents/prompts/` files, that guide an agent to produce and keep project diagrams current, for example architecture, sequence, and data-flow diagrams written as Mermaid (a text-based diagram syntax) or ASCII and embedded in the docs. Dropped as extra prompt assets (conventionally under `.agents/prompts/`) when opted in.
- Justfile of recipes (content): a `justfile` dropped at the project root with ready-made task recipes (build, test, lint, format, a `verify` aggregate, and workflow helpers), matching this project's own just-plus-Nix convention. Opt-in so a project that does not use `just` is not burdened; ownership is a working file (create-if-absent), since a project may already have its own `justfile`.
- Language starters (content): per-language starter skeletons, each a self-contained set of assets keyed to a language (for example a Rust starter with a flake, a `Cargo.toml`, and a `src/` layout, and equivalents for other languages), so a greenfield project gets a working toolchain alongside the agent workflow. Each starter is its own module, opted into by language.
- Container and worktree isolation (behavioural): run an agent's implementation phase in an isolated environment (a git worktree and/or a container) so its changes are sandboxed and easy to review or discard rather than applied straight to the working tree. Because this is behaviour, it is a tool flag, not dropped content, and it would integrate the separate agent-box (`https://github.com/0xferrous/agent-box`) and agent-images (`https://github.com/nothingnesses/agent-images`) projects (separate repositories, not part of this one) rather than reimplement them.

Open sub-questions to resolve in the design pass (record them in Open Questions, with approaches, trade-offs, a recommendation, and reasoning judged against the Project Principles, before implementing):

- The `[[module]]` schema: how a module names itself and references its assets/variables (for example an inline group, or asset entries tagged with a `module = "<name>"` key), and how the loader includes only opted-in modules while the unnamed core assets always load.
- The selection mechanism: a `--module <name>` flag (repeatable), a manifest default-on/off per module, and whether the interactive TUI gains a module pane or mode (`selection-ui` deliberately scoped the TUI to principles and left room to reuse its interaction code for modules).
- Behavioural-module scope: what container/worktree isolation actually does here, whether it belongs in this tool at all or stays a separate concern the scaffolded assets point to, and how it integrates agent-box/agent-images without a heavy dependency.
- Interaction with `--template`: whether external packs may define their own modules (they should, since the manifest is the shared contract), and how `--module` validates against the active pack.

Each resulting sub-step must be evidence-grounded: validate with a proof-of-concept (build plus tests plus, where relevant, a functional run); on failure fall back to the recorded next-best; if exhausted, raise the impasse. A guiding invariant for validation: with no module selected, the scaffold output must be byte-identical to today's core output.

Design-pass decisions (2026-07-15/16, human-decided; `Q-25`). The step is RESHAPED from the illustrative modules above (diagram pack, justfile, language starters) to the two concrete modules the human wants, on top of the `--module` machinery; the illustrative ones remain possible future modules but are not the priority. Build in three reviewed increments, each its own review artifact: (1) the `--module` machinery; (2) the deterministic-checks module; (3) the isolation module.

- Module schema (HYBRID): assets self-declare membership with an optional `module = "<name>"` field on `[[asset]]` (and `[[var]]`) entries (untagged = core, always dropped; tagged = dropped only when `--module <name>` is passed), PLUS a lightweight `[[module]]` section declaring each known module's `name` and `description` (metadata, and the authoritative set that `--module` validates against; an unknown `--module` errors and writes nothing). Chosen over a group section that lists a module's assets (which duplicates the asset list and drifts, Principle 1) and over tag-only (no home for a description or a validation list): membership is single-sourced on the assets (no drift), metadata and the validated module set live in the small `[[module]]` section (Principle 1, Principle 2). The manifest parser already tolerates unknown keys, so it is non-breaking. Selection is a repeatable `--module <name>` flag under the `scaffold` verb; the loader drops core assets always and a module's assets only when it is selected, so with no module selected the output is byte-identical. A TUI module pane is deferred (the selector stays principle-only).

- Increment (2), the deterministic-checks module (`Q-25`): a module that scaffolds a user-configurable, pluggable deterministic verification gate over the workflow's CODE OUTPUT, generalising the hardcoded verification convention (`clippy` / `nix fmt` / ASCII-clean). It scaffolds (a) `ast-grep` rule config as the first-party linter plus a documented way to register other check and format commands (so a language with no ast-grep grammar plugs in `ruff` / `eslint` / `clippy` / etc.), (b) a `checks-reviewer` role prompt, and (c) workflow guidance wiring it in. Execution model (human-decided): FORMATTERS auto-apply during the IMPLEMENTER's verify step (a writer action, generalising today's `nix fmt`); the CHECKS-REVIEWER, spawned as one reviewer in the work-review phase and read-only, runs the LINTERS (detect) AND the formatters in CHECK / dry-run mode (verifying the code is actually formatted without applying, so it catches an implementer that did not format), and writes its findings to a findings file per `findings-files` (`Q-14`); the TRIAGER adjudicates those deterministic findings alongside the LLM reviewers' (fix vs suppress/accept), and the implementer fixes the valid ones. An optional git pre-commit hook may also be scaffolded as a mechanical backstop, secondary to the review-integrated path. Rationale: reuses the role-separated + findings-file pipeline (no new machinery), keeps the orchestrator's role clean (it spawns the checks-reviewer like any reviewer rather than producing findings itself), and makes both linting and formatting deterministically-verified gates, the same "do not trust the LLM, verify with a tool" ethos as the metrics validator and the schema drift guards (Principle 5, Principle 6). Pre-commit hook (human-decided, 2026-07-16): scaffold it as an OPTIONAL part of the checks module, offered at scaffold time either as a CLI flag or as a TUI option of the module (the exact opt-in mechanism, a `--with-precommit-hook`-style flag and/or a module option in the selector, is an increment-2 build detail; note this connects to the currently-deferred TUI module pane). The remaining OPEN decision blocking the build is the CHECKS CONFIG FORMAT (`Q-26`, deferred by the human pre-compaction, to be resurfaced): how a project declares its checks (a declarative `.agents/checks.toml`, recommended; vs justfile recipes; vs ast-grep native config + guidance). The config layout and guidance wording are settled once `Q-26` is decided, at the start of increment (2).

- Increment (3), the isolation module (`Q-18` mechanism; content-module scope, human-decided): a CONTENT module that scaffolds POINTER SETUP integrating the external agent-box (`https://github.com/0xferrous/agent-box`) and agent-images (`https://github.com/nothingnesses/agent-images`) projects for writer-agent isolation, transactional changes, and sandboxing, rather than reimplementing isolation or taking a heavy runtime dependency (the tool stays a scaffolder; the isolation RULE already ships via `agent-isolation`). Opted into with `--module isolation`, it drops the config/scripts that wire a project to worktree/container isolation (worktree-first per `agent-isolation`). Its exact integration shape (which files, how they invoke agent-box/agent-images) is settled in its own design pass when increment (3) is reached.

### `greenfield-flake`: Optional greenfield flake template

Not started; needs a design-questions pass first. Expose a greenfield template (a `templates` output in `flake.nix`, copied by `nix flake new -t`) as a convenience for the new-project case.

Core design tension to resolve before implementing: the core assets are embedded in the binary and rendered at runtime from a selected principle set, but a `nix flake new` template is copied verbatim and cannot render. So decide how the template produces a rendered `AGENTS.md`:

- (a) The template ships a thin init (a flake app, that is a runnable `nix run` output, or a note) that runs `agent-scaffold`, keeping the binary as the single rendering mechanism, with no duplicated content.
- (b) The template ships pre-rendered default assets directly. Simpler to consume, but duplicates the pack content and drifts from it over time.

The recommendation leans to (a), per Principle 1 (one rendering path) and Principle 4 (no drift), but this must be validated in the design pass. Evidence-grounding: a project created from the template initialises cleanly and its `AGENTS.md` matches an `agent-scaffold --principles default` run; fall back to (b) only if (a) cannot be made to work without Nix at scaffold time.

### `later-enhancements`: Optional later enhancements

Not started; conditional and needs a design-questions pass first. Two independent enhancements, to be taken up only if the create-if-absent plus `--force`-overwrite model (see the Ownership decision) proves too blunt in practice:

- Marked-block augmentation of an existing `AGENTS.md`: insert and update a tool-managed block between explicit markers, idempotently, so a hand-written `AGENTS.md` can carry the generated principles without being overwritten. Hard parts to design: the marker format, idempotent re-runs (replace the block in place, never duplicate it), and what belongs inside the block versus the user's own prose.
- An opt-in `update` command doing a 3-way merge: merge upstream pack changes into a user-edited working file. Hard part: where the base (the version last scaffolded) is stored, since a 3-way merge needs a base, the user's edits, and the new version.

Each is evidence-grounded (proof-of-concept plus tests, with a byte-identical-when-unchanged invariant for the augmentation case); record the design questions in Open Questions before implementing.

### `git-url-fetch`: Optional git-URL template fetch

Not started (deferred). Extend `--template` to accept a git URL, fetching the pack (shell out to the `git` binary, no Nix dependency) into a cache directory and then loading it through the same manifest path as a local pack (`byo-template`). This was deferred (it was open question OQ-H; see the note on OQ labels in the Open Questions section) as a much-later optional extra because the core bring-your-own value is already delivered by local-path packs. Sub-decisions to settle in the design pass: ref selection (which branch, tag, or commit of the repository to fetch), an optional in-repo subdirectory (allowing a pack that lives in a subdirectory of the repository rather than at its root), where and how to cache fetched packs, and the fallback behaviour when the `git` binary is not installed. When taken up, run the design-questions pass on those first.

### `tui-authoring`: Optional TUI pack authoring

Not started; optional and needs a design-questions pass first. Add a way to create and edit custom packs through the TUI, so a user can author a bring-your-own pack (`byo-template`) without hand-writing `pack.toml` and `principles.toml`.

Goal: an interactive editor for a pack's structure, reusing the TEA-lite `App`/`update`/`ui` patterns and shared widgets from the principle selector (`selection-ui` and the polish steps) without complicating the existing selection flow (Principle 2). It edits the pack manifest and principle data as structured forms and writes the pack directory back out; editing arbitrary asset source-file bodies is expected to stay out of scope (that is a text editor's job).

Scope (to confirm in the design pass): create a new pack directory with a `pack.toml`; add, edit, remove, and reorder `[[asset]]` entries (`source`, `dest`, `ownership`, `render`) and `[[var]]` entries (`name`, `default`); add, edit, and remove principles (the full `Principle` model: `id`, `name`, `summary`, `rationale`, `tags`, `default_selected`, `default_order`, `references`, `related`); and validate on save with the same rules the loader enforces (unique ids and `default_order`, no id or variable name colliding with a reserved token, referenced source files present).

Open sub-questions to resolve in the design pass (record in Open Questions with approaches, trade-offs, a recommendation, and reasoning judged against the Project Principles):

- CLI surface: a new flag (for example `--edit-pack <dir>` and `--new-pack <dir>`) versus introducing subcommands (`agent-scaffold pack new|edit <dir>`). The CLI is flag-only today, so subcommands are a structural change (clap supports both).
- Serialization: writing `pack.toml` with `toml` (lossy: drops comments and reflows the file) versus `toml_edit` (format- and comment-preserving). Editing an existing hand-written pack must not destroy its comments or unrelated content (Principle 3, safe on existing), which points toward `toml_edit`, but that is a new dependency to weigh (Principle 2). The `manifest` types are `Deserialize`-only today and would need `Serialize`, or an equivalent `toml_edit` mapping.
- TUI composition: whether pack authoring is a separate top-level TUI flow that reuses the shared reducer and widget patterns, or new modes on the existing `App`. Keep the principle selector uncomplicated; a module or pack pane was already anticipated as a reuse point (`selection-ui` scoped the selector to principles deliberately).
- Round-trip and safety: creating then loading a pack via `--template` must scaffold correctly, and re-editing a pack must preserve everything the editor does not touch.

Evidence-grounding: author a pack in the TUI, then load it with `--template` and scaffold, asserting the assets, variables, and principles round-trip; editing an existing pack leaves its untouched content (including manifest comments, if `toml_edit` is chosen) intact. Fall back to the recorded next-best on failure; if exhausted, raise the impasse.

### `workflow-calibration`: Calibrate the workflow's provisional round constants

Not started (deferred). The scaffolded workflow's round constants and the second-triager guard were introduced after a design review (see the `## [Unreleased]` entry in `CHANGELOG.md`) as provisional defaults chosen to be safe rather than measured: the required consecutive clean rounds (one for a trivial or low-risk artifact, two for a risky one) and the total-round cap (five). This step calibrates them against data from real use. This is deliberately deferred: the fixes it revisits were split into cheap no-regret changes applied now (durable ledger, termination caps, acceptance triager, dismissed-high-severity re-check) and the one recurring-cost change (consecutive clean rounds), whose default is provisional pending this data, so nothing here blocks earlier work.

Data to gather. Per review round: the artifact and phase (plan review, work review, or acceptance), whether the artifact changed since the previous round, the round outcome (clean, or new valid findings), the count and severities of valid findings, and the running consecutive-clean count. Per escalation: whether the human made a real decision or just resumed (useful versus friction). Per dismissed high-severity finding: whether the second triager upheld or overturned the dismissal. Per human interrupt: the trivial or non-trivial classification and whether a trivial call later had to be re-planned (a misclassification). The key signal for the required-clean-rounds default is whether a round following a clean round finds a triager-valid issue on an unchanged artifact, which is evidence that one clean round is noisy.

Collection method. For agent-scaffold's own tuning, instruct the dogfooding run (the pending full-codebase review) to record these to the ledger's round-summary lines and a scratch log; the ledger already carries per-round outcomes for this reason. Downstream users get collection through the opt-in `instrument-flag` module, not baked into the pack. Trigger: revisit the defaults once the first several real tasks have produced data. Evidence-grounding: the adjusted defaults must be backed by the logged round outcomes, not re-guessed; if the data is inconclusive, keep the safe defaults and record that.

### `instrument-flag`: Opt-in workflow instrumentation

Not started; optional. The design is decided below; the build is deferred. An opt-in scaffold flag (default off; `agent-scaffold --instrument`) that includes calibration-logging instructions in the rendered workflow and defines a durable, cross-task metrics log, so a user who wants to tune their own workflow's round constants can gather the data `workflow-calibration` describes without every project inheriting metrics collection.

Data-output format (decided). The per-task review ledger stays human-readable Markdown (rows per finding plus round-summary lines), because the orchestrator and humans read it during the task and it is committed beside its plan. The cross-task metrics log is JSON Lines (JSONL): one self-contained JSON object per event, append-only, machine-parseable, and tolerant of fields added over time, so a small script can aggregate many tasks. Each record carries a `type` discriminator (`round`, `escalation`, `dismissal_recheck`, or `intake`) and the fields listed under `workflow-calibration` for that event. This split keeps the in-task artifact readable and the analysis artifact parseable.

Decided design (`Q-4`, `Q-5`, `Q-6`). The CLI surface is a `--instrument` flag (`Q-4`) that toggles a rendered `{{...}}` block in the workflow templates, rather than an optional module under `optional-modules`. The log location is a committed cross-task file, `docs/metrics/workflow.jsonl` (`Q-5`), so it accumulates and travels. The orchestrator writes the JSONL directly rather than through a helper (`Q-6`). Evidence-grounding: with the flag on, a scaffolded project's workflow appends parseable JSONL records that a small script aggregates into the calibration inputs; with it off, the scaffold output is byte-identical to the non-instrumented run.

Outcome (complete; implemented in `1cd3211`, converged at `68da587`). Added the `--instrument` flag (`src/main.rs`), a reserved `{{instrument}}` computed variable (in `build_assets`, filled from the new `pack/instrument.md` render fragment when set, empty otherwise), and `instrument` in `RESERVED_VARS` (`src/manifest.rs`). `pack/instrument.md` carries the metrics-logging section (JSONL to `docs/metrics/workflow.jsonl`, record types `round` / `escalation` / `dismissal_recheck` / `intake` with their fields); it is a render fragment read directly, not a manifest asset. Reviewed with two independent reviewers (opus correctness, sonnet schema/design) and a separate triager: round 1 found two valid medium findings (the raw off render left trailing blank lines so byte-identity only held after the formatter; and the Pack format docs did not mention `{{instrument}}` or `instrument.md`), plus an accepted low (`unwrap_or_default` IO-error masking, matching the `principles.toml` convention) and a deferred low (records lack a task-id/timestamp for cross-task segmentation, carried forward into `state-schema` as `Q-24`'s neighbour); one finding (Roadmap not updated) was ruled invalid. The fix normalises every rendered asset to exactly one trailing newline (in `render()`), so off is now byte-identical without any formatter (a reviewer confirmed the raw binary output is byte-for-byte identical to the committed file), with a test pinning the invariant; the Pack format docs (README and this plan) now document both reserved variables and the fragment pattern. Round 2 (fresh reviewer) was clean, so the low-risk artifact converged at one clean round; 50 tests pass, clippy clean. See `docs/plans/agent-scaffold.ledger.md` for the round records. Data-integrity of the metrics (`Q-24`) is validated in `state-schema` (detection), and the task-id/timestamp gap (V-4) is recorded there to fix when that step lands.

### Workflow self-improvement cluster (structure over orchestrator disposition)

This cluster (`exploration-mode`, `ledger-parse`, `workflow-invariants`, `state-queries`) came out of a dogfooding incident on 2026-07-16: a `complete` step (`pause.md`) was committed outside the review loop on an orchestrator "match ceremony to stakes" judgement, and no check caught it. The human then asked whether the workflow adequately handles deterministic adherence (`Q-27`), token-efficient state navigation (`Q-28`), and design-space deliberation (`Q-29`). All three share one root cause: workflow state lives in the orchestrator's head rather than in checkable structure. The designs were settled by a two-independent-explorer design pass (opus + sonnet, different models) synthesised by the orchestrator, then decided by the human. Common architecture: extend the existing validator/projection family (single source of truth per region, detection not prevention, hard-fail, no binary required at workflow runtime). Build order: `exploration-mode` (cheapest, unblocks proper deliberation), then the `ledger-parse` keystone, then `workflow-invariants`, then `state-queries`; each a separately reviewed step.

### `exploration-mode`: First-class design-space exploration (`Q-29`)

Next. Decided (`Q-29`, human, after the two-explorer design pass). The workflow handles deciding and deferring a decision but has no first-class mode for exploring a design space before options are decidable, which is what the human asked for on `Q-27`/`Q-28`. Four low-ceremony fixes, mostly documentation plus one code line:

- Vocabulary: add `exploring` to `QUEUE_EXACT_STATUSES` in `src/plan.rs` (a sub-state of `open` meaning a design pass is owed before options can be presented), and document it in `pack/plan-template.md` so the existing `plan_template_documents_every_accepted_status` drift guard extends automatically. This is the only code change; `validate --plan` today hard-rejects that phrasing in the status field (as the orchestrator hit when first queuing `Q-27`/`Q-28`/`Q-29`), forcing the nuance into body prose.
- Artifact: a design-notes convention at `docs/plans/<task>.explorations/<question>.md` (parallel to the `docs/plans/<task>.reviews/` convention), whose shape is the human-input contract written to a file before options are decidable: the question, the design space (viable options), each option's trade-offs judged against the numbered Project Principles, a recommendation with reasoning, and an explicit "what I would NOT build" (YAGNI). Prose guidance, not a machine-parsed schema (the document is human-read).
- Intake mode: name design-space exploration as a third entry mode in `pack/AGENTS.md` alongside factual-answer and decision-gate (the Socratic-input section is the natural home): the orchestrator recognises a deliberation request, records an `exploring` queue item pointing at the design-notes file, spawns the explorer(s), optionally runs a light review, then presents options via the human-input contract; the resolved answer becomes a durable Open-Questions decision, converging under the existing no-re-raise-without-new-evidence rule.
- Optional light review: reuse the reviewer/triager roles as a single pass (not the convergence loop) with a "is the design space complete, are the trade-offs honest, is the recommendation Principle-grounded" lens; note that the multi-independent-explorer-plus-synthesis setup (which produced this very decision) is the adversarial analog. Match ceremony to stakes; not mandatory.

Explicitly NOT built: a new workflow phase parallel to Plan/Implement/Accept, a machine-parsed exploration schema, or an `explore` subcommand (the artifact is a Markdown file under a named convention). First dogfood instance: the `docs/plans/agent-scaffold.explorations/workflow-process-cluster.md` note capturing this cluster's own exploration.

Outcome (complete; implemented in `9757a64`, converged at `ad38211`). Added `exploring` to `QUEUE_EXACT_STATUSES` in `src/plan.rs` (documented in `pack/plan-template.md`, so the `plan_template_documents_every_accepted_status` drift guard extends automatically), the design-space-exploration intake mode and the `docs/plans/<task>.explorations/` design-notes artifact convention in `pack/AGENTS.md`, and the exploration-trigger user prompt `explore.md` (sibling of `kickoff.md`, folded in on the human's request during the step). Reviewed by two independent reviewers (opus correctness/consistency, sonnet doc quality) and a separate triager: round 1 found six valid findings, three medium contradictions (the intake prose made the orchestrator the writer rather than spawning explorers; the explorer was an unnamed writer outside the file-safety rules; `orchestrator.md`'s Socratic clause competed with the new mode) plus two low and a nit, all fixed, and two were dismissed as scope creep the step excluded. Round 2 (fresh confirming reviewer) verified all fixes and the `explore.md` wiring and was clean, so the low-risk artifact converged at one clean round; 95 tests, clippy clean, scaffold byte-identical. First live use of the `exploring` status: `Q-30` (test-driven module) and `Q-31` (mutation module), queued for a later design pass. See `docs/plans/agent-scaffold.ledger.md` for the round records.

### `session-preflight`: Preflight review and adherence confirmation (`Q-32`)

Next. Decided (`Q-32`, human, 2026-07-16). Give the workflow a defined moment where the orchestrator establishes adherence with the human instead of assuming it, closing the same "structure over disposition" gap the `pause.md` skip and the untested isolation exposed. Add a short preflight the orchestrator runs at two triggers: before the workflow starts (from the kickoff prompt) and whenever a session is resumed (from the resume prompt, after a compaction or a pause). In the preflight the orchestrator: (1) restates the workflow's process and the disciplines it requires (the review loop and convergence, the separate-triager rule, findings/exploration files, the file-safety rules, the checkpoint/queue-push cadence); (2) detects and states the available writer-isolation modes and tools in the current harness, resolving the `agent-isolation` tier order to what is actually available (container via `agent-box`/`agent-images` if wired, else git worktree, else the file-safety fallback); and (3) confirms with the human, per the human-input contract, what it will do to adhere, before proceeding. This also encodes the human's standing directive that writer work always runs in a separate, isolated (worktree-first) agent where possible, so the orchestrator drives and reviews but does not implement code directly; the preflight is where that resolves to the concrete tier for the session. Build as a pack-doc change: a preflight section in `pack/AGENTS.md`, a pointer from `pack/user-prompts/kickoff.md` and `pack/user-prompts/resume.md`, and the orchestrator's duty in `pack/prompts/orchestrator.md`. Relates to `workflow-invariants` (the deterministic counterpart: preflight is the human-facing confirmation, `--workflow` is the machine check) and `Q-33` (the isolation tiers it must be able to state).

Scope addition (human-decided, 2026-07-16): also document the WORKTREE LIFECYCLE and MERGE-BACK model in the same isolation-operations area of `pack/AGENTS.md`, since the isolation rule established the tiers but never said who merges an isolated writer's work back or who checks it. Decided model, review-in-worktree, merge-on-convergence: the orchestrator owns the worktree lifecycle (create the worktree, spawn the isolated implementer there, spawn the reviewers and separate triager against the worktree branch, drive fix rounds via isolated implementers in that worktree, and merge the branch into the main tree ONLY once the step converges, then remove the worktree), so main only ever carries reviewed, converged steps. The merge distinguishes authoring from integration (human-decided, 2026-07-16): a CLEAN merge (fast-forward or a conflict-free replay of the reviewed commits) authors nothing new, so the orchestrator performs it directly as integration (the same category as committing the plan and ledger), verifying only mechanical correctness (the merged diff is exactly the converged work; tests pass on main after the merge). A merge that needs AUTHORED conflict resolution counts as implementation, not integration (the resolution is content no reviewer has seen), so it is done by an isolated implementer in the worktree and reviewed, after which the orchestrator fast-forwards the now-clean branch. Correctness-according-to-the-plan is established by the review loop and the acceptance review, with `validate --workflow` (once built) as the deterministic backstop that the reviewed rounds happened. Note a merge targets main by definition, so it is intrinsically an un-isolated write; the axis that governs it is authoring-vs-integration, not isolation. This step is being built by the first worktree-isolated implementer, dogfooding the very model it documents.

### `ledger-parse`: Parse the ledger's structured regions (keystone)

Not started. The keystone the enforcement and query steps both consume, and it closes the `state-schema`-deferred "ledger round-summary parsing" gap. Add `src/ledger.rs` parsing the two structured regions of `.agents/LEDGER.template.md` (the free-prose Round records and RESUME STATE narrative are not parsed, exactly as `plan.rs` skips Step Details): (1) the round-summary pipe-table (Round / Artifact / Changed / Outcome / Valid findings / Consecutive clean); (2) a formalised artifact register table (Artifact / Risk class) making the loose prose classification a parsed source, with `RiskClass` extended to include `trivial` alongside the `low_risk`/`risky` already in `src/metrics.rs` (required-clean-rounds is derived from the class: trivial 0, low_risk 1, risky 2, one source of truth). Extract the pipe-table helper now duplicated in `plan.rs` into a small shared `src/table.rs`. `validate` gains ledger well-formedness checks (W1 every register artifact resolves to a Roadmap slug by its leading slug before an optional `-inc<n>`, the same cross-reference style as `decided -> folded into <slug>`; W2 round-table outcomes and the consecutive-clean column are internally consistent). Evidence-grounding (Principle 3): PoC the parser against this repo's own hand-written ledger before building out, and tighten the `LEDGER.template.md` round/register formats (with a drift guard mirroring `plan_template_documents_every_accepted_status`) to whatever the real ledger can satisfy. A derived `WorkflowState` (assembled from `plan` + `ledger` + optionally `metrics`, regenerable, never committed as a source) is the shared projection the next two steps read.

### `workflow-invariants`: `validate --workflow` enforcement (`Q-27`)

Not started. Decided (`Q-27`, human): extend `validate` with a `--workflow` mode (new `--ledger <path>` arg, conventional default `docs/plans/<task>.ledger.md`) that cross-references plan status against the ledger (primary, always present during a task) and the metrics log (secondary corroboration when instrumented), hard-failing (exit 1) into the existing `validate` problem list. Detection not prevention: runs in CI, the checks pre-commit hook, or on demand, with no binary required at workflow runtime. The key invariant (W3): for every Roadmap step marked `complete`, there must be a register entry whose leading slug is that step; if its class is `trivial`, that suffices, otherwise the round table for that artifact must reach the required consecutive-clean streak for its class. A missing register entry is the `pause.md` catch, "step X is complete without a recorded review classification". The strictness tension resolves as trivial-is-declared-not-skipped: a trivial doc-only change may skip the review loop but must be declared `trivial` in the register, so the skip is visible and checked; "doc-only" is not auto-detected (that is agent-owned policy). Optional W4 (only when a metrics log is present): the `round` records for an artifact agree in count and outcomes with the ledger round table, catching a divergent hand-written log. Explicitly rejected: the `agent-scaffold step complete <slug>` state-machine mutation gate (prevention, stateful, makes the CLI a second plan writer and a workflow-runtime dependency); detection gives the same guarantee without those costs. Depends on `ledger-parse`.

### `state-queries`: `next` and a `status --resume` slice (`Q-28`)

Not started. Decided (`Q-28`, human): two thin projections over the `ledger-parse` `WorkflowState`, deliberately small. `agent-scaffold next` emits the single authoritative next required action (derived from the same transition logic `workflow-invariants` checks, so `next` computes the legal next step and the enforcement check verifies the recorded state did not skip one). Realise `state` as an enhanced `status` rather than a third verb: `status` gains a compact default (current step and status, open-status questions, current ledger streak) with the full `{plan, metrics, ledger}` projection behind `--json`, plus a `--resume` mode that also echoes the ledger's `## RESUME STATE` section verbatim (it is human-authored narrative, extractable but not derivable). Token-efficiency reality (the human's stated worry, answered honestly): the win is real and large only at cold resume after a compaction (the unbounded ledger is re-read there) and in a CI/hook (zero agent context), and is negative per-turn during active work when the file is already in context; so scope these to the resume and check paths, repoint `resume.md`/`compaction-prep.md` at `next`/`status --resume` instead of "read the whole ledger", and do not instruct the orchestrator to poll them each turn. Explicitly NOT built: a separate `state` verb (synonym for `status --json`) or a wholesale navigator. Depends on `ledger-parse`; reuses `workflow-invariants`' transition logic.

## Success Criteria

- One command, run from any working directory and targeting any `--output-dir`, drops the minimal core (a usable `AGENTS.md`, a planning-document template, and the core prompts) into both an empty directory and an existing repository. Writes are off unless confirmed: a run previews the plan and only writes with `--write` or the selector's Save confirmation (`--dry-run` previews explicitly), so nothing is written by accident.
- By default it creates only absent files and never modifies existing ones; `--force` is required to overwrite; a default re-run therefore reverts nothing.
- The user can choose which principles and modules to include, interactively with sane defaults or non-interactively via flags or config.
- The dropped assets are immediately usable to run one pass of the workflow (plan -> review -> implement -> review) without further setup.
- Optional modules can be added without touching or complicating the core.
- The tool can scaffold from a user-supplied template pack, not only the built-in one.
- The scaffolded workflow is internally consistent: `pack/AGENTS.md`, the prompts under `pack/prompts/`, the README diagram, and this plan state the same rules the same way, and the convergence accounting (round outcomes and the single escalation cap) is deterministic and computable from the review ledger alone.
- The self-scaffolded copies stay reliably in sync with the pack: editing a `pack/` file and running a plain `cargo build` (no manual `cargo clean`) re-embeds the change, so `scaffold-self` and the golden sync-test regenerate from the current pack, not a stale embedded one.
- The human-interface layer is present: onboarding (a "Getting started, for the human" section that points to the editable kickoff prompt in `.agents/user-prompts/` and states the human's ongoing decision duty) and a single living human-decision queue that the orchestrator updates and pushes to the human at every checkpoint.
- The scaffolded workflow carries the human-input contract (every human-input point, escalation, intake, open questions, and clarifying questions, presents options, trade-offs, a recommendation, and reasoning judged against the project's numbered Principles, scaled to stakes) and the always-separate triager rule (the triager is a separate agent, independent of both the producer and the orchestrator, for every review round).
- The scaffolded workflow carries a git-durability-and-recovery discipline: a clean tree before spawning a writer, commit-before-delete for workflow-managed files, own-files-only formatting (no repo-wide formatters or cross-owner `git checkout`), an orchestrator recovery protocol on any kill or interrupt, and destructive validation in scratch. Writer agents run under capability-tiered isolation (container if available, else worktree, else that discipline), and read-only agents need none.
- Review findings flow through per-agent files that other agents read directly and the ledger references by path, so no finding is lost to orchestrator transcription; the files are committed before the orchestrator deletes them.
- A compaction-prep checkpoint keeps the plan and ledger authoritative for resuming after context loss: before a compaction the orchestrator flushes and commits the plan, the ledger, and the Open-Questions queue and verifies the plan's resume anchor, and on resume state is reconstructed from `AGENTS.md`, the plan, and the ledger.
- Human-invoked user prompts (the kickoff prompt and the compaction-prep prompt) live in their own `.agents/user-prompts/` directory, separate from the role prompts in `.agents/prompts/`, and stay thin triggers that do not duplicate workflow content.
