# driver-output-generation inc1 - Round 2 re-review (fresh adversarial reviewer)

Verdict: CLEAN. No findings. This is the first potential clean round after the new_valid round; a second consecutive clean round is still required to converge the RISKY increment.

Scope reviewed: diff `main (421cc92)..HEAD (23abe24)`, fix commits `d48d185`/`b708484`/`23abe24` on top of `06c9cc4`/`ec4f1bb`. Read/tested in worktree `.claude/worktrees/dog-inc1`.

## What I probed (all pass)

- D1-1 tier-agnostic fragment (`src/isolation_policy.rs:31`): the `ISOLATION_POLICY_FRAGMENT` const no longer hardcodes a tier. `grep` for `worktree` / `merges its branch` in the const returns nothing (the only `worktree` hit is line 14, a doc-comment example of the tier order, not the emitted fragment). New wording "each runs isolated under the same tier order as the implementer, and the orchestrator integrates its result on convergence" is tier-general and coherent. Content-pin test `the_fragment_states_the_writer_classification` (`src/isolation_policy.rs:38`) is genuine: it asserts the real substrings "A spawned planner is a writer" and "distinct from the orchestrator's own integration-level edits on main", so a reword fails it directly. Passes.
- D1-2 build-path slot test `isolation_policy_slot_renders_the_generated_fragment` (`src/main.rs:1919`): non-vacuous. It calls `build_assets`, then for both `AGENTS.md` and `.agents/AGENTS.reference.md` asserts the raw `{{isolation_policy}}` placeholder is absent AND the generated fragment is present; a broken insert or a slot typo fails it. Mirrors `workflow_control_slot_renders_the_generated_fragment`. Byte-guard `the_committed_scaffold_carries_the_isolation_policy_fragment` (`src/isolation_policy.rs:63`) passes post re-scaffold.
- D1s-1 preamble trim (`pack/prompts/orchestrator.md:13`, identical `.agents/prompts/orchestrator.md:13`): the writer-spawn preamble no longer restates the writer classification. `grep` for "planner is a writer" in the preamble returns nothing. It names the slot ("including which roles count as writers and the authoring-versus-integration line") and references "the Writer isolation rule in `AGENTS.md`". Per-spawn originated facts (role, isolation tier, worktree path) and the "treat its absence as a defect, not a matter of taste" framing remain. No P8 second source of policy content.
- Scaffold consistency: `pack/prompts/orchestrator.md` == `.agents/prompts/orchestrator.md` (byte-identical `diff`); root `AGENTS.md` and `.agents/AGENTS.reference.md` carry the same fragment; `pack/AGENTS.md:91` carries the `{{isolation_policy}}` slot. Byte-guard test confirms the committed output equals the generated fragment (regen, not hand-edit). `render --check` reports "up to date".
- Scope + regression: `src/next.rs`, `docs/plans/agent-scaffold.plan.toml`, `docs/metrics/*`, `src/metrics.rs` all untouched (empty `git diff`). `src/main.rs` committed diff is only: `mod isolation_policy;`, the `{{isolation_policy}}` doc-comment line, the `build_assets` insert, and the new test; no stray reformatting of `run_resume`/`run_next` and no behaviour change. `src/manifest.rs` adds only `isolation_policy` to `RESERVED_VARS`. `just clippy` clean. Full suite `cargo test --bins -- --test-threads=1`: 337 passed, 0 failed. `validate` and `validate --workflow` both valid / invariants hold.
- Style: no non-ASCII (em/en-dashes, unicode, emoji) in any changed file; no double-hyphen-as-dash in the fragment source; no `#[allow]` attributes added.

## Non-blocking note (inc2, already flagged by the triager; not a finding)

The fragment retains "per the capability-tiered tier order above" (`src/isolation_policy.rs:31`). The word "above" is anchored inside `AGENTS.md` where the tier order precedes it, so it reads correctly in the inc1 rendered guidance. When inc2 emits this fragment STANDALONE as the driver's writer-state reminder, "above" will dangle. This is an inc2 concern (the standalone emission does not exist yet), matching the triager's ruling; it does not block inc1.
