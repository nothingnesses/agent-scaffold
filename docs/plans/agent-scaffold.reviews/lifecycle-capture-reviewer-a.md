# Reviewer A findings: `plan/lifecycle-capture` (245200e..987a253)

Lens: faithfulness and consistency. Read against `AGENTS.md` (base 245200e) and the branch content in the worktree (987a253). Note: the main repo checkout is at the base commit 245200e, so all branch checks below were run against the worktree / `git show 987a253` rather than the main working tree.

## Finding 1: decision receipt `task` breaks the folded-into-slug convention

Severity: low

Evidence: `docs/metrics/workflow.jsonl` (appended last line) sets `"task":"lifecycle-capture"` on the `type:"decision"` receipt for `q_id:"Q-57"`. But `docs/plans/agent-scaffold.plan.toml` Q-57 has `folded_into = "formatter-reflow-convention"`, and all nine prior `type:"decision"` receipts in the same log set `task` to the STEP SLUG the decision folded into (Q-51 -> `workflow-driver`, Q-53 -> `task-entry-regrounding`, Q-54 -> `human-input-gate-reinforce`, Q-45/46/47/48 -> `structured-skeleton`, etc.). By that established pattern the `task` here should be `"formatter-reflow-convention"`, not the orphan bookkeeping task id `lifecycle-capture`.

Why it matters: the `task` field is provenance/attribution. Anyone filtering receipts by step slug (`task == "formatter-reflow-convention"`) will not find this one, and it is the only receipt whose `task` names the bookkeeping pass rather than the folded-into step. Impact is bounded: `validate --workflow`'s W4 check keys on `q_id` (per `AGENTS.md:145`), so enforcement still passes and `chosen` is validated regardless. This is a consistency deviation, not a broken receipt.

## Finding 2: pack reconciliation does not address the "leaves incidental reformatting to the orchestrator" clause

Severity: low

Evidence: the new sentence in `pack/AGENTS.md` (regenerated identically into `AGENTS.md` and `.agents/AGENTS.reference.md`) reconciles the new convention with only one half of the "Format only your own files" rule: it says the convention "is distinct from the 'Format only your own files' file-safety rule above, which still holds that a writer does not proactively run a repo-wide formatter; ... so the two do not conflict." But that rule (`AGENTS.md:79`) has a second clause: an implementer "leaves incidental reformatting to the orchestrator." The new convention instead has the writer KEEP its own incidental reflow rather than leaving it to the orchestrator. The reconciliation sentence never mentions that clause.

Why it matters: under the narrow reading of `AGENTS.md:79` (the whole clause is scoped to files the implementer does not own) there is genuinely no conflict, since the new convention governs reflow inside the writer's OWN changed/regenerated files. But a reader who takes the broad reading ("any incidental reformatting is the orchestrator's job") sees an apparent contradiction the new text does not resolve. The claim "the two do not conflict" leans on the narrow reading without stating it. This is an incompleteness in the reconciliation, low impact because the narrow reading is the natural one in context.

## Checked and clean (no finding)

- Faithfulness of the pack convention: the wording encodes the human's actual decision (incidental reflow accepted, not a finding, not reverted/stashed/`git checkout`-ed, reviewers and triagers do not raise it). It does NOT overreach: it is scoped to "formatting your own changed files (or regenerating a scaffolded file)" and explicitly restates that a writer "does not proactively run a repo-wide formatter," so it does not license proactive repo-wide formatting.
- Receipt integrity: `chosen` ("Change policy: accept incidental reflow, stop reverting and stashing it") is `options[1]`; `recommendation` ("Format the code and exclude the prose docs from the formatter") is present and is `options[0]`; `chosen != recommendation`, and the plan's Q-57 `ask` narrates exactly that ("Chosen over the orchestrator's recommendation ... and over keeping the per-hunk reverting"). Recording `chosen` as a member of the presented `options` honestly represents that the full set was put to the human (the audit basis in `AGENTS.md:145`).
- Q-57 coherence: `status = "decided"`, `folded_into = "formatter-reflow-convention"`, `receipt = "Q-57"`, and the rewritten `ask` all agree; `folded_into` names a real step slug added in this same diff (`[[step]]` slug `formatter-reflow-convention`, `order = 68`), whose `[step.provenance] decisions = ["Q-57"]` back-references Q-57.
- W4 enforceability claim: Q-57's `ask` says "Q-57 is past the Q-44 baseline, so W4 enforces the receipt." Confirmed: `[meta].w4_baseline = "Q-44"`, Q-57 > Q-44, and a matching `q_id:"Q-57"` receipt exists. (Aside, not a finding for this diff: `AGENTS.md:145` still describes the baseline as a `type:"baseline"` JSONL record, whereas the live mechanism is `[meta].w4_baseline` in the TOML per Q-46's cutover; the Q-57 `ask` correctly says "the Q-44 baseline" without misstating the mechanism. This drift pre-dates the change.)
- Order collision: `order = 68` is unique on the branch (no duplicate `order` values; prior max was 67).
- Status-line arithmetic: "68 steps ... 2 in progress" and "7 open questions" are consistent. Branch step count 67 -> 68 and in-progress 1 -> 2 from the new step. "Open questions" = open (5) + exploring (2) = 7, matching the base convention where 6 = open (5) + exploring (1); flipping Q-57 open->decided (-1 open) plus Q-60 open (+1) nets open unchanged at 5, and Q-59 adds the second exploring.
- Q-59 scope: correctly `exploring` (a design pass owed). It lists the design space as options (1)(2)(3) without choosing, and its principle citations (P5/P8 as drivers, P2 as the caution behind capturing it `exploring`) frame WHY it is worth exploring, not WHICH option wins, so it does not pre-decide. Its code-reality citations are accurate: `next`'s `LoopState` in `src/next.rs` has exactly the nine listed variants (ReadyToPlan, Blocked, AwaitingFirstReview, AwaitingFixes, AwaitingReviewers, Converged, Escalate, RiskClassConflict, Done) and no Active/Checkpointed/Paused state; `pause.md`, `compaction-prep.md`, `resume.md` exist under `.agents/user-prompts/`; the "Checkpoint and resuming after context loss" section and the `workflow-driver` step and Q-58 all exist. Self-contained per Principle 20.
- Q-60 scope: correctly `open`, not pre-decided; records the orchestrator's lean transparently and explicitly says "Do NOT edit the Preflight text in this pass," and the diff indeed leaves the Preflight section untouched. Its premise is accurate: the Preflight text (`AGENTS.md:104`) references the human-input contract but does not restate the recommendation-plus-trade-offs-plus-Principle-reasoning requirement. Self-contained (what/why/who-raised/when).
- Empty Q-59.md / Q-60.md sidecars: consistent with convention; every existing `docs/plans/agent-scaffold.questions/*.md` sidecar is 0 bytes (the `ask` prose lives in the TOML).
- Factual claims revised in the Q-57 `ask` / step detail: "~38 parked `git stash` entries" matches the actual `git stash list` count (38 exactly); "writers stashed" wording aligns with the decision.
- New step artifacts are mutually consistent: Roadmap table row (`in progress`), Step Details body in `agent-scaffold.md`, the `formatter-reflow-convention.md` sidecar, and the TOML `[[step]]` all agree; `[meta].orphan_tasks` adds `"lifecycle-capture"` in alphabetical position.
- Per role contract, line length / prose wrapping and incidental formatter reflow were not treated as findings.
