# prompt-drift-guard, final review: independent re-measurement of the narrowed complement claim

Worktree `.claude/worktrees/final-b-pdg`, detached HEAD `1b9d4cf` ("docs: narrow drift-guard complement claim to asset copies"). Lens: the one sentence at `src/agents_md_drift.rs:61-68` and its immediate paragraph, re-measured from the manifest and the code rather than from any prior measurement.

## FINDINGS: ZERO.

No finding at any severity. No `critical`, no `high`, no `medium`, no `low`. Every conjunct of the narrowed sentence holds for every member of the set it quantifies over, measured below. Stated explicitly, as the role prompt requires: I found nothing to raise.

## The sentence as it stands

`src/agents_md_drift.rs:61-68`, quoted exactly:

> Leaving them uncovered is a scope call whose cost is uneven: widening to the Markdown asset copies is a small change to check 3's filter, since they are prose under the same prettier settings and already satisfy the precondition, while the TOML copies need a comparison of their own, since `.agents/principles.toml` carries lines outside canonical whitespace form (indented multi-line array continuations among them) that `assert_no_unprotected_construct` rejects, and `normalize_wrapping` is a Markdown prose transform with no business canonicalizing TOML.

The word under test is `asset` at `:62`.

## 1. The set, derived independently. SIZE = 16.

Derivation, not inherited. I copied the worktree to a scratch directory (no edit to this worktree's `src/`), added a probe test to the copy's `mod tests` that calls the module's own `self_scaffold_assets()` (`src/agents_md_drift.rs:134-141`, the pinned no-modules self-scaffold config), printed every rendered `dest`, and then applied the filter the sentence's subject denotes: `dest` ends `.md`, minus `AGENTS.md` (check 1), minus `.agents/AGENTS.reference.md` (check 2), minus anything under `PROMPT_DEST_PREFIX` (check 3, `src/agents_md_drift.rs:125` and `:428`). What remains is exactly the Markdown `[[asset]]` copies the COMPLEMENT paragraph covers.

Re-runnable command (from a scratch copy of the worktree carrying the probe test):

```
cargo test --quiet agents_md_drift::tests::probe_markdown_asset_copies -- --nocapture
```

The pinned render emits 30 assets. The 16 members:

1. `docs/plans/TEMPLATE._status-narrative.md`
2. `docs/plans/TEMPLATE.motivations.md`
3. `docs/plans/TEMPLATE.principles-note.md`
4. `docs/plans/TEMPLATE.documentation-protocol.md`
5. `docs/plans/TEMPLATE.repo-layout.md`
6. `docs/plans/TEMPLATE.queue-intro.md`
7. `docs/plans/TEMPLATE.roadmap-intro.md`
8. `docs/plans/TEMPLATE.success-criteria.md`
9. `docs/plans/TEMPLATE.steps/example-step.md`
10. `.agents/LEDGER.template.md`
11. `.agents/user-prompts/kickoff.md`
12. `.agents/user-prompts/explore.md`
13. `.agents/user-prompts/review.md`
14. `.agents/user-prompts/pause.md`
15. `.agents/user-prompts/compaction-prep.md`
16. `.agents/user-prompts/resume.md`

My count is 16, agreeing with the prior measurement. Nothing to report as a discrepancy.

What the narrowing correctly drops, and why the pre-narrowing sentence was false:

- `docs/plans/TEMPLATE.md` is NOT among the 30 rendered `dest` values, so it is not a manifest asset and is not a member. `pack/pack.toml:36-37` says so in the manifest itself: "`scaffold` renders these into the generated `docs/plans/TEMPLATE.md` after the drop (the generated view is not a manifest asset; it is regenerated by `render`)."
- It is also formatter-excluded by name at `flake.nix:53` (`"docs/plans/TEMPLATE.md"`), so it is not "prose under the same prettier settings" either. Demonstrated below as a negative control.
- Both facts are consistent with the narrowing: `asset` removes exactly the member that falsified the sentence, and removes nothing else. The `docs/plans/TEMPLATE` family named at `:61` contributes 9 members (its Markdown asset rows) and correctly contributes neither `TEMPLATE.md` (not an asset), `TEMPLATE.plan.toml` (not Markdown), nor the two `.gitkeep` rows (not Markdown).

## 2. "already satisfy the precondition": 16 accepted, 0 rejected.

I did not reimplement the predicate. The probe calls the module's own `precondition_rejects` (`src/agents_md_drift.rs:515-523`), which wraps the real `assert_no_unprotected_construct` (`src/agents_md_drift.rs:197-222`) in `catch_unwind`. It was run over BOTH sides of the comparison check 3 would perform on each member: the committed file at `CARGO_MANIFEST_DIR/<dest>` and the freshly rendered `asset.contents`.

Result, verbatim from the probe:

```
PROBE complement markdown asset copies: 16
PROBE member docs/plans/TEMPLATE._status-narrative.md | committed accepted | rendered accepted | normalizes_equal Ok(true)
... (14 more, all identical in shape) ...
PROBE member .agents/user-prompts/resume.md | committed accepted | rendered accepted | normalizes_equal Ok(true)
PROBE accepted=16 rejected=0
```

Accepted 16, rejected 0, on both sides, with no member missing from the working tree. The conjunct holds. The probe additionally reports `normalizes_equal Ok(true)` for all 16, so a widened check 3 would pass today rather than fail on pre-existing drift.

## 3. "prose under the same prettier settings": no member is formatter-excluded.

Static evidence:

- The only formatter config is treefmt in `flake.nix` plus the repo-root `.prettierrc.json`. `.prettierrc.json:2` is `"proseWrap": "never"`, and there is no `.prettierignore` and no other prettier config anywhere in the repo (`ls .prettierrc* .prettierignore treefmt.toml` returns only `.prettierrc.json`).
- `flake.nix:64-69` gives prettier `includes = ["*.md" "*.yml" "*.yaml" "*.json"]` with no per-formatter excludes.
- `flake.nix:50-54` is the whole global exclude list: `src/plan/testdata/render-fixture*`, `docs/plans/agent-scaffold.md`, `docs/plans/TEMPLATE.md`. None of the 16 members matches any of those three patterns; the two Markdown entries are literal paths with no wildcard, so `docs/plans/TEMPLATE.md` does not reach `docs/plans/TEMPLATE.motivations.md` or any other member.

Behavioural evidence, run in a scratch copy so this worktree is untouched. I appended a deliberately hard-wrapped two-line paragraph to each of the 16 members plus two negative controls, ran `nix fmt` once, and checked whether prettier rejoined the wrap (`proseWrap=never`):

```
PRETTIER-APPLIED docs/plans/TEMPLATE._status-narrative.md
PRETTIER-APPLIED docs/plans/TEMPLATE.motivations.md
PRETTIER-APPLIED docs/plans/TEMPLATE.principles-note.md
PRETTIER-APPLIED docs/plans/TEMPLATE.documentation-protocol.md
PRETTIER-APPLIED docs/plans/TEMPLATE.repo-layout.md
PRETTIER-APPLIED docs/plans/TEMPLATE.queue-intro.md
PRETTIER-APPLIED docs/plans/TEMPLATE.roadmap-intro.md
PRETTIER-APPLIED docs/plans/TEMPLATE.success-criteria.md
PRETTIER-APPLIED docs/plans/TEMPLATE.steps/example-step.md
PRETTIER-APPLIED .agents/LEDGER.template.md
PRETTIER-APPLIED .agents/user-prompts/kickoff.md
PRETTIER-APPLIED .agents/user-prompts/explore.md
PRETTIER-APPLIED .agents/user-prompts/review.md
PRETTIER-APPLIED .agents/user-prompts/pause.md
PRETTIER-APPLIED .agents/user-prompts/compaction-prep.md
PRETTIER-APPLIED .agents/user-prompts/resume.md
NOT-FORMATTED docs/plans/TEMPLATE.md
```

Controls, same method: `AGENTS.md`, `.agents/AGENTS.reference.md` and `.agents/prompts/reviewer.md` (the ALREADY-GUARDED files, i.e. the "same" the phrase compares to) all came back `PRETTIER-APPLIED`; `docs/plans/agent-scaffold.md` came back `NOT-FORMATTED`. So the two excluded files behave differently from all 16 members, and the 16 members behave identically to the guarded files. "The same prettier settings" is literally true: one config, one include rule, and no member in the exclude list.

Note the negative control `NOT-FORMATTED docs/plans/TEMPLATE.md` is precisely the falsifier the inserted `asset` removes.

## 4. "a small change to check 3's filter": true, and nothing structural blocks it.

Check 3's filter is one line, `src/agents_md_drift.rs:428`:

```
.filter(|asset| asset.dest.starts_with(PROMPT_DEST_PREFIX))
```

I tested the claim literally rather than judging it by eye. In a second, unperturbed scratch copy I changed that single line to `.filter(|asset| asset.dest.ends_with(".md"))` and ran the module's tests:

```
running 5 tests
.....
test result: ok. 5 passed; 0 failed; 0 ignored; 0 measured; 362 filtered out
```

Baseline in this worktree at `1b9d4cf` is the same: 5 passed, 0 failed. So one token in one line widens check 3 from 7 destinations to all 25 Markdown assets of the pinned render (the 16 members, plus the 7 prompts it already covered, plus the 2 already covered by checks 1 and 2) and the suite stays green.

Nothing structural makes it larger than the sentence implies. `committed_asset` (`src/agents_md_drift.rs:158-166`) joins any `dest` onto `CARGO_MANIFEST_DIR`, so it needs no change; the precondition and `normalize_wrapping` calls in the loop are `dest`-agnostic; the non-vacuity assertion still fires. `grep -rn "PROMPT_DEST_PREFIX" --include=*.rs .` returns hits only inside `src/agents_md_drift.rs` (`:45`, `:125`, `:428`, `:436`), so no other test or module depends on the filter.

Two observations that I deliberately do NOT raise as findings, because neither makes the sentence false:

- A widened filter would want the constant, the test name, and the non-vacuity message renamed off "prompt". That is naming around the filter, not the filter; the sentence scopes its claim to "check 3's filter".
- 9 of the 16 members are `ownership = "working"` rows (`pack/pack.toml:43-86`) rather than tool-owned `reference` rows. That is an argument about whether widening is DESIRABLE in a downstream project, not about the size of the filter change, and in this repo `just scaffold-self` passes `--force` (`justfile:47`), so the working copies are regenerated and a guard over them would be coherent here.

## Also checked: the second half, "the TOML copies", without the `asset` qualifier.

Not false, and not raised. Evidence:

- The antecedent one line above at `:60` is "the `.toml` copies under `.agents/`", and those are all manifest asset rows (`.agents/principles.toml`, `.agents/workflow.toml`, and the module-gated `.agents/checks.toml`). There is no non-asset TOML copy for the missing qualifier to wrongly sweep in, which is exactly the asymmetry with the Markdown case: there, `docs/plans/TEMPLATE.md` existed as a non-asset generated Markdown view and made the unqualified reading false.
- The stated reason is verified. Running the real predicate over the committed TOML copies: `.agents/principles.toml` REJECTED, first non-canonical line 4 (`"#   id               stable slug (kebab-case); ..."`); `docs/plans/TEMPLATE.plan.toml` REJECTED, first non-canonical line 21 (`"  \"TEMPLATE._status-narrative.md\","`); `.agents/workflow.toml` accepted.
- The parenthetical "(indented multi-line array continuations among them)" is accurate for `.agents/principles.toml`: `grep -n "^[[:space:]]\+" .agents/principles.toml` returns 6 lines, all array continuations, at `:248-249`, `:264`, and `:514-516`.
- `.agents/workflow.toml` being accepted by the precondition does not falsify the half: the sentence attributes the rejection specifically to `.agents/principles.toml`, and gives a second reason that covers all TOML regardless ("`normalize_wrapping` is a Markdown prose transform with no business canonicalizing TOML"). Both halves are true; the vocabulary differs, the truth value does not. No finding.

## Verdict

The narrowed sentence is TRUE as written. Set size 16; precondition accepted 16/16 on both sides; formatter coverage 16/16 with the two known exclusions demonstrated to behave differently; the filter widening demonstrated as a one-token change with the suite green.

## Method notes

- This worktree's `src/`, plan, ledger, and metrics log were not modified. The only file written is this one. All instrumentation ran in throwaway copies of the worktree under the session scratchpad.
- `cargo test --bin agent-scaffold agents_md_drift` at `1b9d4cf` in this worktree: 5 passed, 0 failed. `checks::tests` was not exercised, so the known step-93 flake did not arise.
- Settled items were not re-raised: the mechanism, the `checks-reviewer.md` exclusion (independently reconfirmed as a side effect of the derivation, since it is absent from the 30 rendered `dest` values), residual R1, residual RD-V2, line length, and formatter reflow.
