# Q-64 / Q-65 fold: faithfulness review

Lens: faithfulness of the fold diff (`git diff main HEAD`) against the two human decisions and the Q-64 synthesis. Two findings, both low. The core encoding (question entries, receipts, endorsed-vs-gated split, promoted steps, identity, rename checklist) is faithful; see the per-check confirmations after the findings.

## Findings

- F1 (low). Location: `docs/plans/agent-scaffold.steps/rename-to-agent-flow.md` line "Yank the old `agent-scaffold` versions only AFTER the redirect and the new crate are live." (mirrored in `docs/plans/agent-scaffold.md` line 134). Problem: the yank step is invented. The human's rename instruction specified exactly four crates.io actions (reserve `agent-flow` at 0.0.2; first real release at 0.0.3; final agent-scaffold publish links to agent-flow and states the name is free to reclaim by opening an issue on the agent-flow repo; hyphen/underscore equivalence). Yanking old versions was not among them, and it sits in mild tension with "free to reclaim": a yanked crates.io crate still holds its name, so yanking does not free `agent-scaffold` for a reclaimer and is orthogonal to the decided handoff-by-issue mechanism. Suggested fix: drop the yank bullet, or move it out of the "durable notes" derived from the decision and label it explicitly as an implementer's open question rather than a decided step, so it is not read as part of the human's instruction.

- F2 (low). Location: `docs/plans/agent-scaffold.steps/generated-projection.md` endorsed-now list (items a/b/c + the two promoted steps); same list in `docs/plans/agent-scaffold.md` step body. Problem: the synthesis recommendation (`Q-64-synthesis.md:77`) names the endorsed core as including "the isolation D4/D5 work (already decided under Q-62)", i.e. both D5 (widen the reminder to reviewer/triager-spawn states, captured here as the promoted `driver-isolation-reminder-scope` step) and D4 ("resolve the isolation tier", `Q-64-synthesis.md:56`). The fold captures only the Q-62 reminder-widening step; D4 tier-resolution is not separately tracked as an endorsed-now item nor listed as gated. Note this is consistent with the human's enumerated decision (which lists only the two promoted steps Q-60 and Q-62), so it is likely intended to be subsumed under `driver-isolation-reminder-scope` or carried by the existing isolation-tier machinery rather than dropped. Suggested fix: confirm D4 is intended to fold into `driver-isolation-reminder-scope` (or is deliberately out of scope); if so, one clause in the generated-projection step body saying the tier-resolution is part of the Q-62 promoted step would close the gap between the synthesis wording and the fold. No change needed if the orchestrator confirms it is subsumed.

## Per-check confirmations (faithful)

- Q-64 / Q-65 `[[question]]` entries: both flipped `exploring` -> `decided`; `folded_into` and `receipt` correct (`generated-projection`/`Q-64`, `rename-to-agent-flow`/`Q-65`); appended DECIDED sentences state Option A refined (slot-wise, not whole-file; endorsed core = drift guard + principle-by-name + roles/findings-naming + promoted Q-60 and Q-62; gate the workflow.toml rule-fragment + `{{workflow_phases}}` margin behind sec-2(d)) and the agent-flow release-timed rename (0.0.2 reserve, 0.0.3 first real release, reclaim-by-issue, not a meta-harness) with no invented scope.
- Decision receipts (`workflow.jsonl:9-10`): options non-empty; `chosen` is an exact member of `options` for both; recommendation present. Q-64 task=`generated-projection`, q_id=Q-64, chosen=recommendation=Option A refined. Q-65 task=`rename-to-agent-flow`, q_id=Q-65, chosen=the agent-flow release-timed option (NOT flowgate), recommendation=flowgate (allowed).
- Endorsed-vs-gated split: `agents-md-drift-guard`, `principle-by-name-projection`, `roles-findings-naming-slots` are `next`; `generated-projection` umbrella is `in-progress`; `workflow-toml-rule-fragments` is `deferred` with the sec-2(d) (i)/(ii)/(iii) un-gate signal quoted. No build item smuggled in beyond the five decided; the five endorsed items match the human's enumeration.
- Promoted steps: `single-source-recommendation-rule` (order 76, Q-60) and `driver-isolation-reminder-scope` (order 77, Q-62) flipped `deferred` -> `next`; orders and `[step.provenance].decisions` unchanged.
- Rename checklist: no in-place rename / publish new crate; reserve at 0.0.2; first real release 0.0.3; final publish links to agent-flow and states reclaim-by-issue-on-agent-flow-repo; hyphen/underscore equivalence. All present and accurate (the only extra is the F1 yank line).
- Identity: "structured workflow validator plus advisory driver", scaffold as one bootstrap subcommand, NOT a meta-harness; the sidecar explicitly avoids "engine"/authoritative-driver over-claim. Faithful, no over-claim.
- Status-line arithmetic in `agent-scaffold.md` is internally consistent (78->84 steps via 6 new; open questions 7->5; in-progress 1->2; new `next`=5; deferred net unchanged at 16).
