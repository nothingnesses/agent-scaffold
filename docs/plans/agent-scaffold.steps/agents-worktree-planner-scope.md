### `agents-worktree-planner-scope`: absorbed into `driver-output-generation`, which authors this clarification in the shared `isolation_policy` fragment (`skipped`)

Skipped. Absorbed into `driver-output-generation` (`Q-51`), whose first fix authors this planner-isolation clarification in the shared generated `isolation_policy` fragment, so it propagates into both AGENTS.md and the driver reminder under one byte-guard rather than landing as a standalone hand-edit. The step schema has no `superseded` status (that is question-only), so it is retired as `skipped` with the absorption named here, following the `ledger-parse` / `state-queries` precedent; the driver step also records the link structurally via `folds`. The original clarification scope is preserved below for the record. AGENTS.md's "Worktree lifecycle and merge-back" section is written only around the IMPLEMENTER (code) case, and its phrase "the orchestrator authors the plan and the ledger on main" invites the wrong generalization: it reads as though all plan authoring happens on main. A spawned PLANNER authoring new plan CONTENT (steps, questions, bodies) is worktree-isolated, reviewed, and merged back exactly like the implementer; that is DISTINCT from the orchestrator's own integration-level on-main edits (step-status flips, increment declarations, recording round records, ledger anchors). Clarify the section to draw that line, referencing (not restating) the Writer-isolation rule. This is a `pack/AGENTS.md` prose clarification. No open decision, a known change.
