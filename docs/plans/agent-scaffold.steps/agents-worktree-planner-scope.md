### `agents-worktree-planner-scope`: Clarify the worktree lifecycle covers the planner, not just the implementer

Deferred pack-prose clarification. AGENTS.md's "Worktree lifecycle and merge-back" section is written only around the IMPLEMENTER (code) case, and its phrase "the orchestrator authors the plan and the ledger on main" invites the wrong generalization: it reads as though all plan authoring happens on main. A spawned PLANNER authoring new plan CONTENT (steps, questions, bodies) is worktree-isolated, reviewed, and merged back exactly like the implementer; that is DISTINCT from the orchestrator's own integration-level on-main edits (step-status flips, increment declarations, recording round records, ledger anchors). Clarify the section to draw that line, referencing (not restating) the Writer-isolation rule. This is a `pack/AGENTS.md` prose clarification. No open decision, a known change. Deferred.
