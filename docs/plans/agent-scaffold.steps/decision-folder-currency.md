### `decision-folder-currency`: name the planner as the folder in the remaining passive decision-folding passages (`Q-67`)

Next. Scheduled by the human (2026-07-27) under the already-decided `Q-67`; this step carries no new question of its own (see "No new question and no receipt" below). Step 89 (`planner-folds-decisions`) named the PLANNER as the folder of every non-trivial decided decision at three points in `pack/AGENTS.md`: the human-input contract (line 41), the Socratic mode (line 43), and the checkpoint queue-push rule (line 71). Its review round recorded an accepted-residual follow-up, and a further read while scheduling that follow-up found one more instance of the same sentence: three passages still describe decision-folding in the passive voice, silent on which role authors the fold. None of them contradicts the now-explicit rule; they are incomplete, so an agent that reads only one of them gets the duty without the actor, which is the exact ambiguity `Q-67` was raised to close.

The passages, quoted as of the commit that schedules this step (find them by paragraph, not by line number, if they have since moved):

- `pack/prompts/orchestrator.md:31`, the paragraph beginning "Every time you put a decision to the human, emit a structured block at that gate per the human-input contract": "for a question whose options are already clear, emit the block and record the resolved answer as a durable Open-Questions decision". This is the Socratic-mode instruction in the ORCHESTRATOR'S OWN prompt, and the highest-leverage of the three: the agent that can slip into folding a decision itself is the one reading this document. Its `pack/AGENTS.md:43` counterpart already says the non-trivial fold is "routed to the planner to author"; this copy does not.
- `pack/prompts/orchestrator.md:33`, the paragraph beginning "The ledger is separate from the plan": "only durable decisions, the ones that change the plan, fold into it". This is the passage the step-89 triager named in its accepted-residual note.
- `pack/AGENTS.md:63`, the "Preventing relitigation (the ledger)" paragraph: "only durable decisions, the ones that change the plan, fold into the plan's steps". This is the same sentence as the one above, in the guidance rather than in the prompt, so fixing either one alone would leave the two copies saying different things.

What the implementer changes, hand-authored prose only:

- `pack/prompts/orchestrator.md`, the Socratic-mode paragraph: name the planner as the author of the non-trivial fold and keep the orchestrator's own duty (routing it) explicit, matching what `pack/AGENTS.md:43` already says.
- `pack/prompts/orchestrator.md`, the ledger paragraph: same, on the "only durable decisions ... fold into it" clause.
- `pack/AGENTS.md`, the "Preventing relitigation (the ledger)" paragraph: same, on its closing "only durable decisions ... fold into the plan's steps" clause.

The exact wording is the implementer's to choose. The requirement is that each passage names the actor at its point of use rather than leaving the reader to follow a cross-reference chain, and that the two ledger-paragraph copies (prompt and guidance) end up saying the same thing. Prefer a short clause over a restatement: `pack/AGENTS.md:41` (the human-input contract) stays the authoritative statement of the rule, and these three points reinforce it.

Single source of the closed list (the constraint step 89 was reviewed into, and the reason this step is deliberately narrow). The orchestrator's closed list of direct-on-main integration edits (a step's status flip, an increment declaration, a round record, and the ledger's resume anchor) is NOT hand-authored prose: it is the generated `ISOLATION_POLICY_FRAGMENT` (`src/isolation_policy.rs`), rendered into the `{{isolation_policy}}` slot at `pack/AGENTS.md:91` and protected by a drift guard. Step 89's plan review caught exactly this (finding F1) and dropped the part that would have restated it. This step therefore restates NONE of that list either: the fragment's existing rationale, that those integration edits "author no reviewed product content and so stay the orchestrator's direct job rather than a spawned agent's", already implies that authoring a `[[question]]` or a `[[step]]`, which IS reviewed product content, is the planner's work. Where a passage benefits from the connection, CROSS-REFERENCE the fragment; do not copy from it. Restating a generated single source in hand prose would violate the AGENTS.md workflow guidance "One source of truth" and plan Principle 8 ("Structured data first, project for humans").

Documentation currency (which deployed files this makes stale). `pack/` is the source; `just scaffold-self` regenerates the deployed copies and then runs `nix fmt`. Three deployed files go stale and must be regenerated as part of this step, not afterwards:

- `.agents/prompts/orchestrator.md`, a verbatim copy of `pack/prompts/orchestrator.md` (`src/manifest.rs` lists it among the scaffolded assets). NOTE: unlike the two guidance copies below, this one has NO whole-file drift-guard test, so nothing fails if the regeneration is skipped. The staleness would be silent, which is why it is called out here explicitly.
- `AGENTS.md` (repository root), rendered from `pack/AGENTS.md`.
- `.agents/AGENTS.reference.md`, the reference copy of the same render.

The last two ARE covered by the whole-file drift guard (`agents-md-drift-guard`, step 80, `src/agents_md_drift.rs`), which fails `cargo test` if the committed copies are not a fresh render, so their regeneration is enforced. No other deployed file is affected: this step edits no source, no template, and no other prompt.

Deliberately out of scope, both considered and both excluded on purpose rather than overlooked:

- `pack/prompts/orchestrator.md:27`, the checkpoint paragraph ("update the plan's Open Questions queue and push its open items to the human"). This is the orchestrator-prompt mirror of `pack/AGENTS.md:71`, which step 89 DID sharpen with the "updates this queue means raising and pushing the open items, not authoring a decided decision's fold" clause. It is a live inconsistency between the guidance and the prompt, but it is a queue-push passage, not a decision-folding passage, so folding it in here would widen the scope past what was scheduled (the AGENTS.md workflow guidance "No silent scope expansion"). It is raised to the human as a separate scheduling call rather than absorbed silently.
- `pack/LEDGER.template.md:3`, "Durable decisions do not live here; they fold into the plan." Also passive, but it is a placeholder note describing what the ledger file is FOR (do not record decisions here), not a statement of the folding duty; naming the actor there would add a fourth restatement at a point where the actor is irrelevant to the instruction, and would drag `.agents/LEDGER.template.md` into the regeneration set for no gain (plan Principle 2, "Minimal by default").

No new question and no receipt. This is a scheduling sub-decision under the already-decided `Q-67`, not a new open question, so no `[[question]]` is registered and no `type:"decision"` receipt is owed. The precedent is the pair of human sub-decisions taken under the already-decided `Q-52` (recorded in the ledger as build-plan refinements, NOT new question ids, so no receipt was owed). W4 requires a receipt only for a decided `[[question]]` past the `[meta].w4_baseline` cutoff, and this step creates no question; registering a spurious id would manufacture a receipt obligation that records nothing new. The step's provenance instead points at `Q-67`, whose receipt already exists in `docs/metrics/workflow.jsonl`.

Principle grounding (by name). Naming the actor at each point of use, rather than through a cross-reference chain, serves the AGENTS.md workflow guidance "Make documentation self-contained" and keeps ONE authoritative statement of who authors decided entries that the other points reinforce (plan Principle 8, "Structured data first, project for humans", and its one-source-of-truth thinking applied to the workflow prose). Aligning the prompt with the guidance is plan Principle 1 ("Prefer the cleaner long-term architecture over the smallest diff"): the prompt is what the orchestrator actually reads, so leaving the rule explicit in the guidance and passive in the prompt is the version most likely to be missed. Restating nothing from the generated fragment is the AGENTS.md workflow guidance "One source of truth". Keeping the scope to the three named passages, and raising the two excluded ones rather than absorbing them, is "No silent scope expansion".

Chosen (human, 2026-07-27) over deferring it as a tracked step (rejected: the gap is live in the document the orchestrator reads every session, and the fix is three clauses) and over dropping it as an accepted residual (rejected: it leaves the `Q-67` rule half-stated, which is how the original ambiguity survived long enough to cause a real misstep).
