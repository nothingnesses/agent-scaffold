### `decision-folder-currency`: name the planner as the folder at the remaining actor-ambiguous decision-folding points (`Q-67`)

Next. Scheduled by the human (2026-07-27) under the already-decided `Q-67`; this step carries no new question of its own (see "No new question and no receipt" below). Step 89 (`planner-folds-decisions`) named the PLANNER as the folder of every non-trivial decided decision at three points in `pack/AGENTS.md`: the human-input contract (line 41), the Socratic mode (line 43), and the checkpoint queue-push rule (line 71). Its review round recorded an accepted-residual follow-up, and reads while scheduling that follow-up found two more points, of a related kind but not the same class (see below): FOUR passages are still out of step with the rule, three of them in `pack/prompts/orchestrator.md` and one in `pack/AGENTS.md`. Note where the weight sits: step 89 fixed the GUIDANCE, but three of the four passages left are in the ORCHESTRATOR'S OWN PROMPT, which is the document the agent that can actually slip reads at every session start.

The four fall into TWO CLASSES, and the operation each needs is different, so do not treat them alike:

- ACTOR-LESS (an omission; the fix ADDS the actor). `pack/prompts/orchestrator.md:33` and `pack/AGENTS.md:63` state the folding duty in the passive voice and name no one. Neither contradicts the now-explicit rule; they are incomplete, so an agent reading either gets the duty without the actor. These two, and only these two, are what the step-89 triager recorded in its residual note in `docs/plans/agent-scaffold.ledger.md`, which names "`pack/prompts/orchestrator.md:33` AND the parallel `pack/AGENTS.md:63`" and no others. (Find that note by the quoted text, not by a line number. A line citation into the ledger rots because the file is edited IN PLACE, not only appended to: the resume block sits above this note and grew by ten lines mid-file, which moved the note from 345 to 355 and broke the citation an earlier draft of this sidecar carried.)
- ACTOR NAMED, WRONG ACTOR (a contradiction; the fix CARVES OUT or REASSIGNS). `pack/prompts/orchestrator.md:27` and `:31` are second-person imperatives addressed to the orchestrator, so they do name an actor and it is the one the rule reserves the work FROM. `:31`'s "record the resolved answer as a durable Open-Questions decision" conflicts with `pack/AGENTS.md:43`, which routes the non-trivial fold "to the planner to author"; `:27`'s unqualified "update the plan's Open Questions queue" is the exact verb `pack/AGENTS.md:71` needed an added sentence to qualify. These two were never covered by the step-89 residual, and adding a mention of the planner without narrowing what the orchestrator is told to do would leave the paragraph saying both things at once.

The passages, quoted as of the commit that schedules this step (find them by paragraph, not by line number, if they have since moved):

- `pack/prompts/orchestrator.md:27`, the paragraph beginning "At each checkpoint, sync the durable state before moving on": "There, update the plan's Open Questions queue and push its open items to the human". This is the most load-bearing of the four, because "update the plan's Open Questions queue" is exactly the verb that reads as licence to AUTHOR a fold, and it sits unqualified in the orchestrator's own prompt. Its `pack/AGENTS.md:71` counterpart already carries the step-89 qualifier ("Here 'updates this queue' means raising and pushing the open items, not authoring a decided decision's `[[question]]` or `[[step]]` fold, which is the planner's job"); the prompt has no counterpart to it at all, so the guidance and the prompt currently disagree about what the verb licenses.
- `pack/prompts/orchestrator.md:31`, the paragraph beginning "Every time you put a decision to the human, emit a structured block at that gate per the human-input contract": "for a question whose options are already clear, emit the block and record the resolved answer as a durable Open-Questions decision". This is the Socratic-mode instruction. Its `pack/AGENTS.md:43` counterpart already says the non-trivial fold is "routed to the planner to author"; this copy does not.
- `pack/prompts/orchestrator.md:33`, the paragraph beginning "The ledger is separate from the plan": "only durable decisions, the ones that change the plan, fold into it". Actor-less. One of the two passages the step-89 triager named in its accepted-residual note in `docs/plans/agent-scaffold.ledger.md` (locate it by the quoted text above, not by line number).
- `pack/AGENTS.md:63`, the "Preventing relitigation (the ledger)" paragraph: "only durable decisions, the ones that change the plan, fold into the plan's steps". Actor-less, and the counterpart sentence to the one above, in the guidance rather than in the prompt, so fixing either one alone would leave the two copies disagreeing about the actor. They are counterparts, not duplicates: this one continues "and a folded decision reopens only by evidence that beats its recorded reasoning", which the prompt's copy does not carry.

What the implementer changes, hand-authored prose only:

- `pack/prompts/orchestrator.md`, the checkpoint paragraph (wrong actor, so NARROW it): give it the SAME qualifier `pack/AGENTS.md:71` already carries, that updating the queue means raising and pushing the open items, not authoring the decided decision's `[[question]]` or `[[step]]` fold, which is the planner's job and which the orchestrator routes. This is the unambiguous target for this passage: match the guidance's existing clause, do not invent a different rule.
- `pack/prompts/orchestrator.md`, the Socratic-mode paragraph (wrong actor, so REASSIGN it): make the second branch say the planner authors the non-trivial fold and the orchestrator routes it, matching what `pack/AGENTS.md:43` already says, rather than leaving "record the resolved answer" addressed to the orchestrator. EDIT THE SECOND BRANCH ONLY. This is a single three-branch sentence (branch 1, answer a purely factual question directly; branch 2, the already-clear-options case, which is the target; branch 3, the not-yet-decidable case that tells you to "record it as an `exploring` Open-Questions item"). Branch 3 belongs to the separate exploration-mode class described under "Deliberately out of scope" below, which is held as the `exploring` queue item `Q-69` with a design pass owed and NO step yet. Leave branch 3 exactly as it is, even though the finished sentence will read oddly until `Q-69` is resolved; do not re-litigate it here.
- `pack/prompts/orchestrator.md`, the ledger paragraph (actor-less, so ADD the actor): name the planner on the "only durable decisions ... fold into it" clause, under the same scope as the other three (see SCOPE below).
- `pack/AGENTS.md`, the "Preventing relitigation (the ledger)" paragraph (actor-less, so ADD the actor): same, on its closing "only durable decisions ... fold into the plan's steps" clause, and under that same scope.

The exact wording is the implementer's to choose; the requirements are these.

- SCOPE, governing all four instructions above. Every actor clause added carries the scope the guidance already ships, and invents none: the planner authors the fold when that fold is NON-TRIVIAL, which `pack/AGENTS.md:41` defines inline as "authoring a `[[question]]` or a `[[step]]`". A trivial fold stays with the orchestrator (`pack/AGENTS.md:39`; `pack/prompts/orchestrator.md:29`), so do NOT write an unqualified "the planner authors that fold" into any of the four passages. Copy the shipped qualifier: widening or narrowing it here would settle by the back door a question that is not this step's to settle.
- Each passage names the actor at its point of use, rather than leaving the reader to follow a cross-reference chain.
- The checkpoint paragraph ends up saying what the MAIN clause of `pack/AGENTS.md:71` says, that is the "Here 'updates this queue' means raising and pushing the open items, not authoring a decided decision's `[[question]]` or `[[step]]` fold, which is the planner's job" half, and NOT its trailing rationale clause after the colon. That trailing clause is one side of the live `Q-69` contradiction (see "Deliberately out of scope" below), so reproducing it here would put a second instance of it into `pack/prompts/orchestrator.md`, two paragraphs above the branch-3 sentence it contradicts, and enlarge what that design pass has to fix.
- The two ledger-paragraph copies (prompt and guidance) end up carrying the same ACTOR CLAUSE, not the same sentence. Do not harmonise the rest of the two, and in particular do not drop `pack/AGENTS.md:63`'s "and a folded decision reopens only by evidence that beats its recorded reasoning" tail, which the prompt's counterpart does not have. Only the actor clause is in scope.
- The unguarded deployed copy is verifiably regenerated: after the step, `diff pack/prompts/orchestrator.md .agents/prompts/orchestrator.md` must produce no output. That check is meaningful today and stays meaningful, because no pack prompt contains a `{{...}}` render slot (verified: zero occurrences anywhere under `pack/prompts/`), so each deployed prompt is a byte-identical copy of its source, and because this step forbids the implementer from running the repo-wide formatter. This is the one part of the currency work no test enforces, so it is the part to check by hand.

Prefer a short clause over a restatement: `pack/AGENTS.md:41` (the human-input contract) stays the authoritative statement of the rule, and these four points reinforce it.

Single source of the closed list (the constraint step 89 was reviewed into, and the reason this step is deliberately narrow). The orchestrator's closed list of direct-on-main integration edits (a step's status flip, an increment declaration, a round record, and the ledger's resume anchor) is NOT hand-authored prose: it is the generated `ISOLATION_POLICY_FRAGMENT` (`src/isolation_policy.rs`), rendered into the `{{isolation_policy}}` slot at `pack/AGENTS.md:91` and protected by a drift guard. Step 89's plan review caught exactly this (finding F1) and dropped the part that would have restated it. This step therefore restates NONE of that list either: the fragment's existing rationale, that those integration edits "author no reviewed product content and so stay the orchestrator's direct job rather than a spawned agent's", already implies that authoring a `[[question]]` or a `[[step]]`, which IS reviewed product content, is the planner's work. Where a passage benefits from the connection, CROSS-REFERENCE the fragment; do not copy from it. Restating a generated single source in hand prose would violate the AGENTS.md workflow guidance "One source of truth" and plan Principle 8 ("Structured data first, project for humans"). The checkpoint paragraph is where this bites hardest, because its `pack/AGENTS.md:71` model ends by POINTING at the fragment ("the generated isolation-policy fragment below lists the orchestrator's closed set of direct-on-main edits, which author no reviewed product content, so a `[[question]]` or `[[step]]` (reviewed product content) is not among them"): copy the pointing, not the list. The prompt may reference the rule in `AGENTS.md` rather than reproduce the fragment's contents, and it must not enumerate the four direct-on-main edits itself.

Documentation currency (which deployed files this makes stale). `pack/` is the source. Three deployed files go stale and must be regenerated as part of this step, not afterwards:

- `.agents/prompts/orchestrator.md`, a verbatim copy of `pack/prompts/orchestrator.md` (`src/manifest.rs` lists it among the scaffolded assets). NOTE: unlike the two guidance copies below, this one has NO whole-file drift-guard test, so nothing fails if the regeneration is skipped and the staleness would be silent. Three of this step's four passages live in that file, so it carries most of the change and none of the enforcement; regenerating it is the single easiest thing to forget here.
- `AGENTS.md` (repository root), rendered from `pack/AGENTS.md`.
- `.agents/AGENTS.reference.md`, the reference copy of the same render.

The last two ARE covered by the whole-file drift guard (`agents-md-drift-guard`, step 80, `src/agents_md_drift.rs`), which fails `cargo test` if the committed copies are not a fresh render, so their regeneration is enforced. No other deployed file is affected: this step edits no source, no template, and no other prompt. Closing the unguarded gap on `.agents/prompts/` is scheduled separately as `prompt-drift-guard` (order 92); this step must not wait on it.

HOW to regenerate, and what NOT to run. Run the RENDER HALF only: `cargo run -- scaffold --output-dir . --write --force --principles default --instrument`. Do NOT run `just scaffold-self`: `justfile:46-48` shows that recipe is the render followed by a repo-wide `nix fmt`, and "Format only your own files" (`pack/AGENTS.md:79`) forbids an implementer from running a repo-wide formatter, with `pack/AGENTS.md:108` ruling on this exact case and leaving incidental reformatting to the orchestrator. Skipping the formatter costs nothing here: the drift guard passes both the fresh render and the committed file through `normalize_wrapping` before comparing, so prettier's wrapping is normalised away on both sides, and the guard's module documentation records that the raw render is already byte-identical to both committed files because the pack authors each paragraph on a single line (`src/agents_md_drift.rs`, module docs and `the_committed_scaffold_matches_a_fresh_render`). The raw render therefore satisfies the guard on its own.

Deliberately out of scope, considered and excluded on purpose rather than overlooked:

- THE EXPLORATION-MODE PASSAGES, a distinct class: `pack/AGENTS.md:45`, `pack/prompts/orchestrator.md:31` third branch (the same sentence as this step's second target), and `pack/user-prompts/explore.md:13` (restated at `:3`) all instruct the orchestrator to record an `exploring` Open-Questions item, which IS a `[[question]]` in the structured source. The TRAILING rationale clause of `pack/AGENTS.md:71` says a `[[question]]` or `[[step]]` is not among the orchestrator's direct-on-main edits, dropping the "a decided decision's ... fold" qualifier that the same sentence's main clause carries, so this is a CONTRADICTION in the shipped pack, not an omission like the four passages above, and resolving it is a design choice rather than a wording fix. It is held as the queue item `Q-69`, status `exploring`, with a design pass owed and NO step yet: the resolution turns on premises that are not settled, so the step is authored after the pass and the human's decision rather than before. Depending on which direction that pass lands on, it may also reach a fourth deployed asset, `.agents/user-prompts/explore.md` (`pack/pack.toml:166-167`; `src/manifest.rs:615`). The implementer of THIS step must leave all three passages untouched, in particular branch 3 of the sentence at `pack/prompts/orchestrator.md:31` that it is otherwise editing.
- `pack/LEDGER.template.md:3`, "Durable decisions do not live here; they fold into the plan." Also actor-less, but it is a placeholder note describing what the ledger file is FOR (do not record decisions here), not a statement of the folding duty; naming the actor there would add a restatement at a point where the actor is irrelevant to the instruction, and would drag a fourth deployed asset, `.agents/LEDGER.template.md`, into the regeneration set for no gain (plan Principle 2, "Minimal by default"). The human confirmed this exclusion when they set the step's scope (2026-07-27).

Scope history, so a later reader does not re-litigate it. Two boundary calls were put to the human rather than taken by the planner, and they went opposite ways.

- The checkpoint paragraph (`pack/prompts/orchestrator.md:27`) was first drafted as out of scope, on the ground that it is a queue-push passage rather than a decision-folding one. The human OVERRULED that and pulled it INTO scope (2026-07-27), reasoning that "update the plan's Open Questions queue" is precisely the verb that reads as licence to author a fold, and that its sitting unqualified in the orchestrator's own prompt makes it arguably the most load-bearing passage of the set. The scope is four passages because of this call.
- The exploration-mode class was raised by the plan-review triager (finding T-3a, `medium`, valid but out of scope) and the human kept it OUT of this step (2026-07-27), giving it its own question so the design gets decided rather than assumed. That call was first carried out as a question plus a step; on 2026-07-28 the human demoted the question to `exploring` and removed the step, because its edits depended on a decision not yet made, so the current disposition is `Q-69` with a design pass owed and no step. Do not fold it back in here.

No new question and no receipt. This is a scheduling sub-decision under the already-decided `Q-67`, not a new open question, so no `[[question]]` is registered and no `type:"decision"` receipt is owed. The precedent is the pair of human sub-decisions taken under the already-decided `Q-52` (recorded in the ledger as build-plan refinements, NOT new question ids, so no receipt was owed). W4 requires a receipt only for a decided `[[question]]` past the `[meta].w4_baseline` cutoff, and this step creates no question; registering a spurious id would manufacture a receipt obligation that records nothing new. The step's provenance instead points at `Q-67`, whose receipt already exists in `docs/metrics/workflow.jsonl`.

Principle grounding (by name). Naming the actor at each point of use, rather than through a cross-reference chain, serves the AGENTS.md workflow guidance "Make documentation self-contained" and keeps ONE authoritative statement of who authors decided entries that the other points reinforce (plan Principle 8, "Structured data first, project for humans", and its one-source-of-truth thinking applied to the workflow prose). Aligning the prompt with the guidance is plan Principle 1 ("Prefer the cleaner long-term architecture over the smallest diff"): the prompt is what the orchestrator actually reads, so leaving the rule explicit in the guidance and passive in the prompt is the version most likely to be missed. Restating nothing from the generated fragment is the AGENTS.md workflow guidance "One source of truth". Keeping the scope to the four named passages, and putting both borderline ones to the human rather than absorbing or dropping them on the planner's own judgement, is "No silent scope expansion" applied in both directions.

Chosen (human, 2026-07-27) over deferring it as a tracked step (rejected: the gap is live in the document the orchestrator reads every session, and the fix is four clauses) and over dropping it as an accepted residual (rejected: it leaves the `Q-67` rule half-stated, which is how the original ambiguity survived long enough to cause a real misstep).
