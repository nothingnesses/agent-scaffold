### `driver-isolation-reminder-scope`: widen the `next` driver's isolation reminder to the reviewer / triager-spawn states (`Q-62`)

Widen the `next` driver so its worktree-isolation reminder fires at the reviewer / triager-spawn states, not only the writer states, matching the uniform-isolation rule (Q-61) at the point of action. Concretely, widen `spawns_writer` (or add a sibling predicate) in `src/next.rs` so the isolation reminder attaches at the review-spawn states `AwaitingFirstReview` and `AwaitingReviewers`, and flip the content-pin test `the_reviewer_states_carry_no_isolation_reminder`.

Why: Q-62. The human chose option (a) on 2026-07-23, over (b) keep-writer-only and (c) split-the-reminder. Reasoning: P1 (one uniform rule, no drift between the guidance and the driver) puts the reminder at the point of action for every spawn state now that Q-61 makes every spawned agent isolate.

Cited locations: `src/next.rs` `spawns_writer`, the review states `AwaitingFirstReview` and `AwaitingReviewers`, and the content-pin test `the_reviewer_states_carry_no_isolation_reminder`.

DONE: increment `driver-isolation-reminder-scope-inc1` (low_risk), converged over two work_review rounds (r1 new_valid on a wording-residual cluster the widening left behind, r2 clean; records 189-190). Realized by renaming and widening the predicate `spawns_writer` -> `spawns_isolated_agent` (one predicate matching all four spawn states `ReadyToPlan | AwaitingFixes | AwaitingFirstReview | AwaitingReviewers`, exhaustive match, single call site), generalizing the emitted lead "Writer isolation" -> "Agent isolation" so it reads accurately at planner, implementer, and reviewer/triager spawns, and flipping the content-pin test `the_reviewer_states_carry_no_isolation_reminder` -> `the_reviewer_states_now_carry_the_isolation_reminder` (asserts the fragment IS present at both review states, and the resolve-note present iff tier unknown). Option (c) split-the-reminder was rejected, so the full reminder including the tier echo fires at the review states too; `ISOLATION_POLICY_FRAGMENT` stays single-sourced (not forked), and the golden fixtures hold a `__ISOLATION_FRAGMENT__` placeholder substituted from the shared const so they pin the reviewer-state line without a hand-copied duplicate. Round 1 (reviewers: correctness opus + consistency sonnet; separate triager opus) found 5 valid wording residuals (1 blocking): the emitted `TIER_RESOLVE_NOTE` string plus four stale "writer" doc-comments/test-header; the fix pass generalized all six "writer" mentions to "agent" (no logic change). Serves Make illegal states unrepresentable (one predicate governs every spawn state; a new state is a compile-time decision) and Prefer the cleaner long-term architecture (one uniform rule, no drift between the guidance and the driver). Endorsed-now core item under `generated-projection` (Q-64, Option A refined).
