### `exploring-item-actor-boundary`: who records an `exploring` Open-Questions item (`Q-69`)

Not started, and BLOCKED ON A DECISION rather than on another step: `Q-69` is `open`, so the concrete edits are not yet fixed. The design space, the trade-offs, the recommendation, and the reasoning judged against the plan's Project Principles by name all live in the `Q-69` queue item; this sidecar does not restate them. What follows is the part that is settled regardless of which option the human picks, plus what each option would require, so the step is actionable the moment the decision lands.

The contradiction, in one line. `pack/AGENTS.md:71` says without qualification that "a `[[question]]` or `[[step]]` (reviewed product content) is not among" the orchestrator's closed set of direct-on-main edits, while `pack/AGENTS.md:45`, the third branch of the sentence at `pack/prompts/orchestrator.md:31`, and `pack/user-prompts/explore.md:13` (restated at `:3`) all instruct the orchestrator to record an `exploring` Open-Questions item, which IS a `[[question]]` in the structured source. Live evidence that the boundary is unclear rather than merely untidy: commit `b6ba317` added `Q-68` (`status = "exploring"`) directly to `docs/plans/agent-scaffold.plan.toml` in a plain single-parent commit on main.

How this differs from `decision-folder-currency` (order 90), and why it is a separate step. Step 90 fixes passages that are INCOMPLETE, either actor-less or naming the orchestrator where the rule reserves the work for the planner; every one of its four edits is a wording fix with a known target. These three passages are a CONTRADICTION between two shipped rules, and fixing them requires first deciding which rule is right. That is a human decision under the human-input contract, so it gets its own question and its own step. The step-90 plan-review triager raised this as finding T-3a (`medium`, valid but out of scope), and the human kept it out of step 90 on 2026-07-27.

What each `Q-69` option would require, so the implementer can start immediately once the human chooses:

- If (a), QUALIFY `pack/AGENTS.md:71`. Narrow its closing clause so the exclusion scopes to a DECIDED question's fold, and say that recording an undecided `exploring` placeholder is orchestrator queue management. Consider a matching half-clause at `pack/AGENTS.md:45` so the two read as one rule. The three call sites then need NO change, because they become correct as written. Deployed files to regenerate: `AGENTS.md` and `.agents/AGENTS.reference.md`.
- If (b), ALIGN THE THREE CALL SITES to `:71` as written. Edit `pack/AGENTS.md:45`, the third branch at `pack/prompts/orchestrator.md:31`, and `pack/user-prompts/explore.md:13` (and its restatement at `:3`) so the planner authors the `exploring` placeholder and the orchestrator routes it. `pack/AGENTS.md:71` needs no change. Deployed files to regenerate: `AGENTS.md`, `.agents/AGENTS.reference.md`, `.agents/prompts/orchestrator.md`, and `.agents/user-prompts/explore.md` (`pack/pack.toml:166-167`; `src/manifest.rs:615` lists that dest).
- If (c), DERIVE the boundary from the criterion the generated fragment already states. Rewrite `pack/AGENTS.md:71`'s closing clause to turn on whether the edit authors reviewed product content, rather than on the `[[question]]` or `[[step]]` token, and add a short worked instance naming the `exploring` placeholder as queue management. The three call sites likely need no change; confirm that when the wording is settled. Deployed files: as (a), unless the worked instance is placed in the prompt too.

Constraint that holds under every option: do NOT restate the generated closed list. It is the `ISOLATION_POLICY_FRAGMENT` (`src/isolation_policy.rs`), rendered into the `{{isolation_policy}}` slot at `pack/AGENTS.md:91` and drift-guarded. `pack/AGENTS.md:71` currently POINTS at it, and whatever replaces that sentence must keep pointing rather than enumerate. This is the same constraint step 89's plan review imposed as its finding F1 and step 90 carries; it serves the AGENTS.md workflow guidance "One source of truth" and plan Principle 8 ("Structured data first, project for humans").

Ordering against step 90. This step does not block step 90 and step 90 does not block it. They touch one sentence in common, `pack/prompts/orchestrator.md:31`: step 90 edits its SECOND branch and is instructed to leave the third alone, and this step touches the THIRD branch only, and then only under option (b). If step 90 lands first, as the order implies, this step rebases onto its result; if the human reverses the order, step 90's sidecar still tells its implementer not to touch branch 3, so the boundary holds either way.

Documentation impact. Depends on the option, enumerated per option above. Note that option (b) is the only one that reaches `pack/user-prompts/explore.md`, adding a FOURTH deployed asset to the regeneration set. In every case, regenerate with the render half only (`cargo run -- scaffold --output-dir . --write --force --principles default --instrument`) and leave the repo-wide `nix fmt` to the orchestrator, per "Format only your own files" (`pack/AGENTS.md:79`, with `pack/AGENTS.md:108` ruling on the same case). The whole-file drift guard covers `AGENTS.md` and `.agents/AGENTS.reference.md` but NOT the prompt or user-prompt copies (`src/agents_md_drift.rs`); if `prompt-drift-guard` (order 92) has landed by then, that gap is closed for `.agents/prompts/` but not for `.agents/user-prompts/`.

No receipt yet. `Q-69` is `open`, not `decided`, so no `type:"decision"` receipt is owed; W4 requires one only for a decided item past the `[meta].w4_baseline` cutoff. A receipt becomes due at the moment the human decides and the item flips to `decided`, and this step is where it is folded.
