### `formatter-reflow-wording-polish`: polish the formatter-reflow-convention pack wording

The `formatter-reflow-convention` pack text (in `pack/AGENTS.md`) reconciles one clause of the "Format only your own files" file-safety rule (that a writer does not proactively run repo-wide formatters). It could optionally also name the sibling clause, that incidental reformatting is left to the orchestrator, so the reconciliation covers both clauses explicitly rather than one.

Low priority wording polish; no behaviour change.
