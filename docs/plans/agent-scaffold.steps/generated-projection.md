### `generated-projection`: demote AGENTS.md to a slot-wise generated projection, build the Option-A endorsed core now, gate the contested margin (`Q-64`)

Umbrella step for the Q-64 decision (human, 2026-07-23): Option A refined. Adopt the generated-projection direction SLOT-WISE, not whole-file. AGENTS.md stays a hand-authored template with a growing set of generated slots single-sourced with the tool; whole-file generation is rejected because roughly 95% of the file is authored prose (judgment-eliciting guidance, unobservable behavioral-compliance rules, the self-bootstrap stub) that would be forced through a structured source for no marginal drift-safety over slots (Q-64-synthesis sec 1, sec 5.2). The irreducible authored floor is small (about 5-10% of the file) and stable, and single-sourcing the un-duplicated floor removes zero drift, so only genuinely-duplicated slices are generated.

The ENDORSED-NOW core is the set all four lenses endorse; build it now:

- (a) The whole-file AGENTS.md drift guard (step `agents-md-drift-guard`, order 80): a byte-equality assertion that the committed root AGENTS.md equals a fresh `build_assets` render, the `render --check` equivalent for AGENTS.md. Cheapest, highest-value, independent of any slot-widening.
- (b) Principle-by-name projection (step `principle-by-name-projection`, order 81): generalize the driver's escalate-only `projected_principle_reminder` to a phase->principle-names map projected BY NAME, replacing fragile numeric citations and killing the numbering-bug class at its root.
- (c) The roles / findings-naming generated slots (step `roles-findings-naming-slots`, order 82): add generated `{{roles}}` and `{{findings_naming}}` AGENTS.md slots single-sourced with the values the driver already emits, killing the existing `next.rs` duplicates.
- The two PROMOTED steps that are already-decided Option-A items: `single-source-recommendation-rule` (order 76, Q-60 recommendation-in-options rule as a rendered single-source fragment) and `driver-isolation-reminder-scope` (order 77, the Q-62 isolation-reminder widening, delivery item D5). Both move from `deferred` to `next` here; they remain their own steps, now in-scope.

The GATED contested margin is `workflow-toml-rule-fragments` (order 83, `deferred`): widening `workflow.toml` to carry per-phase RULE fragments and generating the `{{workflow_phases}}` phase-machine section. This is the margin the skeptic flagged (the workflow protocol changed this week via Q-63, so the meta-stability precondition is not yet met). It un-gates only when ALL of the sec-2(d) signal holds: (i) doc-vs-mechanism drift RECURS at a material rate AFTER the cheap single-sourcing ships (incidents counted in `docs/metrics/workflow.jsonl` / the ledger that ESCAPED review, above a small N/month); (ii) a multi-week stretch with no structural workflow change; (iii) the AGENTS.md-section-from-spec generator PROVEN on one small slice to produce correct readable guidance.

Judged against the plan principles: Structured data first, project for humans and Prefer the cleaner long-term architecture over the smallest diff drive the projection direction; Make illegal states unrepresentable is served by the drift guard and by projecting principles by name; Ground decisions in evidence and Minimal by default are honored by holding the still-molten logic-generation behind the gate rather than shipping it under the same breath as the proven wins.

Relationship to `workflow-driver` (Q-51, decided, order 52): this generation work is Stage-0b generalized (generate the AGENTS.md control fragment from the spec, byte-guarded) and the delivery increment is Stage-1; it advances the already-settled staged driver target and consumes Q-58's structured transient only at the Stage-2 boundary. Reference `workflow-driver`; do not reopen it.

DONE (umbrella closed 2026-07-24, human-decided at checkpoint): the entire ENDORSED-NOW rung-1 core is built and merged, each as its own independently reviewed and converged child step: `agents-md-drift-guard` (80, the whole-file normalized drift guard), `principle-by-name-projection` (81, the phase->principle-names map projected by name), `driver-isolation-reminder-scope` (77, the widened isolation reminder), `single-source-recommendation-rule` (76, the `{{recommendation_rule}}` slot), and `roles-findings-naming-slots` (82, the `{{findings_naming}}` slot; its `{{roles}}` half dropped after a planner pass found no genuine duplicate). Four single-source fragments now exist as the proven pattern for any future slot (`ISOLATION_POLICY_FRAGMENT`, `RECOMMENDATION_RULE_FRAGMENT`, `WorkflowSpec::control_fragment`, `findings_naming`). This umbrella carries a step-level `review-skipped` waiver (`generated-projection-w1`, self-declared) because it has no increment of its own: all its reviewable work was delivered and converged in the child steps above, each with its own round records, so the umbrella needs no separate review round. The GATED contested margin `workflow-toml-rule-fragments` (83) remains a SEPARATE `deferred` step behind its evidence gate; closing this umbrella does not un-gate it. Serves Prefer the cleaner long-term architecture (the endorsed core shipped, the molten margin held back) and Ground decisions in evidence.
