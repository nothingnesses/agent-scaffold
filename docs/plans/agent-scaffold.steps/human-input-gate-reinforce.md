### `human-input-gate-reinforce`: reinforce the human-input contract at the orchestrator's point of action (`Q-54`)

Not started. Decided (`Q-54`, human, 2026-07-19). The human-input contract (present the viable options with their trade-offs, an explicit recommendation, and reasoning judged against the numbered Project Principles, then let the human decide) is stated once in `pack/AGENTS.md`, but the orchestrator drifted off it after a context compaction: it presented several decision gates as options-with-trade-offs while omitting the recommendation and the Principle-judged reasoning. This is control-drift, an agent forgetting a rule that applies right at the point of action, not a missing rule, so the fix reinforces the rule where it is applied rather than restating it.

Part (a), this step: strengthen `pack/prompts/orchestrator.md` so the human-input contract is an explicit per-gate checklist the orchestrator runs at EVERY decision presentation (kickoff intake, escalation, Socratic answers, and every options gate), naming the required parts (options, trade-offs, a recommendation, Principle-judged reasoning) and pointing to the canonical contract in `AGENTS.md` rather than restating it (Principle 8, one source of truth). Built first, as its own low-risk pack-prompt step, because it is the direct recurrence-preventer and should be in place before the next decision gate. Scope is `pack/prompts/orchestrator.md` (and its regenerated self-scaffold copy); it adds no new canonical rule, only a point-of-use checklist referencing the existing contract.

Part (b), folded into `workflow-driver` Stage 1: the advisory `agent-scaffold next` output, when the state is "awaiting a human decision", surfaces the human-input-contract reminder among its phase-keyed reminders. This is exactly the point-of-action anti-drift value the driver is designed to deliver, so this incident is live evidence for Stage 1 (Principle 6, evidence first). Recorded in the `workflow-driver` sidecar's Stage 1 bullet; not built here.

Chosen over: folding the whole fix into Stage 1 (would delay the recurrence-preventer until the driver stage lands); a note-only capture (leans on the AGENTS.md contract that just failed); and a deterministic check (the presence of a recommendation in a prose gate is not reliably machine-checkable). Acceptance: the orchestrator prompt states the per-gate checklist and references the AGENTS.md contract without duplicating it; the self-scaffold regenerates consistently; no canonical rule is duplicated (Principle 8).
