### `planner-folds-decisions`: name the planner as the folder of every non-trivial decided decision (`Q-67`)

Not started. Decided (`Q-67`, human, 2026-07-26). Fix the actor ambiguity that let the orchestrator start to FOLD a decision itself. AGENTS.md names the planner as the decision-folder only on the request-interrupt path (the "non-trivial ... routes to the planner to fold into the plan" line): the human-input-contract paragraph uses actor-less passive voice ("a resolved decision is recorded ... and folded into the step it affects"), the Socratic-mode paragraph only says the mode "reuses the intake and Open-Questions machinery", and the checkpoint rule ("the orchestrator updates this queue") blurs the boundary that reserves the direct-on-main edits to the orchestrator.

What the implementer changes, all in `pack/AGENTS.md`:

- At the human-input-contract, Socratic-mode, and checkpoint points, make explicit that folding a non-trivial decided decision into the plan (authoring a `[[question]]` or a `[[step]]`) is the PLANNER's job, routed by the orchestrator, not the orchestrator's own direct edit.
- Reinforce that the orchestrator's closed list of direct-on-main integration edits (a step's status flip, an increment declaration, a round record, and the ledger's resume anchor) EXCLUDES authoring new questions or steps, so "the orchestrator updates this queue" at a checkpoint means surfacing and pushing the open items, not authoring the decided entries.

Documentation currency: `just scaffold-self` re-renders the root `AGENTS.md` and `.agents/AGENTS.reference.md` from the edited `pack/AGENTS.md`, then runs `nix fmt`; the whole-file drift guard (`agents-md-drift-guard`, step 80) fails if the committed copies are not a fresh render, so the regeneration is required. This step edits no prompt file and no source: it is guidance-only. It does not change the closed list's membership; it only makes the excluded case (authoring decided entries) explicit where the actor was previously unnamed.

Principle grounding (by name). Naming the actor at each point of use, rather than through a cross-reference chain to the request-interrupt path, serves the AGENTS.md workflow guidance "Make documentation self-contained" and keeps ONE authoritative statement of who authors decided entries (plan Principle 8, "Structured data first, project for humans", and its one-source-of-truth thinking, applied to the workflow prose). Keeping the who-may-edit-what boundary crisp is the AGENTS.md workflow guidance "No silent scope expansion" and plan Principle 1 ("Prefer the cleaner long-term architecture over the smallest diff"): a crisp actor boundary is the cleaner long-term rule and is what prevents the recurrence this decision was prompted by.

Chosen (the Q-67 receipt bundles both Q-66 and Q-67 into this one planner pass) over doing Q-66 only and tracking the ambiguity separately (rejected: leaves the live actor-boundary bug unfixed) and over capturing just the diagnosis for now (rejected: the fix is small, and the ambiguity already caused a real misstep). Decision receipt `type:"decision"` `q_id:"Q-67"` in `docs/metrics/workflow.jsonl`.
