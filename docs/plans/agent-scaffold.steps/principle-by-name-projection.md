### `principle-by-name-projection`: project per-phase principles BY NAME via a phase map (`Q-64`)

Generalize the driver's escalate-only principle projection to a phase->principle-names map, so more than one state can surface its applicable Project Principle(s) BY NAME, projected live from the plan's single `[[principle]]` source (Q-64-delivery-fsm D3; the checkable-versus-promptable partition).

Grounding note found during the build: the driver's emitted reminders were ALREADY de-numbered by an earlier increment (`driver-output-generation-inc2`); no fragile hardcoded "Principle N" citation remained to drop. The escalate reminder already projected one principle by NAME with the plan's own `n` as a source-projected locator (not a fragile citation). So this step is the MECHANISM generalization, not a de-numbering: `phase_principle_names(state) -> &[names]` plus `projected_principle_reminders(state, principles) -> Vec<String>` (by-name lookup, graceful degrade), consumed unconditionally at the call site (order: base reminders -> projected principles -> isolation fragment). Serves Make illegal states unrepresentable (lookup by NAME is immune to the two disagreeing numbered principle lists; a mapped name absent from the plan degrades to nothing rather than a dangling reference) and Structured data first, project for humans.

Map populated HONESTLY, only where a principle genuinely fits: `ReadyToPlan` and `Escalate` both -> "Ground decisions in evidence" (the planner drafting a recommendation, and the human's cap-reached decision); the other seven states map to nothing rather than being forced a principle (Minimal by default, Ground decisions in evidence). The map is list-valued so the Q-64 delivery work can add per-phase principles later. `phase_principle_names` matches all nine `LoopState` variants with no wildcard, so a new state is a compile-time decision.

DONE: increment `principle-by-name-projection-inc1` (risky), converged over two clean work_review rounds (r1 adversarial zero + code-quality CQ-1 Info, r2 clean; records 187-188). Residual CQ-1 (the emitted noun "recommendation" is hardcoded in the now-generic projection; fits both mapped states today, only a latent wording issue if a future non-recommendation phase is mapped) accepted in place, NOT a backlog step: the Q-64 delivery work revisits this exact function and would generalize the noun when a non-recommendation phase is actually mapped (Principle 6, Principle 2). Endorsed-now core item under `generated-projection` (Q-64, Option A refined).
