### `rename-to-agent-flow`: rename the crate and binary from agent-scaffold to `agent-flow` at a stable release (`Q-65`)

Rename the project from `agent-scaffold` to `agent-flow` (descriptive, not a metaphor), timed to a release-ready stable state so the name can be claimed on crates.io. The identity has crossed from a scaffolding tool to a structured workflow VALIDATOR plus advisory DRIVER, with `scaffold` as one bootstrap subcommand; the name should stop mis-selling a one-shot generator (Structured data first, project for humans; Prefer the cleaner long-term architecture over the smallest diff). It is NOT a meta-harness: agents INVOKE the tool and it never runs the LLM loop, so "meta-harness" is a category error. Avoid "engine", which implies the unbuilt authoritative driver and would invite name-driven scope creep against Minimal by default.

DEFERRED because it is release-timed: land it at a stable release-ready state, not speculatively.

crates.io RELEASE CHECKLIST (durable notes):

- crates.io has NO in-place rename. Publish a NEW crate `agent-flow`: set the `Cargo.toml` `name`, the binary becomes `agent-flow`, the `scaffold` subcommand stays. Update all in-repo command examples and docs that say `agent-scaffold`.
- Keep the version line continuous: reserve `agent-flow` on crates.io at 0.0.2, with the first REAL release under `agent-flow` at 0.0.3.
- The final `agent-scaffold` publish (0.0.2) README must link to `agent-flow` AND state that the `agent-scaffold` name is free for whoever wants to reclaim it, contact by opening an issue on the `agent-flow` GitHub repo. Leave the old `agent-scaffold` versions un-yanked so the name stays reclaimable. THIS BULLET IS DONE AND ITS CONTACT ROUTE RESOLVED TO A DIFFERENT REPOSITORY THAN IT NAMES; the paragraph below says what shipped and what this step must preserve.
- crates.io treats `-` and `_` as the same name, so publishing `agent-flow` also reserves `agent_flow`.

WHAT `v0.0.2` SHIPPED, AND THE ONE CONSTRAINT THIS STEP INHERITS FROM IT. Published 2026-08-14, so the checklist's first three bullets are settled facts rather than instructions. The contact-route bullet said to open an issue on the "`agent-flow` GitHub repo", and no repository carries that name until this step runs. `ship-v0-0-2` settled it by naming the CURRENT repository instead: the published README says "To ask for it, open an issue on <https://github.com/nothingnesses/agent-scaffold>, the repository that will carry the `agent-flow` rename", and it links <https://crates.io/crates/agent-flow> for the crate.

THE CONSTRAINT IS THAT THE URL IS NOW UNEDITABLE WHERE IT COUNTS. `Cargo.toml` declares `readme = "README.md"`, so that sentence is inside the published 0.0.2 artifact, crates.io renders it as the crate's front page, and a published version cannot be revised. THIS STEP MUST THEREFORE LEAVE `https://github.com/nothingnesses/agent-scaffold` REACHING THE PROJECT'S ISSUE TRACKER. Check that URL after the rename rather than before it, and if the rename breaks it, that is a decision for the human and not a reason to edit a published artifact.

THE crates.io STATE, MEASURED AGAINST THE SPARSE INDEX ON 2026-08-14, because `crates.io/crates/<name>` over `curl` returns 404 for every crate and proves nothing: `agent-flow` carries 0.0.2 with `yanked: false`, and `agent-scaffold` carries 0.0.1 and 0.0.2, both with `yanked: false`. So the reservation is done, the old name stays installable and reclaimable, and the first real `agent-flow` release starts at 0.0.3 as the checklist says.

Folded from Q-65 (rename decision) and Q-64 (the identity conclusion that determines the name). Provenance: Q-65, Q-64.

### Outcome

WRITTEN AFTER THE RENAME MERGED, at `cd97c7e` on 2026-08-15, and it states what the step DID. What is above it is the checklist the step was given plus the constraint `ship-v0-0-2` handed it, and where those and this section disagree, this section is the record. It adds no work. Every fact below was re-measured by the closing pass against the tree and the merge rather than accepted from the writer, and where a measurement corrected the account the step was given, the correction is marked as one.

NO REVIEW LOOP RAN, BY DECISION, AND THE STEP CARRIES A WAIVER RATHER THAN ROUND RECORDS. `Q-65-runshape` (human, 2026-08-14, `type:"decision"` receipt in `docs/metrics/workflow.jsonl`) chose to judge the step by its deterministic oracle. The plan records that as the step-unit `review-skipped` `self-declared` waiver `rename-to-agent-flow-w1`, whose note states what the oracle proved and what it cannot reach. The log carries no `type:"escalation"` record for this step, because no cap was hit, so the waiver claims the self-declared tier that `WaiverReason::required_tier` (`src/metrics.rs`) requires of `review-skipped` and no stronger one.

WHAT WAS RENAMED, AND WHAT WAS KEPT. Renamed: the crate and binary name, every in-repo command example, the pack and its regenerated `.agents/` copies, the pre-commit hook and its `command -v` probe, the `checks` runner's worktree prefix and its staged-snapshot git identity, the render banner and its golden fixture, the test-harness binary references, and the docs prose. Kept: the 214 plan-named paths under `docs/plans/agent-scaffold.*`, measured with `git ls-files 'docs/plans/agent-scaffold*' | wc -l`, and that number is also the count of every tracked path in the repository whose name carries the old string, so the plan-named set and the kept set are the same set. They keep their names because they are this project's own dogfooded plan, its sidecars, its ledger and its review records, named for what the project was called when they were written, and renaming them would break every reference into them for no gain. Dated records keep the name they recorded for the same reason: the ledger, the explorations, the step sidecars, the build plans, the metrics log, and the CHANGELOG entries for 0.0.1 and 0.0.2.

THE VERSION IS 0.0.3 AND IT IS NOT PUBLISHED. 0.0.3 rather than 0.0.2 because `agent-flow` 0.0.2 is the name reservation the `Q-65` checklist called for, which `ship-v0-0-2` published on 2026-08-14. Measured against the crates.io sparse index at `https://index.crates.io/ag/en/agent-flow` on 2026-08-15, `agent-flow` carries 0.0.2 and nothing else, so 0.0.3 exists in this repository only. Publishing is reserved to the human. The writer records `cargo publish --dry-run` as its release check and states that publishing was not part of the change; the closing pass did NOT re-run that command, because publishing is the human's and no dry-run distinction is worth risking, so treat the dry-run as the writer's claim rather than as re-measured here.

THE CONSTRAINT INHERITED FROM `ship-v0-0-2` HELD, AND IT WAS CHECKED AFTER THE RENAME RATHER THAN BEFORE IT. The 0.0.2 README, which is inside a published artifact and cannot be revised, sends anyone asking to reclaim the `agent-scaffold` name to <https://github.com/nothingnesses/agent-scaffold>. Measured on 2026-08-15: that URL returns 200 and its `/issues` returns 200, while <https://github.com/nothingnesses/agent-flow> returns 404. Nothing in this repository's configuration touches that route, and two things that could have were deliberately left alone. The git remote still names `git@github.com:nothingnesses/agent-scaffold.git`, and `Cargo.toml` still declares `repository = "https://github.com/nothingnesses/agent-scaffold"`. Pointing either at `agent-flow` today would ship a dead link in the new crate's own metadata, since that name 404s, whereas the old URL resolves now. IF THE GITHUB REPOSITORY IS EVER RENAMED, the old URL is expected to keep resolving through GitHub's rename redirect, which is what would preserve the published 0.0.2 promise. THAT REDIRECT IS UNTESTED HERE and is the one part of this paragraph that is not a measurement, so whoever renames the repository must re-check both URLs afterwards.

THE CRATE DESCRIPTION MOVED AND THE CLI `about` STRING DID NOT, AND THE SECOND HALF IS THE PART THAT NEEDS A REASON. `Cargo.toml`'s `description` was "Scaffold the agent workflow into a project.", the one-shot-generator framing the rename exists to stop, and is now "Validate a structured agent workflow, project its state, and scaffold one to start.". Two other copies of that sentence were aligned to it in the same pass, `flake.nix`'s `description`, which is user-visible through `nix flake metadata` and `nix flake show`, and the `src/main.rs` module doc. The CLI `about` at `src/main.rs` still reads "Scaffold the agent workflow into a project, and validate or project its state.", and the `scaffold` subcommand's own help is untouched, which is correct there because scaffolding is what that subcommand does.

CORRECTION, ON THE GROUND FOR LEAVING THE `about` STRING ALONE. The reason recorded on the change is that a triager-blessed off-cycle fix set its current wording to stop it over-promising, so re-wording it would re-open a decided item. The decided item is real and the wording is not. The ledger's `state-schema` increment 1 record carries finding `F-1`: the `about` string advertised "validate or project its state" before those verbs existed, so `--help` named commands a user could not run, and the off-cycle fix at `dd4b1bf` reworded it to "Scaffold the agent workflow into a project.". The longer sentence that ships today was restored later, at `fdd3774` in increment 3, once `validate` and `status` existed. So what `F-1` decided is the RULE, that the `about` string names only capability that exists, and today's wording is that rule applied after the verbs shipped rather than the sentence the triager blessed. The conclusion is unchanged and the step was right to leave it: the string already names both halves, it violates no rule, and moving it would change `--help` in a release whose notes tell a 0.0.2 user that nothing else changes. A later reader must not go looking for a triage record that blessed this exact sentence, because there is none.

ONE REAL CONSEQUENCE FOR A 0.0.2 USER, AND IT IS THE ONLY BEHAVIOURAL CHANGE THE RENAME CARRIES. The `checks` runner names its throwaway worktree from `RUNNER_PREFIX` in `src/checks.rs`, now `agent-flow-checks-run-` where 0.0.2 used `agent-scaffold-checks-run-`, and the startup prune matches that same constant. `prune_orphan_worktrees` therefore cannot reclaim an orphan worktree that a killed 0.0.2 run left behind, because the prune and the orphan no longer share a prefix. The CHANGELOG's 0.0.3 entry states this and carries the recovery: `git worktree prune`, or `git worktree remove` while the directory still exists. The same commit renamed the staged-snapshot git identity that `isolation_commit` sets, which appears only on the throwaway snapshot commit inside the isolated worktree.

ROUTED OUT OF THIS STEP, FOUND DURING THE RELEASE WORK AND DELIBERATELY NOT FIXED HERE. The `audit` name now denotes two things in one product, which is `Q-77` in the queue. The two shipped-guidance gaps the audit prompt left are `user-prompts-pointer-and-drift-coverage`. Neither belongs to the rename, and both are recorded where they are owned rather than here.
