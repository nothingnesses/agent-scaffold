### `roles-findings-naming-slots`: generated `{{findings_naming}}` AGENTS.md slot, killing the `next.rs` duplicate (`Q-64`)

Add a generated `{{findings_naming}}` slot to the AGENTS.md template, single-sourced with the findings-file path values the `next` driver emits, so the findings-file naming convention lives in one place and is projected into both the AGENTS.md slot and the driver's runtime path construction. This kills the second, drift-prone encoding of the convention in `src/next.rs` `build_context` (the `review_findings` / `triage_findings` `format!` calls). A read-only planner design pass (`docs/plans/roles-findings-naming-slots.build-plan.md`) validated the step against the code and corrected its scope; the human decided its two forks 2026-07-24.

Scope correction from the design pass (grounded in `file:line` evidence in the build-plan): the step was chartered for TWO slots, but only one is real.

- `{{roles}}` is DROPPED (human-confirmed): there is no genuine duplicated roles fragment. `src/next.rs` `LoopState::role()` is a structural FSM-state -> actor-identifier map (`planner`/`reviewer`/`implementer`/`orchestrator`), not a copy of the `pack/AGENTS.md` role bullets, and the two sets are not even congruent (no `triager` actor; `orchestrator` added as fallback). A `{{roles}}` slot would fabricate a single-source relationship that does not exist (Ground decisions in evidence, Minimal by default). Not a deferral: if single-sourcing the role vocabulary is ever wanted it is a fresh, smaller question with its own evidence. The stale sidecar line number ("roles duplicate around `:825`") did not hold; current `:825` is a doc-comment parenthetical.
- `{{findings_naming}}` is the real, buildable slice. The `pack/AGENTS.md` "Findings files" convention sentence (`<step>-<role>-<disambiguator>.md`, `<step>-triage.md`, `<step>-triage-recheck.md` under `docs/plans/<task>.reviews/`) is a genuine single source that the whole prompt surface already defers to by pointer; `src/next.rs` is the LONE re-encoder. Honest framing: the two encodings have NOT diverged in value today (the driver's literal `reviewer` equals the convention's `<role>` resolved for a reviewer; the missing recheck path is an un-modelled driver state, not a conflict), so the justification is duplicate-removal / drift-prevention, NOT a bug fix.

Mechanism DECIDED = Option B (human, 2026-07-24), the true single source, over Option A (a verbatim convention fragment plus a consistency test that would leave the driver's `format!` in place and guard two copies). A new `src/findings_naming.rs` holds the path FORMAT as named-token templates (the one source), consumed two ways: builder helpers the driver calls to produce filled paths (fill `task`/`step`, leave `<disambiguator>`), replacing the `build_context` `format!` calls; and a `convention_fragment()` that renders the SAME templates all-tokens-unfilled into the `{{findings_naming}}` slot, byte-guarded against the committed scaffold. This differs from the verbatim `ISOLATION_POLICY_FRAGMENT` / `RECOMMENDATION_RULE_FRAGMENT` pattern because the driver's value bears runtime tokens (`task`/`step`), and from `control_fragment` because those tokens are unknown at scaffold time; the source must render both filled (driver) and unfilled (human convention). Chosen because the step exists to KILL the duplicate, not guard it (Prefer the cleaner long-term architecture, Structured data first, project for humans); B's extra cost over A is small and bounded (Minimal by default not badly served); Option C's typed enum was rejected as over-built for one caller and three fixed kinds (Minimal by default, Ground decisions in evidence).

Risk class: low_risk (prose plus advisory driver path-formatting; no security/data/money sensitivity; reversible; byte-guard tested so a mistake fails loudly). YAGNI (from the build-plan): do NOT build `{{roles}}`; do NOT add a driver recheck state to model `<step>-triage-recheck.md`; do NOT fold `<step>-checks.md` (checks module) into the slot; do NOT generate the whole "Findings files" paragraph, only the one convention sentence; do NOT fill `<disambiguator>` in the driver; do NOT rewrite the prompts' existing pointer references.

Endorsed-now core item under `generated-projection` (Q-64, Option A refined); the LAST rung-1 core step, after which `generated-projection` (79) can close.

DONE: increment `roles-findings-naming-slots-inc1` (low_risk), converged over one clean work_review round (record 194; both reviewers zero findings, no triage needed). Built Option B: new `src/findings_naming.rs` holds the path FORMAT as angle-bracket-token templates (`DIR_TEMPLATE`, `REVIEWER_BASENAME`, `TRIAGE_BASENAME`, `TRIAGE_RECHECK_BASENAME`), consumed two ways: driver builders `review_findings_path`/`triage_findings_path` (replacing the `build_context` `format!` calls, filling `<task>`/`<step>`, setting `<role>` to `reviewer`, leaving `<disambiguator>`) and `convention_fragment()` rendered into the new `{{findings_naming}}` slot; registered in `RESERVED_VARS` + `build_assets`, byte-guarded, plus a unit test pinning the builders' byte-equivalence with the old driver output. The driver's `next` output is unchanged (the `next.rs` golden fixtures pass unchanged, proving byte-fidelity), and the committed `AGENTS.md`/reference are byte-identical (the fragment reproduces the prior sentence verbatim), so the single-sourcing is invisible in the output yet real: the shape now lives in one place. Reviewers confirmed the driver fidelity, genuine single-sourcing, `str::replace` correctness (no token collision), YAGNI adherence, paragraph coherence, and ASCII cleanliness. Accepted residual (per build-plan section 6 YAGNI, NOT a finding): the directory string `docs/plans/<task>.reviews/` appears both in `DIR_TEMPLATE` (the driver's single source) and in the hand-authored AGENTS.md prose preceding the slot, because only the one convention sentence was single-sourced, not the whole "Findings files" paragraph. Serves Prefer the cleaner long-term architecture, Structured data first, project for humans, and Reproducible. Note: the review round's findings files were not committed (the harness blocked reviewer A's worktree write; reviewer B's stayed in its worktree); the round verdicts are recorded in metrics record 194 and here.
