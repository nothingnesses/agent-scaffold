### `round-log-core`: Unify round data on the JSONL round log (`Q-34`)

Next. Decided (`Q-34`, human, after a two-explorer design pass synthesised by the orchestrator). Structured per-round data was DUPLICATED across the ledger's `## Round summaries` table (stale at 2 rows) and the metrics JSONL (comprehensive, richer, maintained), violating one-source-of-truth. Make the JSONL (`docs/metrics/workflow.jsonl`) the single STRUCTURED round store; the ledger keeps only its Round-records NARRATIVE + RESUME STATE. LOGGING STAYS OPT-IN (`--instrument` unchanged; the human's original decision holds, and the explorers' always-on recommendation was declined): the workflow's CORE convergence accounting reads the always-present ledger NARRATIVE (the orchestrator records each round's outcome and running streak in prose and recomputes from it, which is what it already does and which survives compaction), and the JSONL is the opt-in structured superset that MECHANICAL tooling reads (`workflow-invariants` enforcement, calibration, `state-queries`); those mechanical features are therefore opt-in-logging features. Also fold in the plan/ledger DE-DUP discipline: the ledger references the plan for status and decisions rather than restating them, and RESUME STATE is a pointer plus the non-plan-derivable transient in-flight state, not a restatement of the Roadmap. Build in reviewed increments: (A) make `risk_class` REQUIRED on `round` records in `src/metrics.rs` and BACKFILL it into the ~46 historical JSONL records (grounded in the ledger's recorded classifications), updating fixtures and the drift guard; (B) rewrite the ledger template (`pack/LEDGER.template.md`: remove the `## Round summaries` and `## Findings` tables and the `Artifact classification` line; keep Round records + RESUME STATE; add the ownership/de-dup note) and the convergence-accounting prose in `pack/AGENTS.md` + `pack/prompts/orchestrator.md` (count from the narrative; when instrumented, also append the JSONL record; state the de-dup ownership rule); (C, orchestrator-owned) slim the live `docs/plans/agent-scaffold.ledger.md` (remove the stale tables, slim RESUME STATE) and this plan. Reopened decisions, all AMENDED not reversed: `Q-1` convergence counting source (ledger table -> ledger narrative; the cap/partition substance stands), `Q-2`/`Q-3` ledger placement stands with trimmed content, `Q-24` validation stands (now over the single structured store). `Q-4`/`Q-5`/`Q-6` (`instrument-flag`) are UNTOUCHED because opt-in is preserved. The `trivial`-completion mechanism and the grandfather boundary for pre-logging complete steps are deferred to `workflow-invariants`. Non-goals (YAGNI): dropping `--instrument`, renaming the JSONL path, `src/ledger.rs`, `src/table.rs`, a generated round table, a new log record type.

Outcome (complete; three increments). Increment A (`risk_class` required in `src/metrics.rs` + backfill into the 46 historical JSONL records, 40 low-risk / 6 risky grounded in the ledger; merged to main at `fc7f2b6`): built by an isolated implementer in worktree `impl/round-log-core`, converged low-risk in two review-in-worktree rounds (round 1 fixed the `instrument.md` optional-vs-required mismatch both reviewers flagged; round 2 clean; no misclassification confirmed). Increment B (the core reshape, merged at `1fada25`): removed the ledger template's `## Round summaries` + `## Findings` tables and the `Artifact classification` line; rewrote convergence accounting in `pack/AGENTS.md` + `pack/prompts/orchestrator.md` so the orchestrator counts the streak and total-round count from the always-present ledger NARRATIVE (Q-1 substance unchanged, only the source moved from the table); framed the JSONL as the opt-in structured superset the orchestrator ALSO appends when instrumented (opt-in preserved throughout, the explorers' always-on recommendation was declined by the human); and folded in the plan/ledger DE-DUP rule (the plan owns step status via the Roadmap and decisions via Open Questions; the ledger references the plan; RESUME STATE is a pointer plus transient state, not a Roadmap restatement). Classified RISKY (rewrites the core convergence rule) and converged in two consecutive clean rounds after the round-1 fixes (the high finding was the template's own RESUME STATE comment still telling authors to restate status). Increment C (orchestrator-owned): slimmed the live `docs/plans/agent-scaffold.ledger.md` (removed the two stale tables, rewrote the RESUME STATE to a pointer + transient per the de-dup rule, dropped the pack-rule restatements) and cleaned the live guidance in this plan (the `workflow-calibration` collection method). NOTE: several COMPLETE steps' historical Outcome/scope narratives (`convergence-accounting`, `state-schema`, `instrument-flag`) still mention the ledger round-summary table; those are frozen historical records of what each step did at the time and are superseded by this migration (counting is now from the narrative; structured round data lives only in the round log). 96 tests, clippy clean, scaffold byte-identical, `validate` clean throughout. Rounds logged to `docs/metrics/workflow.jsonl` (records 53-57). Deferred to `workflow-invariants`: the `trivial`-completion mechanism and the grandfather boundary (including the six pre-logging tasks that appear in the log but never reach a clean-round streak). See `docs/plans/agent-scaffold.ledger.md` for the round records.
