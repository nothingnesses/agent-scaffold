### `validation-constraints`: state W5's waiver-ownership rule against the round log so the two owed waivers become writable (`Q-70`), then treat the validator-cluster defects the record routes here

One thing in this step blocks another step today and the rest of it does not. W5's increment-ownership rule decides whether a `[[step.waiver]]` may name a given increment by a LEXICAL STRIP of the increment's own id (`leading_slug(increment) != waiver.step`, `src/workflow.rs:564`), while W3 decides which step an increment belongs to STRUCTURALLY, from the round log (`round_step_slug` at `src/workflow.rs:119` and `round_increment_id` at `:127`). Where the two disagree, no waiver can be written at all. Two are owed on `workflow-enforcement-tier`, which cannot go `complete` until they can be written.

PROVENANCE. `Q-55-entryroute` (2026-08-11, receipt record 308) scoped a design pass to the validator cluster, meaning the W5 fix plus the three detection mechanisms, over three alternatives one of which was splitting the W5 fix into its own minimal step. Three worktree-isolated explorers on distinct lenses wrote to `docs/plans/validation-constraints.explorations/` (`Q-70-architecture.md`, `Q-70-evidence.md`, `Q-70-minimal.md`); the ledger records the pass in the paragraph found by a line-start anchored grep for "THE `Q-70` DESIGN PASS IS DONE". The human then decided `Q-70` as DIRECTION (iii), THE ROUND-LOG JOIN, over retiring the ownership rule (direction (iv)) and over building both to compare; and `Q-70-resolver` as BOUND MECHANISM 3 NOW, DESIGN IT LATER, over designing and building it in this step and over dropping it. Both are `type:"decision"` records in `docs/metrics/workflow.jsonl`; find them with `jq -r 'select(.type=="decision" and (.q_id=="Q-70" or .q_id=="Q-70-resolver"))' docs/metrics/workflow.jsonl`.

`[step.provenance].decisions` names `Q-70` and `Q-55`, because those are the registered `[[question]]` ids and the field resolves against them. The sub-decision receipts this step actually rests on, `Q-55-entryroute`, `Q-55-mechanism`, `Q-55-resumecost` and `Q-70-resolver`, are receipt ids under a registered parent rather than questions of their own, so the field cannot carry them and they are named in prose instead. That is the same convention the sub-decision ruling below settles.

## The members this step carries

NO COUNT IS STATED HERE AND NONE MUST BE ADDED. The set grows every time a human decision queues work to this step, so a figure written here expires; a maintained count of a moving set is this project's most repeated defect. The authoritative set is the ledger paragraph found by a line-start anchored grep for "THE IMMEDIATE NEXT ACTION IS THE `validation-constraints` STEP", plus every `type:"decision"` receipt in `docs/metrics/workflow.jsonl` whose `chosen` routes work here. Re-derive it rather than trusting the list below, which records where each member stood when this step was authored.

- THE W5 FIX, which the ledger routing paragraph states as "teaching W5 the structured step association W3 already uses". DECIDED as direction (iii). It is `validation-constraints-inc1`.
- THE THREE DETECTION MECHANISMS the round 3 review of `workflow-enforcement-tier-inc4` produced: the waiver-note breakdown join, dangling decision-receipt detection, and the quotation resolver. NO INCREMENT IS DECLARED FOR ANY OF THEM, deliberately; see "The three detection mechanisms" below for what the pass settled about each and what is still open.
- THE TWO PRE-EXISTING `validate` DEFECTS routed here from `workflow-enforcement-tier-inc3`: the plain-`validate` inconsistency where a mode-000 log FILE exits 1 while an unsearchable DIRECTORY over the same log exits 0, recorded in the ledger paragraph beginning "ONE ITEM IS QUEUED BY THIS DECISION RATHER THAN FIXED"; and the containment TOCTOU where a FIFO-widened mid-run symlink swap yields `workflow invariants hold` at exit 0 over a log OUTSIDE the project root, recorded in the paragraph beginning "THE PRE-EXISTING CONTAINMENT TOCTOU IS CONFIRMED PRE-EXISTING AND IS ROUTED". They are `validation-constraints-inc3`.
- THE `agent-scaffold next` DEFECTS, routed here by the human decision of 2026-07-30. The record says both three and four and records four as the measured figure, with the correction appended centrally in the routing paragraph rather than by rewriting the frozen passages, per the ledger paragraph beginning "ON THE FIVE LIVE"; re-derive the set from the two paragraphs below rather than from either figure, since nothing stops a further one being found. They are described in the ledger paragraphs beginning "THREE DEFECTS IN `agent-scaffold next`" and "A FOURTH `agent-scaffold next` DEFECT". They are `validation-constraints-inc4` and `validation-constraints-inc5`; the division is argued below.
- PROJECT IDENTITY IN THE ROUND RECORD, queued by receipt `Q-55-mechanism` (2026-07-31, `chosen` "Anchor plus refusal, identity queued"), with the recorded human constraint that the record schema W3, W4 and W5 all read must take ONE DELIBERATE EDIT RATHER THAN A RIDER ON A PATH FIX. Re-measured for this fold: `Round` (`src/metrics.rs:620-651`) carries `line`, `task`, `artifact`, `outcome`, `consecutive_clean`, `risk_class`, `step` and `increment`, and no `project`, so it is still unbuilt.
- THE SHARED ROOT CAUSE behind the resume cost, `src/main.rs:project_root_of_source`'s fallback to the plan's own parent where no `docs/plans`-shaped ancestor exists, queued by receipt `Q-55-resumecost` (2026-08-02, `chosen` "Accept as (iv), queue the shared cause") as ONE treatment joining the project-identity work rather than a fresh accepted cost per surface. Find it in the ledger by the anchored text "`Q-55-resumecost` DECIDED". Because the record queues it as one treatment with the member above, both are `validation-constraints-inc6`.

TWO `src/` DEFECTS THE `Q-70` REGISTRATION LOOP FOUND AND DID NOT FIX also belong here, recorded in the ledger paragraph beginning "TWO `src/` DEFECTS THE LOOP FOUND". The first, that W5's ownership message ASSERTS AN OWNERSHIP FACT THAT NEED NOT BE TRUE OF THE PLAN, naming a Roadmap step that does not exist, is closed BY CONSTRUCTION by inc1 and is not separately scheduled. The second, that `src/workflow.rs:553`'s sibling real-step rule is STRUCTURALLY UNREACHABLE on the TOML path, is `validation-constraints-inc2`.

ONE INPUT HAS NO OWNER AND IS CARRIED HERE RATHER THAN SCHEDULED. The LEDGER HALF of the `run_next` false green, recorded in the ledger paragraph beginning "THE BACKSTOP CORRECTED BOTH EARLIER AGENTS ON OWNERSHIP", belongs to neither queued member: project identity filters `Round` records and `[meta]` and CANNOT CHANGE WHICH LEDGER FILE `run_next` OPENS. The record states it has NO OWNER anywhere in the plan. It is named here because an input with no owner is the one most easily lost, and it is NOT declared as an increment, because nothing has decided that this step owns it. A human decides that at step entry.

## The blocker, re-measured for this fold

`leading_slug` (`src/workflow.rs:88`) strips only a `-inc<alnum>` suffix. So an increment identity whose `leading_slug` is not the step it joins under W3 CANNOT BE OWNED BY ANY WAIVER UNDER W5, whatever the plan says. THE AFFECTED POPULATION IS A PROPERTY, NOT A LIST: it is every identity in that shape, and it grows with the plan. Reproduce the current set with

```
jq -r 'select(.type=="round") | [(.step // (.task|sub("-inc[a-zA-Z0-9]+$";""))), (.increment // .task)] | join(" ")' docs/metrics/workflow.jsonl | sort -u | awk '{lead=$2; sub(/-inc[a-zA-Z0-9]+$/,"",lead); if (lead != $1) print $1, $2}'
```

TWO MEMBERS BLOCK A STEP TODAY, `workflow-enforcement-tier-fold` and `workflow-enforcement-tier-endproperty-fold`, both plan-review loops that closed under human escalation and were never declared. THE REST ARE LATENT FOR THREE DIFFERENT REASONS, and the third is the one the `Q-70` item does not model. First, W3 skips every step that is not `complete` (`src/workflow.rs:445-447`), which is why the `workflow-driver` trio (`-stage0a`, `-stage0b`, `-stage1`, all DECLARED `[[step.increment]]` entries that carry no `-inc<alnum>` suffix) does not fire. Second, the same skip hid the two blockers while `workflow-enforcement-tier` was `in-progress`; flipping the status is what exposed them. THIRD, AND THIS IS THE ONE TO CARRY: a member can sit inside an ALREADY-`complete` step and stay silent because its increments CONVERGED, so no waiver was ever needed and W5 was never consulted about it. The current instance is `decision-folder-currency-fold`. FLIPPING A STEP TO `complete` IS THEREFORE NOT SUFFICIENT TO EXPOSE A MEMBER, and a reader who takes the first two reasons as exhaustive will under-estimate the population.

## The decided fix

Direction (iii), stated against the round log rather than against the plan. All three explorers reached it independently from different lenses, and each BUILT it rather than proposing it.

1. `run_checks` (`src/workflow.rs:206-221`) ALREADY HOLDS the `rounds` slice and already hands it to `w3_problems`; it simply does not hand it to `w5_problems`, which today takes `waivers`, `steps` and `escalations` (`src/workflow.rs:544-548`). Widen the signature and pass it through.
2. EXTRACT THE SHARED PREDICATE. The relation the fix needs already exists inside W3's covering-waiver match at `src/workflow.rs:498-502`, which compares the waiver's `(step, increment)` pair against the round-log grouping structurally. Extract it into one named predicate over `round_step_slug`/`round_increment_id` and have BOTH W3 and W5 consult that one implementation, so the two cannot drift apart again. This is the repository's own established idiom for this class: `peak_consecutive_clean` is `pub(crate)` precisely so `next` and W3 run identical arithmetic, and `WaiverReason::required_tier` is single-sourced between W5 and `src/plan/source.rs`.
3. REPLACE the lexical comparison at `:564` with a lookup into that predicate, so every refusal W5 emits is backed by a record rather than by a substring. The message must stop naming a step derived from the id, which is the first of the two `src/` defects above.
4. The rule text moves with the code; see Documentation impact.

WHY THE FIX SHIPS FIRST AND ALONE, which is the pass's central finding and the reason inc1 is sequenced by itself. The coupling question the pass was commissioned to answer is a CONSEQUENCE OF THE DIRECTION rather than an empirical unknown: under (iii) the W5 rule and the prospective waiver-note join resolve through the SAME round-log predicate, so they share a join; under (i) they would key on two different sets. And the recorded schema constraint binds the OTHER pair, not this one: project identity adds `project` to `Round` and the note join needs `valid_findings` on the same `Round`, so THOSE two couple, while direction (iii) ADDS NO FIELD TO ANY STRUCT. `Q-55-mechanism`'s "one deliberate edit rather than a rider on a path fix" therefore binds the note join and project identity and DOES NOT BIND THE W5 FIX.

## Declaring the two fold tokens is necessary, not merely insufficient

`Q-70`'s escape route 4 records that declaring `workflow-enforcement-tier-fold` and `workflow-enforcement-tier-endproperty-fold` as `[[step.increment]]` entries does not admit the owed waivers. That is true against the shipped binary and it is only half the fact. A SECOND VALIDATION PATH runs on the TOML substrate: the per-step waiver loop at `src/plan/source.rs:791-856` builds the step's declared increment set (`:792-793`) and checks the waived increment's membership in it (`:807-811`). That check is a HARD GATE no change to W5 can bypass, and two explorers measured it firing against a direction (iii) build with the tokens left undeclared. So DECLARING IS REQUIRED UNDER EVERY DIRECTION, and every proposal's edit surface includes a plan edit as well as a source edit.

THE ORDER MATTERS AND IS PART OF INC1. Declaring alone leaves W5 refusing; fixing alone leaves the source path refusing; the two together return `workflow invariants hold` at exit 0, which two explorers measured end to end on a fixture carrying both declarations, both owed waivers and the step flipped to `complete`. The declaration and the status flip are the orchestrator's own direct-on-main integration edits; the two waivers are plan content and are authored by the planner. Their drafted notes are already in the ledger, found by a line-start anchored grep for "TWO WAIVERS ARE OWED AND CANNOT YET BE WRITTEN", so the next agent does not have to reconstruct them. NEITHER WAIVER MAY BE WRITTEN BEFORE THE CODE LANDS: writing them first turns `validate --workflow` red, which is the state this step exists to end.

ONLY THE OWNERSHIP CHECK BLOCKS THEM, which bounds the fix. Each fold token already has a `type:"escalation"` record whose `task` equals it and whose `human_decision` is `decision`, and which carries no structured `increment` id, so W5's record-backed evidence join resolves through the `task` fallback (`escalation_increment_id`, `src/workflow.rs:141`) and passes.

## The known behaviour change, and the one point the pass left open

DIRECTION (iii) IS NOT BEHAVIOUR-NEUTRAL, and `Q-70` prices it as though it were. Under the lexical rule, an increment-unit waiver whose increment has NO round records at all passes W5 silently, because the rule only compares strings. Under the round-log rule there is nothing for it to join to, so the fix NARROWS WHAT A WAIVER MAY COVER. There is no live regression: one explorer ran its prototype against the unmodified plan and log and got `workflow invariants hold` at exit 0, so every waiver currently committed to this plan satisfies the new rule with no edit to any waiver and no edit to the append-only log. Re-establish that before relying on it, with the whole live plan as the fixture.

THE OPEN POINT, RECORDED AS OPEN BECAUSE THE PASS DID NOT SETTLE IT AND THE HUMAN HAS NOT DECIDED IT. The explorers' prototypes DIVERGED on the unobserved case. Two REPORT it, on the ground that it is the only thing that catches a typo'd increment id on the Markdown and JSONL substrates, where no declared set exists to catch it. One leaves W5 SILENT there, on the ground that the increment has nothing to attribute yet and that `src/plan/source.rs:807` already catches the undeclared case on the TOML path; that explorer measured the residual honestly, namely that an increment both DECLARED and never logged would then pass W5 silently. The implementer states which it took and argues it, or raises it as a fork. The step's default is to REPORT, because it is the substrate-uniform answer and because the narrowing is then visible rather than implicit, and it is carried with a test either way (acceptance check 4).

THIS OPEN POINT ALSO EXPLAINS A DISAGREEMENT IN THE MEASURED EDIT SURFACE, so do not treat the difference as one explorer being wrong: the number of existing W5 unit tests that need a round-log fixture rather than a mechanical empty slice IS A FUNCTION OF THIS CHOICE. Under the reporting form, every test that constructs an increment-unit waiver with no log needs a fixture; under the silent form, only the test that exercises a genuine mis-scoped waiver does.

## The increments, and why in this order

The division follows the rule this plan already uses: an increment is a unit whose review asks ONE question.

- `validation-constraints-inc1`, THE W5 OWNERSHIP FIX AND THE UNBLOCKING. The shared predicate, the widened `w5_problems`, the replaced rule body and its two fact-backed messages, the tests, the rule text in its three prose copies, and then the plan-side unblocking: the two `[[step.increment]]` declarations, the two owed waivers, and `workflow-enforcement-tier` flipped to `complete`. The review question is "does the ownership rule now assert a fact the round log records, and does the blocked step actually unblock".
- `validation-constraints-inc2`, THE SIBLING REAL-STEP RULE AT `src/workflow.rs:553`. On the TOML path a `[[step.waiver]]` inherits its `step` from the containing `[[step]]` (`step: step.slug.clone()`, `src/workflow.rs:258`) and the typed `Waiver` struct (`src/plan/source.rs:279-300`) is `deny_unknown_fields` with no `step` field at all, while `check_workflow_toml` derives both `plan.step_views()` and `waivers_from_toml(plan)` from the same `plan.steps`, so the rule cannot fire there. THE TREATMENT IS NOT PRE-DECIDED. One reading is that an unreachable rule is a success rather than a bug, the substrate having made the bad state unrepresentable, in which case the work is to say so where the rule is documented and to keep it live on the JSONL substrate, where `step` is a free string and the state IS authorable. The other is that a rule whose premise cannot hold on the project's own substrate should be restructured. The implementer takes one, argues it, or raises the fork. The review question is "is the rule's reachability now described truthfully, on both substrates".
- `validation-constraints-inc3`, THE TWO PRE-EXISTING `validate` LOG-PATH DEFECTS. The mode-000 file versus unsearchable directory inconsistency, and the containment TOCTOU. They are one increment because both are about what plain `validate` and `validate --workflow` conclude from a log path they cannot fully resolve, and a reviewer who has built the fixture for one has most of the fixture for the other. The review question is "does the tool report the same thing about a log it cannot read, whichever way it cannot read it, and can the containment guard still be defeated between the check and the use".
- `validation-constraints-inc4`, THE `agent-scaffold next` TRUTHFULNESS DEFECTS: the false completion recommended from a converged last increment while the sidecar commits to unbuilt work, the oversized output from dumping the ledger resume state verbatim, and the calm exit reporting "no plan source" when `--source` is omitted. One review question, "does `next` say something true and proportionate", and one surface.
- `validation-constraints-inc5`, THE UNUSED `blocked_by` FIELD. Split from inc4 for a reason the record itself gives: populating `blocked_by` is PLAN CONTENT and so planner work, whereas inc4 is source work, so the two are authored by different roles against different artifacts and reviewed by different questions. Re-measured for this fold with line-start anchored greps, since the naive form has an exception in prose: `grep -c '^\[\[step\]\]$'` and `grep -c '^blocked_by = \[\]$'` over the plan TOML return the same number, so the field is empty on every step and populated on none. THE TREATMENT IS NOT PRE-DECIDED: populate it, teach `next` to honour it, retire the field, or some pair of those. The review question is "does the structured source now encode the ordering that today lives only in ledger prose".
- `validation-constraints-inc6`, THE ONE DELIBERATE RECORD-SCHEMA EDIT AND THE SHARED ROOT CAUSE. Project identity on `Round` and on `[meta]` with the join filtered in `check_workflow_toml`, plus `project_root_of_source`'s fallback, which the record queues as one treatment with it. Its three recorded limitations are inherited from the `workflow-enforcement-tier` explorer that built it: it is opt-in, so an unmigrated plan is unprotected; a filter placed inside `run_checks` does not cover `next`, which derives its forward verdict independently; and `decisions` and `escalations` are read unfiltered. The review question is "does an identity that is declared actually separate two projects sharing one log, and does an identity that is absent change nothing".

THE ORDER. INC1 IS FIRST AND ALONE, on two grounds: it is the only member that blocks a step today, and the pass established that it ships without coupling to the rest, since direction (iii) adds no field to any struct and so is not bound by the one-deliberate-edit constraint that governs the other members. INC2 FOLLOWS IT because it is a claim about the same function that inc1 has just changed, and the cheapest moment to state the reachability of one rule truthfully is while its sibling is fresh. INC3, INC4 AND INC5 ARE INDEPENDENT of each other and of inc1; the order between them is NOT load-bearing and is stated as a default rather than a constraint, so re-ordering them needs no decision. INC6 IS LAST AMONG THE DECLARED INCREMENTS, and this edge IS load-bearing: the human's recorded constraint is that the record schema takes ONE deliberate edit, the prospective waiver-note join needs `valid_findings` on the same struct, and whether that join is built at all is undecided. Landing the schema edit before that is decided risks being the first of two edits, which is the thing the constraint exists to prevent. INC6 THEREFORE HAS AN ENTRY GATE: put the note-join question to the human before it starts.

## The three detection mechanisms

NONE OF THE THREE HAS A DECLARED INCREMENT, and that is deliberate rather than an omission. They are in this step's scope by the human's `Q-55-entryroute` decision, which this step relays and does not reopen. What the pass ruled about each is recorded here so the eventual build starts from it.

MECHANISM 1, THE WAIVER-NOTE BREAKDOWN JOIN, checking a waiver note's `<total> (<r1>, <r2>, ...)` per-round breakdown against the `valid_findings` of that increment's round records. IT IS A REGRESSION GUARD, NOT A DEFECT FINDER: every breakdown currently in the plan agrees with the records. Re-measured for this fold: enumerate the breakdowns with `grep -onE "[(][0-9]+(, [0-9]+){1,6}[)]" docs/plans/agent-scaffold.plan.toml` and compare each against `jq -r --arg t <increment-id> 'select(.type=="round" and ((.increment // .task)==$t)) | .valid_findings' docs/metrics/workflow.jsonl`; every one agreed. NO COUNT OF THE NOTE SITES IS STATED, because the population grows with every escalated loop that earns a waiver. ITS JOIN IS THE PREDICATE INC1 BUILDS, so under the decided direction the join costs nothing; ITS DATA IS NOT SHARED, since `Round` carries no `valid_findings` and `parse_rounds` (`src/metrics.rs:660-711`) discards it even though `check_record` requires it in the raw record. That data half is the coupling the human's one-deliberate-edit constraint governs, and it belongs with inc6 if it is built at all. WHETHER IT IS BUILT IS NOT DECIDED.

MECHANISM 2, DANGLING DECISION-RECEIPT DETECTION. ITS RED LIST IS EMPTY TODAY under the parent-resolution reading, so it too is a regression guard rather than a defect finder. THE SUB-DECISION RULING, which all three explorers reached independently and which changes what the mechanism is: a receipt `q_id` of the form `Q-<n>-<suffix>` whose parent `Q-<n>` is registered is a LEGITIMATE SUB-DECISION CONVENTION, not a dangling receipt. Re-measured for this fold, and the figure had already moved since the explorations were written, which is why no figure is repeated here: extract the receipt ids with `jq -r 'select(.type=="decision") | .q_id' docs/metrics/workflow.jsonl | sort -u`, extract the registered ids with `grep -oE '^id = "Q-[^"]+"' docs/plans/agent-scaffold.plan.toml`, take the set difference, and check the two properties rather than the size: every unresolved id is `Q-<n>-<suffix>` shaped, and every parent `Q-<n>` is registered. Both held. The pass's own measurement that every unresolved id shared the single parent `Q-55` NO LONGER HOLDS, because `Q-70-resolver` was written the same day; the STRUCTURAL property survived the change and the enumeration did not, which is the whole argument for stating properties rather than sets. A detector built without the parent clause would be red on the project's own dominant convention.

MECHANISM 3, THE QUOTATION RESOLVER, is BOUNDED BY THIS STEP AND DESIGNED LATER, by the human's `Q-70-resolver` decision. It is the only one of the three that reaches the dangling-quotation class, which no gate in this repository catches. A DESIGN PASS IS OWED BEFORE IT IS BUILT; when that pass runs and who registers it are not settled here. Its three measured design constraints, recorded so the later pass starts from them rather than rediscovering them:

- ITS OPENING RED LIST IS THE WHOLE STALE-CITATION SET, not a subset. `Q-70` records that every distinct `src/checks.rs` citation in `docs/plans/agent-scaffold.steps/checks-runner-worktree-name-collision.md` resolves to unrelated content in the current source, because that step's own fix replaced the code the sidecar cites, and two explorers spot-checked that claim without finding an exception. `Q-55-check21b` ruled those deliberately left stale and that ruling stands. Enumerate them with `grep -oE "src/checks[.]rs:[0-9]+(-[0-9]+)?" docs/plans/agent-scaffold.steps/checks-runner-worktree-name-collision.md | sort -u`, then open each at the line. A resolver whose first run reproduces an already-recorded fact has not yet earned its cost, so the pass must say what the mechanism does about an accepted-stale citation.
- THE SELF-QUOTING RECORD (constraint 3a). A resolver that asks only "does this quoted string occur in the cited file" GOES GREEN ON THE EXACT CLASS IT EXISTS FOR as soon as the record writes the defect down: when an edit deletes a sentence, the deleted sentence comes to occur again inside the post-mortem OF the deletion, so the dangling quotation resolves. A resolver must scope to LIVE passages and exclude a record's own post-mortems and round records. THIS PROJECT QUOTES ITSELF AS A MATTER OF PRACTICE, so the case is not rare, and the constraint is not confined to the ledger: it has already fired inside the `Q-70` item itself, which is why that item's own control command is corrected in the same fold that authors this step.
- EXPECTED TOOL OUTPUT IS NOT A DOCUMENT QUOTATION (constraint 3b). The validator problem strings `Q-70` quotes from its own fixtures are runtime-substituted format output and exist in full nowhere but that item and its rendered projection; the format string that produces one of them is at `src/plan/source.rs:809`. A resolver with no expected-output escape reports the item's strongest evidence as dangling. Verify with a fixed-string grep for either quoted problem string across the tree.

## The naming collision, and how this step writes it

"W6" ALREADY NAMES TWO DISTINCT UNBUILT CHECKS in the durable record: the waiver-note breakdown join above, and `Q-59`'s `session_state` transition-legality check, whose richer lifecycle state the human's 2026-07-23 decision DEFERRED behind an evidence gate rather than dropped, so the collision does not lapse on its own. Re-measured for this fold: the token `W6` occurs in `docs/plans/agent-scaffold.plan.toml` exactly once outside the `Q-70` item, and that occurrence is `Q-59`'s. THIS FILE THEREFORE NEVER USES "W6" TO NAME A CHECK. It writes "the waiver-note breakdown join" and "`Q-59`'s transition-legality check". Neither check is renumbered by this step; both are unbuilt, and a collision between two unbuilt checks costs nothing until one ships, at which point whoever ships first assigns the number once with the other claim visible. SEPARATELY AND NOT TO BE CONFLATED: the two owed waiver ids `workflow-enforcement-tier-w5` and `-w6` are WAIVER IDS continuing that step's established `-w1` to `-w4` sequence, not check names. The waiver-id convention is established and is not the thing to change.

## Risk classification

Classified once, here, so the required clean-round count is a recorded property of each artifact rather than a fresh judgement each round.

`validation-constraints-inc1` is RISKY (two consecutive clean rounds). It changes the verdict of the check the project's whole enforcement tier rests on, and its failure mode is not a crash but a confident wrong answer in either direction: a false green admits a waiver that grants an exemption nothing evidences, and a false red blocks a correct plan. It ships a KNOWN NARROWING of what a waiver may cover, which is a deliberate break rather than a strict tightening, and the narrowing's exact boundary is the one point the design pass left open, so the review has to judge a decision the implementer makes rather than only the code. It also edits three drift-guarded prose copies of a rule that ships into every scaffolded project, and this project's calibration on prose that states an enforcement boundary is its worst measured class. The counter-argument, that the code half is about thirty lines in one file and two explorers ran the full suite green on it, is about diff size and not blast radius, which this project has recorded losing before.

`validation-constraints-inc2` is RISKY (two consecutive clean rounds). Its cheapest treatment is a claim about a check's reachability on two substrates, which is a documentation-claim surface, and its other treatment edits `w5_problems` again immediately after inc1 changed it. A wrong claim here is worse than no claim: it would tell a reader that a rule guards something it cannot guard.

`validation-constraints-inc3` is RISKY (two consecutive clean rounds). The TOCTOU half is a containment guard whose failure is a `workflow invariants hold` at exit 0 over a log outside the project root, which is the false-green class this whole cluster exists to remove, and a race is not established by the obvious test. The mode-000 half changes exit codes on an error path, which is the externally depended-on contract.

`validation-constraints-inc4` is RISKY (two consecutive clean rounds). `next` emits an INSTRUCTION that an agent acts on, so a wrong answer is not a wrong report but a wrong action, and the worst of the three defects already caused exactly that: it advised marking a step complete over committed unbuilt work.

`validation-constraints-inc5` is LOW_RISK (one clean round) AS SCOPED TO PLAN CONTENT. Populating `blocked_by` changes no product behaviour today, because nothing reads the field, so a wrong value is inert and reversible in one revert. RE-CLASSIFY IT TO RISKY BEFORE IT CONVERGES if the chosen treatment also teaches `next` to honour the field or retires the field from the schema, since either makes it a change to the driver's advice or to a hand-authored schema across every step. Say which happened in the round report rather than leaving the classification to be inferred.

`validation-constraints-inc6` is RISKY (two consecutive clean rounds). It edits the record schema W3, W4 and W5 all read, under an explicit human constraint about how that edit is made, and it changes `project_root_of_source`, through which every command's path resolution flows. Its own recorded limitations mean a naive filter placement leaves `next` unprotected while appearing to protect everything, which is a silent partial fix.

## Acceptance check

Every claim below is a command with an expected exit code, so a round is settled by running it rather than by reading the diff. `Q-66` applies: a behavioural claim owes a demonstration that is RED against the pre-fix build and green after, and the round report names which mutation or which pre-fix revision produced the red. THE LIST IS A FLOOR AND NOT A TOTAL, and no total is stated: every fix round that closes a defect owes its own red-then-green beside these.

BEFORE RUNNING ANY OF THIS: point `TMPDIR` at a directory OUTSIDE any git repository. Three of the suite's tests assert their scratch directory is not inside a repository, so a worktree-internal `TMPDIR` fails them spuriously. That is a property of the suite, held by its own step `test-tmpdir-repo-assumption`, and it is not a regression here.

1. Build, suite and lint: `cargo build`, `cargo test`, and `cargo clippy --all-targets -- -D warnings`, all clean, including the drift guard `the_committed_scaffold_matches_a_fresh_render`.
2. AFTER INC1, THE LIVE PLAN IS UNCHANGED IN VERDICT, which is the no-regression check and the migration proof: `agent-scaffold validate --source docs/plans/agent-scaffold.plan.toml --workflow` exits 0 with `workflow invariants hold`, with every waiver already committed to this plan satisfying the new rule and NO edit to any waiver and NO edit to the append-only log.
3. AFTER INC1, THE BLOCKED STEP UNBLOCKS: with both fold tokens declared as `[[step.increment]]` entries, both owed waivers written, and `workflow-enforcement-tier` at `complete`, the same command exits 0 with `workflow invariants hold`. Against the pre-fix binary the identical tree exits non-zero on the ownership rule alone, and the round report quotes both.
4. AFTER INC1, THE NARROWING IS PINNED AS A DECISION, not left implicit. State which form the implementer took for an increment-unit waiver whose increment has NO round records, and pin it with a test: under the reporting form, such a waiver is REPORTED and the test asserts the message; under the silent form, it PASSES W5 and the test asserts that `src/plan/source.rs`'s membership check still catches the undeclared case, plus the residual that a DECLARED and never-logged increment passes. A test that does not distinguish the two forms has not pinned the decision.
5. AFTER INC1, THE MESSAGE ASSERTS A FACT. Reproduce the item's own fixture, a waiver naming an increment that belongs to no step, and confirm W5 no longer reports a step derived from the id. THE CONTROL GREP MUST BE LINE-START ANCHORED (`grep -c '^slug = "..."$'`), because the unanchored form matches the fixture's own description of itself in the plan and returns a false positive.
6. AFTER INC1, BOTH SUBSTRATES AGREE. Run the same shape on a Markdown plan plus a JSONL `type:"waiver"` record. The pre-fix binary refuses a correctly-scoped waiver there and the fixed binary accepts it, so the fix is not TOML-only.
7. AFTER INC1, W3 AND W5 CONSULT ONE IMPLEMENTATION. Demonstrate it by mutation rather than by reading: change the shared predicate and show that a W3 test and a W5 test both go red. If only one goes red, the predicate was copied rather than shared.
8. AFTER INC1, the plan still renders and still validates: `render --check --strict` reports up to date and `validate --source ... --workflow` exits 0.
9. AFTER INC2, the reachability claim is checkable: a reader of the rule's documentation can predict, for each substrate, whether the rule can fire, and the round report shows the fixture for the substrate where it can.
10. AFTER INC3, the two `validate` defects are closed with the fixtures the ledger records: a mode-000 log FILE and an unsearchable DIRECTORY over the same log now agree, and the FIFO-widened mid-run symlink swap no longer yields `workflow invariants hold` at exit 0 over a log outside the project root.
11. AFTER INC4, `next` no longer recommends a completion its own step's brief contradicts, its output is proportionate rather than a verbatim ledger dump, and a run with no `--source` fails loudly rather than exiting calmly.
12. AFTER INC5, the structured source encodes the ordering: state the treatment taken, and show that the answer `next` gives about which step is next follows from the structured source rather than from ledger prose.
13. AFTER INC6, a declared identity separates two projects sharing one merged log, an absent identity changes nothing, and the round report states which surfaces the filter placement does and does not cover, `next` included.

## Documentation impact

All in-repo. Each item travels with the increment that makes it stale.

INC1:

- THE RULE TEXT, IN THREE FILES THAT MUST MOVE TOGETHER. The ownership clause is stated verbatim in the pack source `pack/instrument.md` and in the two drift-guarded generated copies `AGENTS.md` and `.agents/AGENTS.reference.md`; the guard is `src/agents_md_drift.rs` and the test that fails on a partial edit is `the_committed_scaffold_matches_a_fresh_render`. Locate the three with `grep -rn "must own its .increment." pack/instrument.md AGENTS.md .agents/AGENTS.reference.md` rather than by line number. The change is one parenthetical: the clause currently says the increment's leading slug equals the step, and after the fix it states the observed relation.
- THE REGENERATION HAZARD, stated because it has bitten this repository before. Regeneration is `just scaffold-self`, whose second line runs `nix fmt` OVER THE WHOLE TREE, and this repository is NOT formatter-clean at HEAD, so a naive run reflows unrelated files. Either run the render half alone, or regenerate and then commit only the rule files, leaving the unrelated reflow uncommitted. Do not resolve this by hand-editing a generated file.
- `src/workflow.rs`'s `w5_problems` doc comment, which restates the lexical rule, and `leading_slug`'s doc comment, which already calls itself a shim that "remains only for pre-migration records that omit the structured id" and whose call sites the fix reduces.
- `src/plan/source.rs`'s waiver-loop comment. It introduces the block as rules moved from the round log's `check_record` waiver arm plus the W5 pairing, closing "one behaviour, two data representations". That reaches the presence rules and the pairing, both of which genuinely hold, and it does NOT reach the membership check, which has no `check_record` ancestor because `check_record` has no access to a step's declared increments. This is a documentation defect and its fix is one clause naming that check for what it is, an internal cross-reference of the same class as `Provenance.decisions`, which the same file already documents. It travels with inc1 because the two comments must agree about which check owns ownership after the fix.
- `CHANGELOG.md`, the `## [Unreleased]` section, which has `Added` and `Changed` and no `Fixed`. The narrowing is a behaviour change to `validate --workflow` and the population it affects must be named.
- NOT `README.md`: it states no ownership rule (`grep -c "must own its" README.md` returns 0). NOT `pack/plan-template.plan.toml` and NOT `src/plan/render.rs`: the commented waiver example and `waiver_note` move only under a waiver-schema rework, which is not the decided direction.
- THIS FILE and the `Q-70` item both describe the pre-fix tree in places. Their currency is inc1's, not a later pass's, because the claims are about the rule inc1 changes.

INC2: the doc comment or comments that state the sibling rule's scope, plus the same three prose copies IF the treatment changes what the shipped rule text says. Check before assuming it does not.

INC3: `src/main.rs:run_validate`'s doc comment and the `README.md` `validate` paragraph, both of which describe what an unreadable or unpairable log does; and `CHANGELOG.md`.

INC4 and INC5: the `next` help strings and any `README.md` text describing what `next` reports; and, for inc5, `blocked_by`'s own doc comment in `src/plan/source.rs`, which describes a typed field that nothing currently reads.

INC6: `pack/instrument.md`'s `round` record schema and its two drift-guarded generated copies, since an added field is a schema change that ships; `README.md`'s `validate`/`status` text if the filter changes what is reported; and `CHANGELOG.md`.

NOT OWED BY THIS STEP, recorded so it is not re-raised: the ledger's live "three `agent-scaffold next` defects" passages are NOT rewritten. They sit in frozen round records and decision records, and the ledger's own convention is to append a correction centrally rather than rewrite a decision record, which the paragraph beginning "ON THE FIVE LIVE" states. The correction lives in the routing paragraph.

## Scope: what this step does not do

- It does not reshape the waiver schema. No nesting of a waiver under an increment, no collapsing of the unit field, no change to the JSONL waiver arm, no rewrite of the committed waivers. The precondition for encoding ownership positionally is a declared increment set that models the identities the checks operate on, and that set is hand-maintained and derived from nothing; compare `jq -r 'select(.type=="round") | (.increment // .task)' docs/metrics/workflow.jsonl | sort -u` against the declared `[[step.increment]]` ids before assuming otherwise. Two logged identities cannot be declared at all under `is_kebab_case_token` (`src/plan/source.rs:475-477`), which forbids an uppercase byte and is pinned by `an_uppercase_increment_id_is_flagged`, while the round log uses an `-incA`/`-incB` form.
- It does not widen `plan::Step`. It is `Serialize` and is the `status --json` payload, so widening it changes a machine output contract, and nothing in the decided direction needs it. This is also why the declared-increment lookup (direction (i)) is not the fix: it would feed W5 data only one substrate possesses, against the parity property `src/next.rs:517-523` records.
- It does not make the ownership rule substrate-conditional and does not retire it. `run_checks` is deliberately ONE implementation across both substrates (`src/workflow.rs:196-200`, stated as Principle 16), and the decided direction keeps it that way.
- It does not derive the declared increment set from the round log, or rule on what a declared increment is FOR. That is a separate step and its precondition is undiagnosed.
- It does not build the waiver-note breakdown join, the dangling-receipt detector, or the quotation resolver. The first two have empty red lists today and neither is decided; the third is bounded here by the human's decision and owed a design pass.
- It does not renumber either unbuilt check, and it does not rename the `-w5` and `-w6` waiver ids.
- It does not register the `Q-<n>-<suffix>` receipt ids as questions. Under the sub-decision ruling they are a convention a detector models, not defects to be cured by registration.
- It does not rewrite, re-key or re-model the append-only round log. Every direction that required that was ruled out.
- It does not rename any already-declared increment id. Under the decided direction the `workflow-driver` trio joins correctly as it stands.
- It does not claim ownership of the ledger half of the `run_next` false green. That input has no owner in the plan and is carried above rather than scheduled.
