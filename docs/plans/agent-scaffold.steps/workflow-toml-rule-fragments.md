### `workflow-toml-rule-fragments`: widen `workflow.toml` to per-phase rule fragments and generate the `{{workflow_phases}}` section (`Q-64`, gated)

Widen `pack/workflow.toml` (today deliberately constants-only, "ONLY constants, no logic and no orderings") to carry per-phase RULE fragments, and generate the `{{workflow_phases}}` AGENTS.md section from that widened source. This is the CONTESTED margin of Q-64 Option A: the skeptic reads it as edging into the rung-2 knee, where cost overtakes benefit, because widening the constants-only spec to states/transitions/guards/templates plus a generator that synthesizes readable workflow prose from a state table is unproven work (neither `control_fragment`, which renders one arithmetic line, nor `render`, which splices verbatim sidecars, synthesizes control-logic prose), and because the workflow protocol changed THIS WEEK via Q-63, so the meta-stability precondition is not met (Q-64-synthesis sec 2, sec 5.6).

DEFERRED behind the sec-2(d) evidence gate. It un-gates only when ALL hold: (i) doc-vs-mechanism drift RECURS at a material rate AFTER the cheap single-sourcing ships, counted as incidents in `docs/metrics/workflow.jsonl` or the ledger that ESCAPED review, above a small N/month; (ii) a multi-week stretch with no structural workflow change; (iii) the AGENTS.md-section-from-spec generator PROVEN on one small slice to produce correct readable guidance. Until then, prose remains the cleaner substrate for the still-evolving logic under Prefer the cleaner long-term architecture over the smallest diff, and holding this behind the gate honors Ground decisions in evidence and Minimal by default.

Gated contested-margin item under `generated-projection` (Q-64, Option A refined). Do not build until the gate clears.
