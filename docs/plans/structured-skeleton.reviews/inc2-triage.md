# Inc 2 triage (structured-skeleton-inc2)

Triager: independent (not producer, not reviewer, not orchestrator). Change under review: commit `e395d44` on base `a780541`. Contract: the Inc 2 bullet in `docs/plans/agent-scaffold.md` (an additive optional `step`/`increment` id on new `round`/`escalation` JSONL records; W3/W5 prefer it and fall back to `leading_slug(task)`/`task` for pre-migration records).

Verification cross-check (from the diff and the reviewers' own runs, both confirmed against the change worktree at `e395d44`): 229 tests green, clippy clean, `validate --workflow` green. The join tests exercise the real over-strip path (`foo-incidental` -> `foo`) rather than passing vacuously. I did not re-run; both reviewers independently reproduced the same numbers and I confirmed the code paths by reading the diff.

## Verdicts

| Finding | Verdict | Final severity | Rationale |
| --- | --- | --- | --- |
| O1 | VALID | low | Confirmed: `round_step_slug`/`round_increment_id` (and the escalation pair) fall back per-field with no coupling, and `require_structured_ids` checks each field independently, so an `increment`-only record resolves its STEP join via `leading_slug(task)` and re-introduces the T3 over-strip on the step axis when `task` ends `-inc<alnum>`. The contract ("When either is present it must be a non-empty string") deliberately permits independent optionality, so the code is contract-correct; the genuine gap is that this partial-record join behavior is undocumented and untested, i.e. the residual over-strip on the unfilled axis is an accident rather than a pinned, chosen outcome. Low because it is data-dependent, fails toward a false W3 FAILURE (safe direction), and the instrument.md doc introduces the two ids as a pair so the orchestrator normally writes both. Overlaps S3 (see below). |
| O2 | DISMISSED | low (as recorded) | Confirmed: `parse_rounds`/`parse_escalations` accept any non-empty `step`/`increment` with no cross-reference to a declared registry, so a typo drops a round from W3's view. But the plan EXPLICITLY defers resolvability to Inc 4 ("W3 joins rounds to the declared `[[step.increment]].id` ... Inc 4 uses whatever join exists and does not hard-depend on Inc 2"), and the resolvability target (a declared increment-id registry) does not exist until the TOML lands in Inc 4, so an Inc 2 resolvability check is not even well-defined for the increment axis. The fail direction is safe (a mislabeled `step` -> pause.md false FAILURE, not a false pass; a mislabeled `increment` splits a loop and trips `round_log_consistency_problems`). The deferral is legitimate; this is a correctly-recorded future gap, not an Inc 2 defect. The reviewer himself framed it as "fine to defer, but note the gap" and did not ask for an Inc 2 fix. No action required in Inc 2. |
| S1 | VALID | low (down from medium) | Confirmed: `pack/instrument.md` (the `{{instrument}}` source template) was updated for both bullets, but the deployed `AGENTS.md` (ownership=working) and `.agents/AGENTS.reference.md` (ownership=reference, per `pack/pack.toml`) still carry the pre-Inc-2 short bullets (grep for "structured skeleton" returns 0; the escalation bullet is the old one-line form). The reference copy is contractually always-fresh, so it is stale against its source until regenerated. The neutral context tells me WHO regenerates (the orchestrator's `just scaffold-self` at merge) and WHEN, not WHETHER it must happen: the increment is not complete until the deployed docs match the template, so this is a VALID tracked completion gate, not an implementer coding defect. Re-severitised to low (from medium): it is a pure mechanical docs regen with a benign fail direction (old records still validate; new records simply keep using the shim, which still works), and the blast radius is only this repo's own dogfooded copies (a fresh scaffold reads the corrected `pack/instrument.md`). It stays a HARD completion gate regardless of severity: if the regen is forgotten at merge, this repo's own future rounds silently omit the fields and keep T3 alive here. |
| S2 | VALID | low | Confirmed: `pack/instrument.md`'s `waiver` bullet still describes W5's scope check as "the escalation's `task` equals the waived increment, or its leading slug equals the waived step" (the pre-Inc-2 algorithm), while the W5 code now routes the escalation side through `escalation_increment_id`/`escalation_step_slug`, which prefer the structured id. The `workflow.rs` doc comment for this clause WAS updated in the diff; the prose doc was not, so template and code disagree (Principle 19). Benign in direction (code is more permissive than the doc claims), hence low. Shares a regen tail with S1: fixing the bullet in `pack/instrument.md` only reaches the deployed copies through the same scaffold-self regen. |
| S3 | VALID | low | Confirmed: the four metrics tests cover both-fields-present and empty-single-field, and the two projection tests cover both-present and both-absent, but nothing asserts that validation ACCEPTS a record carrying exactly one field (`step` present / `increment` absent, or vice versa). The contract makes each field independently optional, so a regression that accidentally couples the two checks would pass unnoticed (Principle 11). Low. Overlaps O1: S3 is the validation-acceptance half, O1 is the join-semantics half of the same untested single-field case; judged together and remediated together. |

## Tally

Valid: 4 (all low). Dismissed: 1 (O2, a legitimately Inc-4-deferred gap). No high or critical findings, so no dismissal requires second-triager re-check.

## What each VALID fix must achieve (scope, not a patch)

- O1 + S3 (judged together, the single-field case). Add tests that pin the independent-optionality behavior the contract permits: (a) validation ACCEPTS a `round` (and an `escalation`) carrying only `step` and only `increment`; (b) the join uses the structured id on the filled axis and the `leading_slug`/`task` shim on the unfilled axis, so an `increment`-only record whose `task` ends `-inc<alnum>` demonstrably still over-strips on the STEP axis. Document (in the `Round`/`Escalation` field docs or the join-helper comments) that the two ids are independent and a single-field record falls back on the other axis, so the residual over-strip is a known, chosen outcome. Do NOT require the two ids to co-occur in `require_structured_ids`: that would contradict the contract's "when either is present" wording.

- S1. Before Inc 2 is called complete, regenerate the deployed `AGENTS.md` and `.agents/AGENTS.reference.md` from the updated `pack/instrument.md` (the orchestrator's scaffold-self step at merge) so the reference copy's ownership contract holds and this repo's own instrumentation docs describe the new `step`/`increment` fields. Track it explicitly as a merge-gate item so it is not dropped.

- S2. Update the `waiver` bullet's W5 scope-check description in `pack/instrument.md` so the escalation side reads via the structured id first (for example "the escalation's structured `increment` id, or `task` when absent, equals the waived increment; or its structured `step` slug, or `leading_slug(task)` when absent, equals the waived step"), or cross-reference the `escalation` bullet's structured-id description. This is the same file S1 regenerates, so land it before the S1 regen so one regen propagates both.
